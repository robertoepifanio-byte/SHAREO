# ShareO — Status do Projeto

**Atualizado em**: 2026-08-24 — **🔎 A lição do dia tem gatilho claro: número repetido de memória e estado de sistema do tipo "nunca rodou" / "já existe" são as duas classes de afirmação que falham — e a fonte quase sempre está a uma consulta de distância.** Três vezes hoje: eu disse "~177 reservas com período invertido" (eram **46**, o banco sabia); repeti o dia inteiro que "nada do caminho de pagamento jamais rodou contra a Stripe" (a `stripeEventQueue` tinha eventos de checkout processados desde 21/08); e "corrigi" a contagem de PRs para 5 lendo UTC, quando `00:26Z` no Brasil é 23/08 — **são 4**. Nenhuma correção veio de raciocínio: vieram de abrir o `STATUS.md`, o banco e a fila. **A regra vale também na direção de publicar, não só de afirmar** — o caso mais caro do dia foi um *dado copiado de um rascunho direto para um documento legal*: a razão social que eu ia publicar (`ShareO Marketplace de Aluguel Ltda.`) veio do RIPD, escrito **antes de a empresa existir**; a Receita registrou `SHAREO MARKETPLACE DE INTERMEDIACAO DE NEGOCIOS LTDA`. Num bloco cuja única função é identificar a PJ, o nome errado **anula o propósito**, ficando pior que não ter bloco. 👉 **Antes de afirmar um número ou publicar um dado, saber de que fonte ele veio.** **7 PRs mesclados hoje** ([#354](https://github.com/robertoepifanio-byte/SHAREO/pull/354) e [#360](https://github.com/robertoepifanio-byte/SHAREO/pull/360)–[#364](https://github.com/robertoepifanio-byte/SHAREO/pull/364), mais este) — o #352 NÃO entra: mesclou 00:26Z, que no Brasil é 23/08 às 21:26, e a entrada de ontem já o conta.

**🎯 O B1 FECHOU — o D4 volta a ser o único bloqueador de go-live, e maior do que era.** O B1 era *constituir a PJ* **+** *contratar o PSP*; a PJ estava feita desde 11/08 e a metade travada era o **contrato do Mercado Pago**. O fundador decidiu que **o MP não será utilizado**, e o contrato **ficou sem objeto**: a Stripe se formaliza por aceitação eletrônica no cadastro, sem instrumento à parte. Sobrou uma **confirmação, não uma negociação** — que a conta plataforma estivesse no CNPJ da PJ, confirmada no mesmo dia. 🪤 **Mas trocar de PSP não é trocar de nome nos documentos:** o parecer ([`parecer-juridico-revisado-mp.md`](juridico/parecer-juridico-revisado-mp.md)) e o RIPD foram escritos com o MP, entidade **brasileira**; a Stripe é **estrangeira**, então o compartilhamento vira **transferência internacional (LGPD art. 33)** — outra base legal, outra análise, e ela **volta para o D4** (PSP-01 no [backlog](backlog-atividades-priorizadas.md); atinge [`transferencia-internacional-dados.md`](juridico/transferencia-internacional-dados.md) e o `rascunho-ripd.md`). Por isso o parecer ganhou **ressalva no topo em vez de ser marcado obsoleto**: o racional continua valendo, só a hipótese de base mudou.

**EVAL-P0-01 fechado:** até hoje o CNPJ não aparecia em **nenhum `.tsx`** do projeto — as menções em `/termos`, `/privacidade` e `/politicas` eram todas sobre o CNPJ *do usuário PJ*. Agora razão social, CNPJ e sede aparecem nessas três páginas e nas duas telas equivalentes do app, de fonte única (`LEGAL_ENTITY` em [`lib/legal-config.ts`](../lib/legal-config.ts)) espelhada em `apps/mobile/lib/legalConfig.ts`, com teste que compara os dois e falha se divergirem.

**💥 Mercado Pago removido por completo — código, schema e banco (−1.417 linhas)**, revogando a decisão nº 6 da [ADR-028](adr/ADR-028-reversao-stripe-connect.md), que o preservava dormente "caso fosse preciso reavaliar". Saiu tudo: SDK, rotas, componente, flag, 9 colunas, a fila de eventos e a linha de config, pela migração `20260824190000_remove_mercado_pago`; os dados apagados eram de sandbox, com escopo autorizado. 🔑 **Ensaiei o SQL contra o banco real dentro de transação com rollback forçado, e foi isso que pegou o defeito sério:** eu havia escrito `platform_config` no singular, e a tabela é `platform_configs` — a migração abortaria, o `migrate deploy` a marcaria como FALHADA e isso **travaria todos os deploys seguintes** até alguém editar a `_prisma_migrations` à mão. 🪤 E aposentar a decisão exigiu **caçar a premissa em mais cinco lugares** (checklist de go-live, manual, e 3 pontos da avaliação multiperfil) — corrigir só a linha do item teria deixado o documento certo brigando com os que se lê no dia a dia.

**🔴 O dado de maior alcance do dia, e ele não é uma entrega:** o repasse ao proprietário **nunca rodou uma única vez**. Levantamento no staging: **35 repasses em `PROCESSING`, 3 em `PENDING`, nenhum `COMPLETED`, e ZERO `PlatformTransaction`** — `createOwnerTransfer` jamais foi executado. E das **69 reservas `PAID`, apenas 4 passaram por checkout real**; as outras foram marcadas por seed e script, o que **envenena qualquer leitura financeira do staging**. O checkout, esse sim, já funcionou: a fila tem eventos `checkout.session.completed` processados desde 21/08.

**ATOR-03 e ATOR-04 fechados — e o ATOR-03 quase saiu com um defeito de dinheiro.** Aprovar extensão não cobrava nada; agora **a extensão só vale depois de paga** (reserva já paga vai a `AWAITING_PAYMENT` e o `endDate` só se move quando o pagamento confirma; reserva não paga recalcula e o checkout normal cobra tudo). **A revisão pegou o defeito antes de ir ao ar:** eu inflava o `ownerNetAmount` somando as diárias extras, mas esse dinheiro está em **outra cobrança** — e a Stripe **exige** `source_transaction` no Brasil, ligando a transferência a UMA cobrança. Extensão pequena: a transferência passa e **come a taxa da ShareO**. Extensão grande: a Stripe recusa e **ninguém recebe, em silêncio**. Virou **um `Payout` por cobrança** (`payouts.sourcePaymentIntentId`), com as duas fatias somando exatamente o líquido. A mesma revisão achou que a idempotência do webhook era **acidental** (só não duplicava porque o `endDate` já teria se movido) — virou guard explícito por estado. 🪤 **Schema e código foram em PRs SEPARADOS de propósito:** o `deploy.yml` sobe o código (linha 230) **antes** de rodar `migrate deploy` (linha 244), então juntos causariam P2022 e **erro 500 na página de reserva** para todo mundo na janela entre os dois.

**46 reservas com período invertido corrigidas** (resíduo do `mark_active` antes do #345). 🪤 A distinção entre os dois casos não era preciosismo: **uma das afetadas tinha extensão de prazo APROVADA**, e aplicar a fórmula em bloco teria apagado a extensão em silêncio — ela ficou `22/08 → 27/08`, a fórmula daria `24/08`. Sem impacto financeiro: o único uso de `startDate` em dinheiro é o cancelamento, permitido só em PENDING/CONFIRMED, e as 46 estavam em COMPLETED ou DISPUTED.

**Roteiro de teste da Stripe criado** ([`guias/roteiro-teste-stripe-ponta-a-ponta.md`](guias/roteiro-teste-stripe-ponta-a-ponta.md)) **e fase 1 verificada ao vivo** pelo fundador: pagamento com sessão `cs_test_`, webhook `COMPLETED` na fila, período coerente, contas fechando (7000 = 1050 + 5950), foto obrigatória cumprida, repasse criado. O roteiro divide o trabalho de propósito — o fundador clica, o assistente confere o banco —, porque é essa conferência que separa "a tela mostrou" de "o estado gravou". **A fase 1 já rendeu um defeito:** uma reserva **já paga e com o item já retirado** exibia "Assinatura do contrato pendente". O guard de `mark_active` consulta a flag `rentalContractAcceptanceEnabled`; o banner não consultava nada. Com a flag OFF (default, gated D4), a tela **prometia uma obrigação que nada impõe** — num documento de locação, pior que não ter banner. Corrigido no [#363](https://github.com/robertoepifanio-byte/SHAREO/pull/363), com a config pública passando a expor a flag para o app também. **A fase 3 (extensão paga) rodou até a metade e rendeu o segundo defeito do dia.** O que a correção do ATOR-03 promete **funcionou**: o banner "Extensão aceita — falta pagar" apareceu, **a data de devolução NÃO se moveu** (seguia 25/08 enquanto a extensão é para 29/08) e o valor saiu certo (4 diárias × R$ 35 = R$ 140). Mas clicar em "Pagar diárias extras" devolvia **"Erro interno."**: a Stripe exige `expires_at` da Checkout Session **entre 30 minutos e 24 horas**, e a cobrança avulsa usava **72h** — a sessão nem chegava a nascer. 🔑 **A lição não é o número: é que copiar um valor de código NUNCA EXERCITADO propaga o defeito dele.** As 72h vieram da cobrança de taxa de atraso, onde o erro estava latente desde sempre porque ninguém nunca a disparou; o checkout da locação, que funciona, usa 30 minutos e estava do lado. Corrigido nos dois no [#364](https://github.com/robertoepifanio-byte/SHAREO/pull/364), com teste que fixa o **intervalo aceito pela API** em vez da constante. 🪤 Ao pedir o `CRON_SECRET` para disparar o cron à mão, mandei o fundador buscá-lo no Vercel **minutos depois de nós dois constatarmos que valores `Sensitive` não são legíveis lá**; ele acabou colando um `whsec_` de teste em texto puro. Varredura confirmou o repositório limpo e o `settings.local.json` sem ocorrência. **Lição: quando a instrução envolve segredo, verificar antes se ele é recuperável.**

**Evidência:** `tsc` e lint limpos em todos os PRs · **1.108 testes web** (67 suítes) e **652 do app** (33 suítes) no fim do dia · migrações **ensaiadas contra o banco real com rollback** antes do merge · migração do MP **verificada no banco após o deploy** (0 colunas `mp*`, tabela removida, 102 reservas e 38 repasses intactos) · bloco de identificação da PJ **verificado no ar** nas três páginas do staging · deploy final servindo `fa54612`, o commit mais recente.

**Aberto, por depender de decisão ou de terceiro:** **D4** (agora com a transferência internacional dentro dele) · **LEGAL-02** — o contrato de locação e o consentimento de biometria ainda nomeiam a empresa que nunca existiu, e são textos versionados e hasheados, matéria do jurídico · **PSP-02** — o app ficou sem caminho de pagamento próprio, e o retorno por deep-link **nunca existiu**, então portar o checkout Stripe é trabalho novo, não paridade · **ATOR-02** — `mark_active` não confere pagamento, travado por o E2E não ter caminho de pagamento.

**⏳ Continua NÃO verificado:** que um evento de conta do Connect (`v2.core.account[…]`) **chegue de fato** — o escopo `@accounts` foi corrigido no #352, mas a fila só tem os pings. Os eventos `COMPLETED` desta leva são de **checkout**, não de Connect; a pendência de 23/08 segue aberta, e é a fase 5 do roteiro. **Estado: 5 PRs abertos, todos do Dependabot** — os 7 do dia foram mesclados ([#354](https://github.com/robertoepifanio-byte/SHAREO/pull/354), [#360](https://github.com/robertoepifanio-byte/SHAREO/pull/360)–[#364](https://github.com/robertoepifanio-byte/SHAREO/pull/364) e este), com o deployment servindo `6cd395b` e a config pública já devolvendo `rentalContractRequired` — **verificado por resposta da API, não pelo check verde** (o run do commit de docs fechou antes e daria falsa confirmação).

**🗓️ 23/08 (ciclo de locação ao vivo):** **🔁 O fundador percorreu o ciclo de locação inteiro no staging, como locador e locatária, e o padrão que apareceu vale mais que qualquer correção isolada: o caminho de exceção é sempre o que esquece a regra.** `confirm_return` criava `Payout`, `resolve_completed` (resolução de disputa pelo admin) **não criava nenhum** — o proprietário ganhava a disputa e nunca recebia, sem erro, sem log, sem linha no banco. `cancel` gravava `refundAmount`, `resolve_cancelled` **não gravava** — e como o estorno é executado **à mão** no painel da Stripe, isso significa que não havia o que executar: o locatário ganhava a disputa e nada dizia quanto devolver. `mark_active` conferia o código de retirada mas **não olhava o contrato** — visto ao vivo, com `activatedAt` 14:15:24 e `contractSignedAt` 14:18:14, ou seja, a locatária assinou **três minutos depois** de já estar com o item. Cada branch foi escrito olhando só o caso feliz. **Regra que fica: ao mexer em qualquer transição de reserva, procurar o irmão** — o caminho administrativo ou de exceção que chega ao mesmo estado — e conferir se ele aplica as mesmas regras. **11 PRs mesclados hoje** ([#341](https://github.com/robertoepifanio-byte/SHAREO/pull/341) e [#343](https://github.com/robertoepifanio-byte/SHAREO/pull/343)–[#352](https://github.com/robertoepifanio-byte/SHAREO/pull/352) — o #342 é de 22/08). **O achado mais sério do dia:** uma locação fechou o ciclo completo com **zero fotos** — a locatária marcou "item limpo e no estado recebido", devolveu, e **em seguida abriu disputa dizendo que não funcionava**. Não existia uma única imagem para o time de mediação arbitrar. O checklist exigia 3 de 4 itens e "Fotos do estado atual tiradas" era justamente o que dava para deixar em branco; o upload era **opcional** — apesar de o cabeçalho do próprio `ReturnChecklist.tsx`, na linha 6, já dizer "+ upload de foto". A documentação do componente descrevia uma regra que o código não aplicava. **[#349](https://github.com/robertoepifanio-byte/SHAREO/pull/349) tornou a foto obrigatória na API** (`mark_returned` → 422 `RETURN_PHOTO_REQUIRED`), não só na tela: o app mobile e qualquer chamada direta passam pelo mesmo ponto. **O alcance foi grande e foi conferido ANTES de escrever o guard** — 6 specs de E2E chamavam `mark_returned` direto pela API sem foto, e **4 scripts de simulação também parariam no meio do ciclo, incluindo o robô diário, em silêncio, porque ele só registra o status e segue**. O `features-jun09` não precisou de ajuste: o `superRefine` do Zod devolve 400 antes do guard. **Mas a correção criou um defeito no dia seguinte, encontrado pelo fundador:** com a foto obrigatória, o item "Fotos do estado atual tiradas" continuou sendo **pergunta manual** — anexar a imagem deixava a caixinha vazia, faltava 1 de 3, e a tela **empurrava para marcar "Caixa/embalagem original (se aplicável)" num item sem caixa**. A interface induzia a uma declaração falsa para liberar o botão, o oposto do que a mudança pretendia. E a mesma caixinha **sempre pôde ser marcada sem foto nenhuma** — era um dos caminhos para fechar a devolução sem prova. **[#350](https://github.com/robertoepifanio-byte/SHAREO/pull/350) tornou o item DERIVADO da foto**: marca sozinho quando há imagem, não é clicável, relata o fato em vez de pedir promessa. **O dinheiro da disputa foi extraído para `lib/payout.ts`** e chamado nos dois caminhos, porque duplicar garantiria a próxima divergência; junto vieram dois defeitos do bloco original — o `if (ownerAccount && …)` **sem `else`**, que fazia o repasse sumir calado quando faltava conta de recebimento, e o fato de `Payout.bookingId` **não ser único no schema**, o que permitia repasse duplicado assim que existissem dois caminhos criando um. **Decisão do fundador sobre o estorno da disputa a favor do locatário: INTEGRAL, de propósito sem `calcRefund`** — a escada de cancelamento (100%/70%/50% por proximidade da retirada) pune quem desiste em cima da hora, mas em disputa a demora é do processo de mediação e o caso **só existe depois da retirada**, então a escada daria 50% a quem o administrador acabou de dar razão. **O guard do contrato ficou atrelado à MESMA flag `rentalContractAcceptanceEnabled`** (OFF por padrão, gated D4): com ela desligada nada muda, e há teste para os dois lados — sem isso, ligar a trava por engano passaria despercebido. **Duas correções de interface saíram de confusões reais do fundador, não de auditoria:** o **calendário de disponibilidade** parecia um seletor (células destacadas, hoje contornado, sob o título "Disponibilidade") mas era `<div role="gridcell">` **sem `onClick`**, e o formulário que reserva fica em outra coluna — ele clicou nos dias e concluiu, com razão, que "a tela não deixa selecionar a data"; agora dia livre é `<button>` que preenche a retirada, **rola até o formulário e foca o campo** (ocupado e passado seguem inertes de propósito: botão desabilitado ali só geraria outro clique sem resposta), com a ponte por evento de `window` em `lib/selecionarRetirada.ts`, já que calendário e formulário são Client Components **irmãos** sob um Server Component. E a tela **`/perfil/recebimentos`** abria com o formulário de PIX sob o título "Conta de Recebimento PIX", com a Stripe num card secundário lá embaixo — ele procurou onde informar os dados da Stripe e **não achou**. O texto descrevia o mundo anterior à ADR-028 ("cadastre a chave PIX… verificação manual pela equipe em até 1 dia útil"); é o mesmo drift que `/ajuda` e `/politicas` corrigiram em 20/08, e esta tela ficou de fora daquela varredura. **[#351](https://github.com/robertoepifanio-byte/SHAREO/pull/351) inverteu a ordem — e NÃO removeu o PIX, porque não podia:** o `cron/payout` só usa a Stripe quando `stripeConnectStatus === "ACTIVE"`; fora disso cai no ramo **manual**, onde um `ADMIN_FINANCEIRO` executa o PIX à mão. Sem o formulário, quem não conectou ficaria sem receber. **O lixo de E2E teve a causa atacada, não o sintoma:** o staging tinha **183 itens e 186 reservas** de teste afogando as reservas reais na lista do usuário. A causa não era falta de cleanup — o `return-flow.spec.ts` **já** chamava `DELETE /api/items/{id}` no `finally`, mas esse DELETE é **soft** (grava `deletedAt`, a linha fica) e a **reserva não era apagada por nada**; não existe endpoint para apagar reserva, nem deveria, porque reserva é registro de negócio. Sem caminho por HTTP, a limpeza precisa do banco: passo novo no job **`E2E Regressão Contínua (Staging)` do `deploy.yml`**, com `always()` (o lixo nasce mesmo quando a suíte reprova) e `continue-on-error` (faxina que falha não pode reprovar o E2E). **🪤 O passo nasceu no job errado** — o "E2E Tests (Playwright)" do `main.yml`, que **não roda o `return-flow`** — e saía verde imprimindo "NADA A FAZER" para sempre, com o acúmulo intacto e a aparência de resolvido; **só apareceu porque a SAÍDA do passo foi lida em vez de o verde ser aceito**, o mesmo padrão de ausência-de-sinal-por-aprovação que já mordeu este repositório (job pulado por herança de `needs`, veredito PARCIAL que não reprovava). **🪤 Segundo defeito no próprio script:** foto de reserva estava na trava de "dependentes financeiros" — como a foto virou obrigatória no dia anterior, **toda** reserva do E2E tem uma e o script **abortaria sempre**, fazendo a limpeza do CI nunca rodar. Foto é prova, não dinheiro, e some por cascata. Verificado **rodando duas vezes no CI**, apagando exatamente o que cada rodada criou. **O último achado do dia é o de maior alcance para produção:** o **event destination** do Connect existia desde 21/08, **Ativo**, URL certa, os 2 tipos corretos assinados, **ping respondendo 200** — e **nunca entregou um único `v2.core.account[...]`**. A conta de teste do proprietário saiu de `RESTRICTED` para `ACTIVE` às 22:49 UTC e **nenhuma entrega aconteceu; nem falhada** — e foi essa ausência que denunciou, porque `withStripeEventQueue` grava a linha **antes** de processar e marca `FAILED` com `lastError` se o handler quebra, então "nada na fila" só é possível se o evento nunca chegou. A aba **Eventos** do Workbench fechou o caso: os eventos **não existiam nem no fluxo da plataforma** — não era entrega falhando, era emissão em outro lugar. **Causa: o `create` nunca mandou `events_from`, e a Stripe assume `["@self"]`** (só eventos emitidos pela própria conta plataforma), enquanto `v2.core.account[...]` de uma connected account é emitido **por ela**; faltava `@accounts`. **[#352](https://github.com/robertoepifanio-byte/SHAREO/pull/352)** corrigiu o script e — mais importante — **fechou o buraco que faria o conserto passar batido**: a checagem de duplicata dizia "já existe, nada a fazer" ao encontrar a mesma URL, ou seja, ia pular exatamente o defeito que precisa apontar; agora lê o `events_from` do destino e reclama. Junto, o script passou a **imprimir o ambiente da chave e listar todos os destinos existentes** (com chave `sk_live_` ele diria "nenhum para essa URL" e criaria no ambiente errado) e a **recusar valor que não comece com `sk_`** (um `whsec_` colado em `STRIPE_SECRET_KEY` dá "malformed API Key" com stack do SDK, erro que não sugere qual variável está errada — aconteceu). **Destino recriado com `["@accounts", "@self"]`, confirmado pela API; secret novo no Vercel e runtime redeployado.** **⚠️ Fica registrado como NÃO verificado: que um evento de conta agora CHEGUE de fato** — escopo aplicado e secret no lugar, mas nenhum evento novo passou pelo caminho; a prova vem no próximo cadastro de proprietário ou mudança de requisito, conferindo `stripeEventQueue`. **Por que importa além do teste:** é assim que a plataforma descobre que um proprietário ficou apto a receber — sem esses eventos, o status só atualiza quando o próprio dono **volta à tela** de recebimentos, e quem fechar a aba fica invisível, com repasses seguindo para a fila manual sem ninguém notar. **Três destravamentos de ambiente:** a conta Connect do proprietário de teste ficou **`ACTIVE`** (`charges_enabled` e `payouts_enabled` true), liberando as fases 4–6 do roteiro; o **preview de PR passou a ter banco** — até então `/api/health` do preview respondia `"db":"error"`, `"dbUrl":{"presente":false}`, `NO_DATABASE_URL`, porque o job injeta só variáveis de **build** e `DATABASE_URL` não estava no escopo *Preview* do Vercel, o que significa que **nenhum preview jamais abriu página com dados** e ninguém viu porque o E2E aponta para o **staging**, nunca para o preview; e o staging perdeu **183 itens e 186 reservas** de lixo acumulado. **Erros meus corrigidos em público:** previ que o `Payout` não seria criado por a conta estar `RESTRICTED` — foi criado, porque o `Payout` depende de existir `OwnerPaymentAccount`, não de o Connect estar ativo (o `RESTRICTED` barra depois, na transferência); e apostei em "Invalid signature" no webhook quando o ping já provava que o secret estava certo. **Evidência:** **1084 testes web verdes (64 suítes)** e **648 mobile (32 suítes)**, `tsc` e `lint` limpos, E2E verde em todos os PRs, faxina do CI verificada pela saída do passo, deploys confirmados pelo **SHA do deployment** e não pelo verde da CI (staging serviu `630fa59` às 00:08:20 UTC), e o calendário verificado **no navegador pelo fundador** — clique em dia livre rola, preenche e foca; dia ocupado inerte. **Aberto, por depender de decisão e não de código:** **ATOR-02** (`mark_active` não confere pagamento — travado por o E2E não ter caminho de pagamento; um guard aqui quebra a suíte inteira sem alternativa), **ATOR-03** (extensão de prazo aprovada não cobra nada), **ATOR-04** (notificação de extensão usa tipo `BOOKING_CONFIRMED` — exige migração no enum), o banner "Assinatura do contrato pendente" que aparece mesmo com a flag OFF, e as 4 decisões jurídicas com o Raimundo, sem retorno. **Sobras conhecidas:** arquivos órfãos no bucket `booking-photos` (a cascata é só do banco) e **outra fonte de acúmulo de itens fora do escopo `e2e`** — o staging tinha 1.345 itens e só 1 era `Devolução E2E`.

**🗓️ 22/08 (painel de dois atores):** **🔴 A API entregava o endereço residencial do locador a qualquer pessoa que criasse uma reserva.** Achado por um painel de dois atores (locador + locatário) contra o staging. `GET /api/bookings/[id]` selecionava `cep`/`street`/`neighborhood` do proprietário **sem gate de status ou pagamento** — e criar reserva não exige aceite do dono. As duas telas do produto escondem o endereço até `paymentStatus === "PAID"`; a API não escondia, e **a API é o que um script lê**. O projeto já tinha essa postura em `GET /api/items/[id]` (que zera `address` e trunca coordenadas para quem não é dono, com comentário explícito de privacidade) — faltava aqui. **[#342](https://github.com/robertoepifanio-byte/SHAREO/pull/342) mesclado e verificado lendo a API do staging logado como locatário**: reserva não paga devolve `street: null`, paga devolve o endereço; `city`/`state` seguem visíveis porque já são públicos no anúncio. **Assumido no PR: o [#340](https://github.com/robertoepifanio-byte/SHAREO/pull/340), do mesmo dia, AGRAVOU o vazamento antes de fechá-lo** — ao tornar o endereço obrigatório para confirmar, aumentou o número de donos com endereço preenchido. **O segundo mais grave ficou aberto, com motivo escrito:** `mark_active` **não verifica pagamento** — dá para entregar o item com a reserva `PENDING`, `ownerNetAmount` nulo e split não gravado. O controle pretendido é o `pickupToken` (a tela só mostra ao locatário com `PAID`), mas o mesmo GET devolve o token sem gate: o controle existe e está **furado**, não ausente. Não corrigido porque **a suíte E2E inteira depende do furo** — `return-flow`, `booking-flow`, `features-jun09`, `review`, `security2/3/5` vão todos de `confirm` direto para `mark_active`, e **não existe caminho de pagamento para teste** (o `x-e2e-token` é só bypass de rate limit). Fechar exige **primeiro** decidir como o E2E paga; emendar às cegas quebraria 8 arquivos de spec. **O painel: 9 agentes, 25 achados brutos → 8 refutados na verificação adversarial → 17 confirmados.** O valor veio de ter **dois atores** — cada um tentando as ações do outro é o que expõe falha de permissão, e foi assim que saíram os dois mais graves. **Três PRs abertos, todos com teste que comprovadamente reprova a versão anterior** (verificado revertendo a correção e conferindo quantos caem): **[#343](https://github.com/robertoepifanio-byte/SHAREO/pull/343) disputa** — o motivo era **exigido e descartado** (só o branch `cancel` gravava `cancelReason`, então a disputa entrava no banco sem justificativa e quem arbitrasse abria o caso às cegas), ninguém era notificado (`notifMap` sem `open_dispute`, enquanto o repasse da outra parte ficava bloqueado), e quando notificava dizia ao destinatário que **ele mesmo** abriu (`notifyRole` calculado para quem RECEBE, usado como quem ABRIU); **[#344](https://github.com/robertoepifanio-byte/SHAREO/pull/344) extensão de prazo** — aprovável **depois da devolução** (o POST exige `ACTIVE`, o PATCH não exigia nada: dava para pedir → devolver → dono aprovar, empurrando o `endDate` de uma reserva `RETURNED`), data virando o dia anterior (`new Date("2026-08-27")` é meia-noite UTC = 26/08 às 21h no Brasil — **terceira vez** que esse mecanismo morde o projeto, ver #337), seletor oferecendo treze datas que a API recusa, e botão reaparecendo com pedido pendente (409 por clicar no que a tela ofereceu); **[#345](https://github.com/robertoepifanio-byte/SHAREO/pull/345) reserva** — cancelar reserva **nunca paga** gravava `refundAmount > 0`, e como o estorno é executado **à mão** no painel da Stripe esse número ia para a fila de trabalho de uma pessoa como se fosse real; e `mark_active` recalculava `endDate` da retirada real sem tocar `startDate`, produzindo **início depois do fim** em retirada antecipada. **7 achados seguem abertos por dependerem de decisão** — três já estão no PDF do Raimundo (cancelamento pelo locador não devolve integral; janela de 48h da disputa não existe em código; **teto de R$ 1.000 contornável omitindo o campo — o `.max()` do #339 valida o valor informado, não a ausência dele**), dois **mexem em dinheiro** e não foram decididos sozinho (disputa resolvida a favor do locador **nunca gera Payout**; `confirm_return` conclui **em silêncio** sem gerar Payout quando falta `OwnerPaymentAccount` — o `.catch()` só loga e a API responde sucesso), e dois são de produto (extensão não cobra nada embora a Ajuda prometa que cobra; o `mark_active` acima). **Um oitavo ficou de fora por exigir migration:** as notificações de extensão usam `BOOKING_CONFIRMED`, errado inclusive na **recusa** (`extend/route.ts` linhas 142 e 265) — corrigir precisa de valor novo no enum `NotificationType`, e não se roda migration em staging sem acompanhamento. **O SLA de disputa (3 dias úteis na Ajuda × 5 nas Políticas), pendência aberta desde 20/08, NÃO foi tocado pelo #343** — continua sendo decisão do Raimundo, não bug. **Evidência:** web 63–64 suítes / 1063–1073 testes conforme a branch, `tsc` e `lint` limpos em todas menos a do #341 (só fixtures, sem essa verificação registrada), deploy de staging e E2E de regressão **success**, e o vazamento verificado **na API do staging**, não por inferência. **Estado: 4 PRs abertos** — #341 (fixtures, pronto para merge), #343/#344/#345 (aguardando revisão; nenhum bloqueado).

**🗓️ 22/08 (mesma leva, antes do painel): as decisões dos fundadores viraram código — e o E2E caiu junto.** A auditoria de pagamento de 21/08 (9 agentes, relatório em `docs/auditorias/auditoria-pagamento-stripe-2026-08-21.md`) listou **24 pontos** do projeto que contradiziam a realidade de pagamento só-cartão. Os fundadores decidiram 5; **três eram executáveis e entraram**, e **duas viraram a pauta do Raimundo** por exigirem escolha de negócio (cancelamento pelo locador; prazos de disputa) — `docs/juridico/pauta-raimundo-2026-08-22.md`, 5 páginas em linguagem de negócio, com 4 decisões e três bloqueando go-live. A que passa despercebida é **quem arca com a taxa de serviço** quando o locador cancela e o locatário recebe 100%: hoje ninguém paga, porque a promessa não é cumprida. **[#339](https://github.com/robertoepifanio-byte/SHAREO/pull/339)** executou as três — as contradições em si já estão descritas na entrada de 20/08 abaixo; o que este PR acrescenta é: **estorno** — o texto parou de prometer prazo porque **o código não emite estorno** (`refunds.create` não existe no repositório), então o relógio de "5 a 10 dias úteis" só começa quando alguém clica no painel da Stripe; **repasse** — corrigido em 5 trechos, e **a homepage quase escapou** (`components/home/Seguranca.tsx` seguia com "toda segunda-feira" no mesmo site e mesmo deploy dos Termos que o PR corrigia); **teto de R$ 1.000** — validado na criação **e na edição por regra de NÃO-REGRESSÃO** (aceita dentro do teto **ou** não maior que o valor atual), porque o `.max()` só na criação era contornável em dois passos, e recusar tudo acima na edição travaria o catálogo legado: **59 dos 92 itens do staging (64%) já estavam acima do teto**. **[#340](https://github.com/robertoepifanio-byte/SHAREO/pull/340):** uma dúvida real do locatário durante o teste — *"e agora, como retiro o equipamento?"* — expôs que a tela mostrava **"Natal — RN"** como LOCAL DE RETIRADA, com a advertência *"a retirada deve ocorrer exclusivamente neste endereço, não aceite outro local"*: mandava alguém a uma **cidade inteira** e o instruía a recusar alternativas. O formatador aceitava cidade+estado como "tem endereço", e **41 dos 46 donos de item (89%) não têm rua**. Regra dos fundadores: **endereço completo não é exigência de cadastro, vira exigência quando há locação** — guard `422 OWNER_ADDRESS_REQUIRED` no `confirm`, banner na tela antes do erro, e teste fixando que **cancelar continua possível sem endereço** (prender o dono numa locação que ele não consegue recusar seria pior que o problema). O app tinha **cópia literal** do formatador antigo, então consertar só o site teria deixado web e mobile divergindo no mesmo defeito. **🪤 O deploy do #340 derrubou o E2E de regressão na hora** — `return-flow.spec.ts` com *"confirm falhou (422)"*: a fixture de proprietário não tinha rua e, pela regra nova, não poderia confirmar reserva nenhuma. Corrigido o dado no staging **e a origem** ([#341](https://github.com/robertoepifanio-byte/SHAREO/pull/341)); as 4 falhas de "cleanup 403" do mesmo run eram **cascata** disso, não problema separado — hipótese minha que se provou errada. **[#338](https://github.com/robertoepifanio-byte/SHAREO/pull/338):** a Central de Ajuda do app tinha **22 trechos** citando "Mercado Pago" contra 1 do site, prometia Pix, Amex, Elo e Hipercard, e alegava **licença do BACEN e certificação PCI-DSS** de um provedor que nem é mais o nosso. Reescrita por transcrição, com **8 respostas de número errado** que a primeira passada deixou (prazo do proprietário 24h vs 48h da config; multa de 1× vs 1,5×; *"taxa de 30%"* vs faixas de 100/70/50%). **🪤 O primeiro teste que escrevi era vazio:** lia só o texto renderizado, e as respostas do FAQ vivem em acordeão **recolhido** — *"não promete Pix"* passava com o bug de volta. Reescrito para ler os builders, passou a reprovar a versão antiga em 12 de 12, e foi ele que revelou as 8 divergências. **[#337](https://github.com/robertoepifanio-byte/SHAREO/pull/337):** confirmar retirada e devolução era **impossível fora de UTC** — o campo de horário era pré-preenchido com componentes UTC num `<input type="datetime-local">`, que lê como local; no Brasil nascia 3h no futuro e o Zod recusava. Determinístico para todo usuário a oeste de Greenwich. Além do conserto, o cliente **parou de enviar `actualTime` quando o usuário não edita o campo** — o valor era julgado contra o relógio do servidor, então um cliente adiantado alguns segundos era recusado num horário que ninguém digitou.

**📝 20/08:** **Central de Ajuda e Políticas reescritas para Stripe Connect — e alinhadas ao CÓDIGO, não à política.** [PR #324](https://github.com/robertoepifanio-byte/SHAREO/pull/324) (`f71b961`), mesclado e **verificado em staging**. `/ajuda` ainda descrevia o **Mercado Pago** como PSP em 13 trechos e `/politicas` **não descrevia o modelo de pagamento em nenhum lugar**; a [ADR-028](adr/ADR-028-reversao-stripe-connect.md) (19/08) tinha revertido o PSP para Stripe Connect. **Premissa do pedido corrigida antes de escrever:** o pedido dizia que o checkout já aceitava cartão + Pix + boleto — **não aceita**. `app/api/payments/checkout/route.ts` tem `payment_method_types: ["card"]`, e a própria ADR-028 registra que "Pix e boleto no checkout ainda faltam". Cartão/Pix/boleto é a **decisão**, não o estado; a copy foi escrita para **cartão de crédito à vista, sem parcelamento**, porque prometer meio de pagamento inexistente numa página que vai ao jurídico é oferta vinculante (CDC art. 30). **A frase aprovada no parecer D4 sobre custódia não descreve mais o sistema:** o parecer validou *"a ShareO não retém nem custodia o valor devido ao locador"* para o desenho do MP, mas a implementação da ADR-028 é *separate charges and transfers* — a cobrança cai **na conta ShareO na Stripe** e o `Transfer` só sai no cron de repasse. A copy nova não afirma quem custodia; diz só que o valor **fica retido**. **🔴 O achado maior: três "textos desatualizados" eram políticas DECIDIDAS que o código nunca implementou** — repasse **semanal toda segunda** (decidido em s24/PR #40, 17/06) contra elegibilidade de **N dias após a devolução** (`payoutWindowDays`, default 3) num cron **diário**; multa de atraso de **1 diária** contra **1,5 diária** (`lateFeeMultiplier`=150); teto de **R$ 1.000 por bem "validado ao publicar"** contra **nenhuma validação** **(implementada em 22/08 pelo #339)** (`estimatedRetailPrice` só exige ≥ 0). Mais duas divergências: "proprietário tem 24h para confirmar" são **48h** (`autoCancelOwnerHours`), e a política de cancelamento divergia entre as duas páginas **e** do código (`/ajuda`: "grátis 24h / taxa de 30%"; `/politicas`: "72h/50%/0%"; código: **24h→100% · 24–6h→70% · <6h→50%**). **A copy foi alinhada ao código** — descrever comportamento que o sistema não executa é o risco do art. 37 — e a decisão foi escalada: se a intenção for manter as políticas originais, **muda-se o código primeiro e reverte-se a copy depois**, não o contrário. **Nenhum número mora mais no texto:** taxa, teto, janela de repasse, multa, faixas de cancelamento e prazo do proprietário vêm do `PlatformConfig` em runtime (`HelpVars` em `/ajuda`, props no topo de `/politicas`) — mudar a config muda as duas páginas juntas; somem os placeholders `TAXA_FAQ_PLACEHOLDER`/`LOCADOR_STEP6_EXAMPLE`. **Fonte única das contas publicadas** (achado da revisão `/simplify`): os exemplos deixaram de recalcular dinheiro à mão e passam por **`calcSplit()`**, a mesma função do checkout; a fórmula da multa, que só existia inline no cron, virou **`calcLateFee()`** em `lib/platform-config.ts` (cron e Ajuda consomem a mesma, com 4 testes) — o valor divulgado é provadamente o cobrado. Formatadores duplicados em 4 telas viraram `formatPercentLabel`/`formatMultiplier`/`formatPriceShort` em `utils/format.ts`, matando o hack `formatPrice().replace(",00","")`. `/politicas` ganhou a cláusula **§1.7 Pagamentos e Taxa de Serviço**, que não existia, + §2.1/§2.3 (Stripe como operador de dados financeiros) e §4.3 (estorno). **Pacote para a advogada:** `docs/juridico/copy-pagamento-stripe-connect.md`, que **substitui** `copy-pagamento-validacao-juridica.md` (era do MP), com as duas decisões pendentes e a tabela política×código. **Evidência:** CI toda verde (Lint/Tests/Mobile Gate/Build/E2E Playwright/Security Audit), Deploy Staging **success**, E2E de Regressão **success**, e as duas páginas em `shareo-rouge.vercel.app` retornando **200** com **0 ocorrências** de "Mercado Pago", "segunda-feira" e "custódia" — exemplos renderizados com a config real do banco (taxa 15%, janela 3 dias, multa 1,5×). **Gated D4 — nada em produção** (`Deploy Production: skipped`; só dispara em tag `web-v*` ou `workflow_dispatch`). **Pendências deixadas explícitas** (decisão de negócio, não corrigidas): SLA de disputa diverge entre as páginas (3 vs 5 dias úteis); a janela de 48h para abrir disputa **não é validada em código** (hoje dá pra abrir enquanto a reserva estiver `ACTIVE`/`RETURNED`); `/perfil/recebimentos` ainda exibe "Repasse semanal — próxima segunda-feira" **(fechado em 22/08 pelo #339)**.

**✅ 20/08 (mesmo dia, continuação): certificado digital tirado do repo público + a branch protection de hoje travava todo PR só de docs.** **[#328](https://github.com/robertoepifanio-byte/SHAREO/pull/328) — certificados:** o **`.pfx` do e-CNPJ** (que contém a **chave privada da empresa**) e o zip da emissão estavam soltos na **raiz do repositório**, como untracked, desde 17–18/08. **Não houve exposição** — `git log --all --full-history` não os encontra em **nenhuma** branch, nunca entraram no histórico; o risco era um `git add -A` distraído num repo **público**. Arquivos movidos para fora da árvore (SHA256 conferido antes e depois, `Move-Item` e não cópia), e `.gitignore` ganhou `*.pfx`/`*.p12`/`*.cer`/`*.crt`/`*.key`/`Certificado_Digital*`. **Verificado com arquivo isca em vez de assumir:** `git check-ignore -v teste-isca.pfx` casa em `.gitignore:120` e o arquivo não aparece em `git status`. Enquanto o PR não entrava, as mesmas regras ficaram em `.git/info/exclude` (local, vale em qualquer branch) — hoje redundantes. **[#329](https://github.com/robertoepifanio-byte/SHAREO/pull/329) — o CI:** a branch protection criada **hoje** (3 required checks) colidiu com o `paths-ignore` do gatilho de `pull_request` do `main.yml`. Num PR **só de docs** os jobs não disparam, e o GitHub **não trata check ausente como dispensado** — ele espera indefinidamente: o PR fica `BLOCKED` **sem nenhum check vermelho** explicando por quê. Mesma família do *"`skipping` é ausência de sinal, não neutralidade"* já registrado aqui. Descoberto na prática: o **#327** (a atualização de STATUS logo acima) travou e só entrou com `gh pr merge --admin`. **A causa do ponto cego era um comentário:** o `main.yml` justificava o filtro com *"main não tem required checks"* — premissa que **morreu no dia em que a proteção nasceu**, e que fazia a armadilha parecer intencional para quem lesse o arquivo. Correção: `paths-ignore` sai do `pull_request` e **fica** no `push` (lá é inofensivo, nada depende de push isolado); comentário substituído por aviso de não reintroduzir, com o caso concreto. **Checado antes de escrever** que os 3 obrigatórios não são gateados pelo job `changes` — `lint`/`test` sem `needs`, e `Mobile Tests Gate` com `if: !cancelled()` aceitando `skipped` — senão tirar o filtro só moveria o problema. **[#330](https://github.com/robertoepifanio-byte/SHAREO/pull/330) — a prova:** o #329 **não se provava sozinho** (toca `.github/workflows/`, caminho que nunca esteve no filtro, então rodaria de qualquer jeito). PR descartável tocando **apenas um `.md`**: os 3 obrigatórios rodaram e passaram (Lint 1m8s, Tests 1m30s, Mobile Gate 4s) e o estado foi de `BLOCKED` → **`CLEAN` sem `--admin`**. Fechado sem merge, branch apagada, nada entrou no `main`. **Bônus — a trava de corrida de deploy do #303 foi exercitada de novo:** os merges de #328 e #329 em sequência dispararam 2 deploys; o `concurrency` serializou (staging de `9c26040` **terminou antes** do de `bc9dd30` começar), commit mais novo por último — o inverso do que aconteceu em junho. Estado: **0 PRs abertos**.

**💳 20/08 (3ª leva do dia): as 3 pendências técnicas do ADR-028 fechadas + Dependabot 13 → 5.** **[#325](https://github.com/robertoepifanio-byte/SHAREO/pull/325) — Pix/boleto, reversão de Transfer e webhook de Connect.** **(1) Pix e boleto** entram em `payment_method_types` — mas o achado que importa é que **os dois são ASSÍNCRONOS**: a Checkout Session dispara `checkout.session.completed` com `payment_status` ainda **não** `"paid"` e o pagamento confirma depois. Sem tratar `async_payment_succeeded`, pagamento por Pix/boleto **nunca marcaria a reserva como paga — e falharia em silêncio**, porque o `completed` chega normalmente. Handlers novos para `async_payment_succeeded`/`async_payment_failed`. **(2) Reversão de `Transfer` cobrindo reembolso E disputa perdida**, que são o mesmo fato ("o dinheiro voltou pro locatário") e por isso têm um caminho só: `reverseOwnerTransfer()` em `lib/payments/owner-transfer.ts`, proporcional, **cumulativo** (soma o já revertido e reverte só a diferença, então reembolso parcial em vários eventos converge sem estourar o `already reversed` da Stripe) e idempotente pela chave. **A disputa parecia inalcançável porque a função recebia um `Stripe.Charge`** — trocando a assinatura para valores (`clawbackAmount`/`chargedAmount`), `dispute.amount` + `booking.totalPrice` bastam, sem ida extra à API. Erro na reversão **propaga (500)** em vez de ser engolido: dinheiro no lugar errado não pode falhar calado. **(3) Webhook de Connect** em **rota separada** (`/api/webhooks/stripe-connect`) — contas Accounts v2 não disparam o `account.updated` clássico, e a verificação de assinatura é outra função (`parseEventNotification`) com outro secret; escolher entre as duas no mesmo endpoint exigiria **olhar o payload antes de verificar a assinatura**. **🪤 ARMADILHA pega na revisão, antes de ir pro ar: NÃO usar `notification.fetchRelatedObject()`.** Ele faz um GET **sem `include`**, e na v2 `configuration.recipient`/`requirements` são **opt-in** — sem eles `deriveStripeConnectStatus()` leria capabilities indefinidas e o sync **REBAIXARIA uma conta `ACTIVE` para `ONBOARDING`**, quebrando o repasse de quem já recebia, **em silêncio e a cada evento**. A lista virou constante compartilhada (`lib/stripe-connect-events.ts`), usada também pela rota de retorno do onboarding, que já a tinha à mão. **🪤 Segunda armadilha, também da revisão: faltava a guarda de idempotência.** O webhook mantinha uma **3ª cópia** dos efeitos de "reserva paga" — e essa cópia **não tinha** a guarda que `markRentalPaid()` (o caminho do MP) sempre teve. Sem ela um reprocessamento **regenera o `pickupToken` e invalida o código que o locatário já tem em mãos**; risco que Pix/boleto **agravam**, porque passam a existir **dois eventos distintos** capazes de marcar pago (a fila dedupa por `event.id`, o que não cobre esse par). Resolvido chamando o helper que já existia. Mais três correções da mesma revisão: **`lib/stripe.ts` ganhou `timeout: 15s` + 1 retry** (o default do SDK é 80s e 2 retries — **uma** chamada travada consome ~240s, mais que o orçamento inteiro de 60s do cron de repasse, que processa lotes de 10); **`lib/stripe.ts` deixou de importar `next/server`** (o verificador de assinatura devolve `NextResponse` e arrastava o runtime do Next para o módulo do cliente Stripe, que é alcançado por testes em **jsdom**, sem o global `Request` — quebrava uma suíte inteira por um import alheio a ela; o envelope mora em `lib/payments/stripe-webhook.ts`); e **`checkout.session.expired` usava `update()`**, que lança **P2025** se a reserva sumiu → 500 → **retry perpétuo** da Stripe, agora `updateMany` fundido com `async_payment_failed`. Fim do proxy `stripe` deprecado (os 2 últimos consumidores migraram para `getStripe()`). **20 testes novos** (aritmética da reversão + roteamento de eventos); suíte web em **1031**. **⚠️ NÃO exercitado ponta a ponta** — faltam 4 passos manuais no Dashboard/Vercel listados em "Como verificar" no ADR-028, sendo o mais silencioso **marcar os 2 eventos novos no webhook**: sem isso Pix/boleto não marca pago e nada acusa erro. Estado honesto: **implementado, aguardando verificação — não ✅**. **[#326](https://github.com/robertoepifanio-byte/SHAREO/pull/326) + [#332](https://github.com/robertoepifanio-byte/SHAREO/pull/332) — Dependabot 13 → 5.** O número 13 estava inflado: eram **8 pacotes**, com `postcss` respondendo por 4 alertas e `nanoid` por 2. **#326:** `nanoid` e `brace-expansion` — nos dois, **a versão corrigida já convivia no lockfile com a vulnerável**, então alinhar foi só remover a cópia velha (escopados por major, `nanoid@3`/`brace-expansion@5`, para não colidir com o override `brace-expansion@1` existente). **#332:** `shell-quote` (dev, via react-devtools-core) e o **`postcss` do Metro** — esta é a "trilha Expo" que a triagem de 19/08 **deliberadamente evitou**, e a diferença é que aqui o salto é **minor dentro do 8.x**, para a **mesma 8.5.26 que o resto do repo já usa** (a árvore converge em vez de divergir), com override **escopado a `@expo/metro-config`**. **Confirmado que o `next>postcss` da triagem anterior funcionou:** o `pnpm why` exibe um `postcss@8.4.31` sob o Next, mas ele **não está no lockfile** — é `node_modules` velho ou faixa de peer. **Ficam 5, por impedimento real:** `image-size` (66/67) e `extract-zip` (70) **sem patch publicado**; `deepmerge-ts` (72) e `uuid` (4) exigiriam **major** dentro de `@prisma/config` e `xcode`, que sobem sozinhos — forçar agora troca vulnerabilidade de build-time por risco de quebra real. **Nenhum dos 5 alcança o runtime web.** **🪤 GOTCHA de processo — o E2E acusa vermelho sem haver regressão:** o job "E2E Tests (Playwright)" roda **contra o staging publicado** (`ALVO: staging, não um servidor local`, dito no próprio `main.yml`), ou seja, o código do `main` — **não o do PR**. No #325 o **mesmo commit** teve uma execução verde e outra vermelha, falhando em *recuperação de senha*, sem relação com o diff de pagamentos. Antes de concluir qualquer coisa de um E2E vermelho: olhar **qual** teste falhou. O conflito do #325 (só `.gitignore`) foi resolvido tomando a versão do #328, mais ampla que a minha. Estado: **0 PRs abertos**.

<details>
<summary><strong>Histórico de atualizações anteriores</strong></summary>

**2026-08-19** — **🔒 Triagem dos 32 alertas Dependabot por alcance real + `@sentry/nextjs` v8→v10 validado no dashboard.** Higiene de dependências (não gated por D4). Classificação por **alcance real**, não o "runtime scope" do Dependabot (que engloba build-time): das 17 deps, só **4 cópias vulneráveis tocam o runtime web de produção** — `cookie` (@supabase/ssr), `sharp` (next/image), `xlsx` (rota import PJ) e `@opentelemetry/core` (@sentry/node). **[#319](https://github.com/robertoepifanio-byte/SHAREO/pull/319):** cookie 0.6→0.7 (override), sharp 0.34.5→0.35.2, **xlsx→exceljs** (xlsx sem patch no npm — GHSA prototype-pollution + ReDoS; `parseXLSX` reescrito com guard anti-`__proto__` + teste de integração), e **`@sentry/nextjs` 8.55.2→10.70.0** (limpa rollup/uuid@9/fast-uri e leva o otel a 2.x; exigiu a **migração do modelo de instrumentação do SDK v9+**: novo `instrumentation.ts` + `instrumentation-client.ts`, `hideSourceMaps` removido do `next.config.ts` — sem isso o tracking server/edge **morre em silêncio** com o build verde). **Sentry VALIDADO de ponta a ponta** por endpoints de debug protegidos por token no preview: eventos de **client+edge+server** confirmados no dashboard do projeto `shareo-web` (endpoints removidos pós-validação). **GOTCHA de infra corrigido:** o job de **preview** do `deploy.yml` nunca inlinava `NEXT_PUBLIC_SENTRY_DSN` (só staging/prod faziam) → previews rodavam sem observabilidade. **[#321](https://github.com/robertoepifanio-byte/SHAREO/pull/321):** `@supabase/ssr` 0.4→0.12 (**SEC-MAJ-04**) fecha o cookie na raiz e remove o override — a API `getAll/setAll` já estava migrada, só ajuste de tipos do realtime em `_ChatWindow.tsx`; + brace-expansion 1.1.14→1.1.18 (dev). **Overrides moram no `pnpm-workspace.yaml`** (pnpm 11 ignora `pnpm.overrides` do package.json). **Lição:** puxar o range/patch REAL de cada alerta via `gh api .../dependabot/alerts` — os brace-expansion (#55/#56) **não** eram stale. **32→7 alertas**; os 7 restantes todos deferidos (image-size/extract-zip **sem patch**; nanoid/postcss internos do **Expo/metro**; deepmerge-ts **major acoplado ao Prisma**), **nenhum tocando runtime web de produção**. #321 mesclado com o **Mobile Tests vermelho** — flake herdado do #320 (`screens-recebimentos`, check **não-obrigatório**, comprovadamente sem relação com o diff), rastreado à parte. Evidência: `next build` + jest web (1006) + mobile (627) verdes; eventos Sentry no dashboard. Pendência manual do fundador: **remover `SENTRY_TEST_TOKEN` do Vercel**. Estado: **0 PRs abertos**.

</details>

<details>
<summary><strong>Histórico de atualizações anteriores</strong></summary>

**2026-08-19** — **🔄 Reversão de PSP: Mercado Pago → Stripe Connect ([ADR-028](adr/ADR-028-reversao-stripe-connect.md)), PIX manual removido do código.** Decisão do sócio majoritário Raimundo: o Modelo B do Mercado Pago (ADR-026) exigia que **cada proprietário conectasse a própria conta MP via OAuth** para o split funcionar, e isso gerou **forte rejeição comercial** — abrir conta em outra instituição financeira só para alugar um item é fricção alta demais para o perfil do ShareO (pessoa física, uso ocasional). Revertido para **Stripe Connect** como PSP definitivo: split automático **85% proprietário / 15% ShareO** via `application_fee_amount`+`transfer_data.destination` (executado pelo Stripe, não mais só bookkeeping), Pix + cartão à vista + boleto (parcelamento no crédito **desabilitado**), proprietário **sem abrir conta Stripe própria visível** — dados bancários + KYC dentro/via link hospedado a partir da UI do ShareO (Custom vs. Express, decisão de implementação ainda em aberto). **Mercado Pago fica DORMENTE, não removido:** flag `mercadoPagoEnabled` mantém default **OFF** (já era o padrão), código de OAuth/checkout/webhook preservado; **staging segue com a flag ON desde 30/06/2026** — desligar é pendência de execução (`scripts/disable-mercadopago-staging.ts` preparado, ainda não rodado). **PIX manual da plataforma removido integralmente do código** (não só desligado) — era o maior risco regulatório exposto hoje (dinheiro de terceiros passando pela conta pessoal do fundador Raimundo Gomes da Silva, Banco Santander): removidos `app/api/bookings/[id]/declare-pix/route.ts`, `app/reservas/[id]/_PixPaymentPanel.tsx`, `app/api/admin/bookings/[id]/confirm-pix/route.ts`, `app/admin/reservas/[id]/_ConfirmPixButton.tsx`, a seção `PlatformPixConfig`/`getPlatformPixConfig()` de `lib/platform-config.ts`, e o campo `Booking.pixDeclaredAt`. **Não confundir** com a chave PIX do próprio proprietário para receber repasse (`OwnerPaymentAccount.pixKeyType/pixKey/holderName/bankName/status`) — feature intacta, não foi tocada. **Schema (aplicado no código, não em banco):** `OwnerPaymentAccount` ganhou os campos de Connect (`stripeAccountId`, `stripeConnectStatus`, `stripeChargesEnabled`, `stripePayoutsEnabled`, `stripeDetailsSubmitted`, `stripeDisabledReason`, `stripeConnectedAt`) e `Booking` ganhou `stripeConnectedAccountId`; duas migrations novas (`20260819120000_add_stripe_connect_fields`, `20260819120100_remove_platform_pix_manual`), **nada aplicado em nenhum banco ainda**. **Ordem de prioridade do checkout hoje** em `app/reservas/[id]/page.tsx`: (1) Mercado Pago se a flag estiver ativa → (2) Stripe Checkout simples como fallback — **Connect em si ainda não foi implementado**, é trabalho futuro fora desta mudança. **Pendência jurídica nova:** o parecer D4 validou o desenho do Mercado Pago Modelo B especificamente; o desenho análogo do Connect (dinheiro do locatário vai para a *connected account* do proprietário, não para a conta da ShareO) não foi objeto desse parecer — confirmação formal necessária antes do go-live (ver ADR-028, seção "Riscos/Pendências"). `CLAUDE.md`, este arquivo e o card estático de `/admin/financeiro` atualizados para refletir a decisão (teste `e2e/financeiro.spec.ts` #31 ajustado junto). Estado: **0 PRs abertos** — mudança de doc/schema local, nada deployado ainda.

</details>

<details>
<summary><strong>Histórico de atualizações anteriores</strong></summary>

**2026-08-14** — **🔐 Revisão de segurança E2E (4 correções) + `CRON_SECRET` rotacionado + registros reconciliados.** **Premissas corrigidas antes de testar:** o pedido listava MFA, RLS, Cloudinary, Registro.br e Meta API — **MFA não existe** no app (as menções são ao Supabase Dashboard), **RLS é desabilitado por decisão de arquitetura** (incompatível com PgBouncer; segurança é guard server-side), Cloudinary não é usado, o domínio está na GoDaddy e o pixel da Meta é inerte. Testar o que não existe produz relatório verde sobre ausência. **Simulação de DDoS recusada** — viola a política de Vercel/Supabase e o staging divide infra com o `shareo-prod`; a pergunta legítima ("degrada sob carga?") se responde com o k6 que já existe. **4 correções mescladas, cada uma com prova de que o teste pega o defeito** (revertendo a correção e mostrando quantos falham): **[#305](https://github.com/robertoepifanio-byte/SHAREO/pull/305)** webhook do MP — assinatura era condicional e sem `MP_WEBHOOK_SECRET` o handler PULAVA a validação; agora **falha fechado (503)** com credencial real, usando o prefixo `TEST-` do próprio token como chave do modo (não `VERCEL_ENV`, ambíguo porque staging também deploya como "production") — 3/12 testes falham no código antigo; **[#306](https://github.com/robertoepifanio-byte/SHAREO/pull/306)** `CRON_SECRET` em texto claro em 6 lugares de um **repositório público** — já era o `SEC-CRIT-02` conhecido, mas com severidade subestimada (o backlog dizia "quem tem acesso ao repo"; é qualquer pessoa na internet) e alcance errado (o checklist mandava rotacionar "só em produção", mas o staging tem os **leads reais da campanha** e os crons disparam e-mail via Resend); **[#307](https://github.com/robertoepifanio-byte/SHAREO/pull/307)** SSRF por redirect nos webhooks de saída — o guard validava a URL registrada (resolve DNS, recusa IP privado, roda na criação E na entrega) mas o `fetch` seguia redirect: `302 → 169.254.169.254` derrotava tudo, agravado pelo `lastStatusCode` exibido ao dono virar oráculo de serviço interno — 1/5; **[#308](https://github.com/robertoepifanio-byte/SHAREO/pull/308)** invalidação de sessão — falha de escrita do epoch era engolida e `/api/user/password` respondia `{ ok: true }` mesmo com as outras sessões vivas; a **leitura segue fail-open de propósito** (fail-closed derrubaria todas as sessões a cada blip do Upstash), fixado em teste com o motivo escrito — 5/9. **Padrão: 3 dos 4 eram fail-open**, seguindo em frente com um `warn` que ninguém lê — mesma família do veredito PARCIAL de ontem. **Auditado e CORRETO (registrado para não ser mexido à toa):** `state` do OAuth do MP (128 bits, `httpOnly`+`sameSite`+path+10min), `assertCronAuth` e `verifySignature` (`timingSafeEqual`, falha fechado), `resolveUserId` (já checa `isSessionStale` para Bearer e recusa refresh como access), `isUrlSafeForWebhook` (fail-closed se o DNS não resolve). **🔑 `CRON_SECRET` ROTACIONADO (14/08)** nos dois ambientes com valores distintos de 256 bits, Vercel env + GitHub Secret dos dois lados, com redeploy — **prova em staging: o segredo antigo passou de `200` para `401`** em `/api/cron/purge-consent-ips`, com `/api/health` seguindo `200`. Antes disso, aquele mesmo comando **executava o cron sem credencial nem sessão**. Em produção não é verificável de fora (Deployment Protection devolve 302 antes do app) — o sinal válido é o painel de Cron Jobs do Vercel. **🪤 Gotcha caro:** `gh run rerun` do run mais recente **não deploya** se os últimos commits forem só de docs (o job `changes` gateia por mudança de web) — o workflow fica **verde sem ter deployado**, e parar ali teria reportado a rotação como concluída com o `200` ainda de pé. **[#309](https://github.com/robertoepifanio-byte/SHAREO/pull/309) reconciliou os registros:** 4 entradas descreviam o mundo errado — parecer D4 (recebido 30/06), projeto `shareo-prod` (criado 05/08) e `GAP-CRIT-04b` (fechado) marcados como pendentes, e a descrição do `MP_WEBHOOK_SECRET` obsoleta desde o #305; adicionados os itens reais que não estavam escritos (`NEXT_PUBLIC_APP_URL` ausente no staging, dívida do meta-teste E2E). Regra que ficou no topo do backlog: *um checklist que lista como pendente o que já foi feito treina quem lê a ignorá-lo*. **Deploy de produção executado** (interno, `workflow_dispatch`) aplicando 3 migrations aditivas de `founder_leads` que nunca tinham rodado lá; health check `302` = Deployment Protection ativa, `shareo.com.br` intocado. **⚠️ Camadas de provedores e frontend NÃO foram revisadas** — não tratar como cobertura completa. Estado: **0 PRs abertos**.

</details>

<details>
<summary><strong>Histórico de atualizações anteriores</strong></summary>

**2026-08-13** — **🎉 CNPJ ATIVO + o gate de CI parou de mentir (9 PRs).** **Jurídico:** CNPJ **68.512.556/0001-09** emitido em 11/08 (SHAREO MARKETPLACE DE INTERMEDIACAO DE NEGOCIOS LTDA, ME, ATIVA, sede em Pinheiros/SP), 2 dias antes da estimativa. CNAE principal **74.90-1-04 — intermediação e agenciamento**, que bate com o modelo do parecer (receita = só a comissão de 15%; os 85% são de terceiros em trânsito) — se tivesse saído como comércio, o cadastro fiscal contradiria o parecer. **B1 NÃO fechou:** B1 = "contrato MP assinado **+** conta PJ ativa"; o CNPJ resolve a segunda metade e habilita a primeira, mas **o contrato com o Mercado Pago segue por assinar** e continua sendo o único bloqueador `🔒` de go-live. Regra absoluta intacta. Pendência criada: os textos legais **não identificam a ShareO como pessoa jurídica** (nem razão social, nem CNPJ, nem sede) — todas as menções a CNPJ em `/termos` e `/privacidade` são sobre o CNPJ *do usuário PJ*. **Qualidade/CI — o tema do dia foi "verde que não significa nada", em 5 variações:** (1) **[PR #298](https://github.com/robertoepifanio-byte/SHAREO/pull/298)** — os specs `e2e-*-plan` calculavam veredito `PARCIAL` quando um step `high`/`medium` falhava, gravavam no JSON e **não lançavam nada**: o Playwright dava `passed`. Regressão em compartilhamento, bloqueio de `/admin`, XSS e a11y passava verde no gate. `assertNoFailedSteps()` em `e2e/_support.ts` aplicado em 16 pontos de 13 specs (+7 testes unitários que fixam os dois lados). (2) Ao consertar, apareceu que o job de E2E **media dois ambientes ao mesmo tempo**: os specs liam `STAGING_URL ?? 'shareo-rouge...'` e ignoravam o `BASE_URL: localhost:3000` do job, então chamada absoluta ia pra staging e relativa pro localhost — no mesmo teste. Job passou a apontar explicitamente pra staging (`BASE_URL` e `STAGING_URL` com o MESMO secret + `E2E_SECRET` pro bypass de rate limit); Postgres/seed do job saíram (nenhum spec toca o banco). (3) **2 bugs REAIS de produto** que o veredito engolia: **`UserDropdown` inalcançável** — painel `overflow-hidden` sem altura máxima, lista de ~680px sob header de 64px, o "Sair" caía fora da viewport e o menu não rolava (**em laptop 1366×768 o usuário não conseguia sair da conta**); e **`id` duplicado na home** (`#casos-renda`/`#itens-procurados` no wrapper E no `<section>`) — HTML inválido que fazia o seletor casar 2 elementos. Ambos verificados no ar. (4) **[PR #299](https://github.com/robertoepifanio-byte/SHAREO/pull/299)** auto-cancelamento de reserva PENDING **2h → 12h** (a de 2h cancelava reserva legítima de dono que estava dormindo) + cron de 1×/dia → **6/6h** (janela real caiu de **12h–36h** para **12h–18h**) + tela `/admin/reservas` pra ajustar sem deploy, com o caminho confirmado ponta a ponta até o cron (≠ do `_AmbassadorConfigForm`, que grava chaves que ninguém lê). (5) **[PR #302](https://github.com/robertoepifanio-byte/SHAREO/pull/302)** — **todo PR do Dependabot estava vermelho, provavelmente há semanas**: o GitHub não expõe secrets em PR do Dependabot, então `NEXT_PUBLIC_APP_URL: ${{ secrets.STAGING_URL }}` chega como **string vazia**, e `?? ` não cai no fallback com `""` → `new URL("")` → build morria em "Failed to collect configuration for /_not-found". **Mesmo defeito do `getHmacKey()` (#125, junho)**: `??` onde precisa ser `||`. Corrigido nos 6 pontos que leem `NEXT_PUBLIC_APP_URL`; os 5 PRs do Dependabot (#278–#282) mesclados em seguida. **[PR #303](https://github.com/robertoepifanio-byte/SHAREO/pull/303)** — `concurrency` por job no `deploy.yml`: os 5 merges seguidos geraram 5 deploys paralelos e **o último a TERMINAR foi o commit mais ANTIGO da fila** (2s depois do HEAD) — staging serviria código 4 commits atrasado com a CI verde; `cancel-in-progress: false` no staging/produção (cancelar pode matar o run entre migrations do `prisma migrate deploy`) e `true` no preview. **[PR #300](https://github.com/robertoepifanio-byte/SHAREO/pull/300)** 28 testes pra `apps/campanha` (funil de captação estava sem nenhum) e **[PR #301](https://github.com/robertoepifanio-byte/SHAREO/pull/301)** contraste WCAG AA na landing — medido no navegador com composição de alfa e geometria real do gradiente: bullets reprovavam **só no escuro** (4,45) porque o `--primary` em dark é mais claro, e o **placeholder dos inputs (3,14) era o pior caso e não estava em nenhuma suspeita**. Estado final: **0 PRs abertos**, deploy de staging verde no commit certo, E2E de regressão verde — e agora esse verde significa alguma coisa.

</details>


<details>
<summary><strong>Histórico de atualizações anteriores</strong></summary>

**2026-08-06** — **Painel de auditoria de testes não-funcionais sobre o `shareo-prod`: 28 achados, todos endereçados.** Painel de especialistas (qa/devops/arquiteto/segurança/designer) auditou os 5 eixos não-funcionais (desempenho/carga/estresse/segurança/usabilidade) contra o ambiente de produção real, com verificação adversarial de cada achado (12 refutados de 40 brutos). **3 lotes de PRs, todos mesclados + deployados em staging E produção com evidência real** (deploy verde + migrations aplicadas + health check + suíte E2E completa): **[PR #265](https://github.com/robertoepifanio-byte/SHAREO/pull/265)** (4 achados Alto — cache do `isSessionStale` no Upstash, migration de produção sem `continue-on-error`, `CRON_SECRET_PROD` criado, geocode automático corrigido no fluxo de criar item que salvava coordenadas erradas permanentes); **[PR #266](https://github.com/robertoepifanio-byte/SHAREO/pull/266)** (11 achados Médio/Baixo mecânicos — Sentry por ambiente, kill-switch de rate limit, tap targets, cores hardcoded em dark mode, `maxDuration`, rate limit no export LGPD, índice `pg_trgm` de busca); **PRs [#267](https://github.com/robertoepifanio-byte/SHAREO/pull/267)–[#271](https://github.com/robertoepifanio-byte/SHAREO/pull/271)** (`NFR-BL1`–`7`, achados que exigiam migração de schema/nova arquitetura — bounding box no filtro de distância, batching de `viewCount` via Redis, cache das queries do detalhe, `bookingsCount` denormalizado, cron de lembretes em lote, retry automático de geocode com status, fila de retry de e-mail crítico — implementados em paralelo via 5 subagentes em worktrees isolados, cada um revisado/verificado manualmente antes do merge; 2 rebases de `vercel.json` por conflito de cron concorrente). **Único item que resta**: `connection_limit=1` no `DATABASE_URL_PROD` — bloqueado em ação do Roberto (exige a senha do banco, que o Claude nunca viu), registrado em "Pendências imediatas" do backlog. `NFR-BL8` (Deployment Protection bloqueia stress test externo) não é bug — é a proteção funcionando; documentado, não corrigido. Ver `docs/backlog-atividades-priorizadas.md` (seção "Painel de Testes Não-Funcionais"). **✅ 06/08 (mesmo dia, continuação): `connection_limit=1` resolvido** (Roberto atualizou GitHub Secret + Vercel env var). **🔴→✅ Achado maior na sequência: o projeto Vercel `shareo-prod` tinha ZERO Environment Variables configuradas no painel** — os 9 secrets `_PROD` do GitHub só alimentam o CI (build/migrations), nunca as funções serverless reais; sintoma real (não só teórico): cadastro de conta em produção retornava 500 (`DATABASE_URL not configured`) e "esqueci a senha" não enviava e-mail. Painel populado do zero (Supabase/Mapbox/Sentry/Resend + 4 secrets regerados que só existiam write-only no GitHub) e **confirmado por teste real: cadastro de conta e troca de senha funcionando em `shareo-prod`**. `shareo.com.br` segue no placeholder, nenhuma mudança de exposição pública.

</details>

<details>
<summary><strong>Histórico de atualizações anteriores</strong></summary>

**2026-08-05** — **Ambiente de produção (`shareo-prd`/`shareo-prod`) criado e testado ponta a ponta, uso interno.** Continuação direta da sessão de 04/08 (exceção autorizada pelo fundador naquele dia): Supabase `shareo-prd` criado (ref `jdxdndrhjxtkaifbpagr`, pooler `aws-0-sa-east-1` — diferente do dev/staging que é `aws-1`), Data API hardened, migrations aplicadas em banco **vazio** (ARQ-ALTO-15, nunca clonou staging), 3 buckets de Storage criados. **9 GitHub Secrets `_PROD` configurados** — `DATABASE_URL`/`DIRECT_URL`/`NEXT_PUBLIC_SUPABASE_URL`/`_ANON_KEY`/`SERVICE_ROLE_KEY`/`VERCEL_PROJECT_ID` (todos passados por Roberto via `gh secret set` local, sem eu ver senha/chaves sensíveis) + `AUTH_SECRET`/`ENCRYPTION_KEY`/`HMAC_KEY` (gerados novos e **exclusivos** de produção, não reusam staging — mitiga achado de LGPD sobre chave única entre ambientes). Projeto Vercel `shareo-prod` criado (time SHAREO Marketplace/Pro) com **Deployment Protection** (Vercel Authentication) ativa. **Achado real corrigido** ([PR #262](https://github.com/robertoepifanio-byte/SHAREO/pull/262)): o job "Deploy Production" do `deploy.yml` reusava `VERCEL_PROJECT_ID`/`AUTH_SECRET`/`ENCRYPTION_KEY` do **staging** (sem sufixo `_PROD`) — se disparado como estava, sobrescreveria o staging. Corrigido pra usar os secrets `_PROD` dedicados + novo gatilho `workflow_dispatch` (deploy manual interno, sem precisar de tag `web-v*` pública) + health check contra a URL dinâmica do deployment (não `www.shareo.com.br`, que segue no placeholder). **✅ Testado ao vivo antes de mesclar** (run `31012928956`): build+deploy+migrations = sucesso; health check retornou **302** (redirect pro login do Vercel) — confirma a Deployment Protection bloqueando acesso não-autenticado, exatamente o esperado. **`shareo.com.br` continua intocado no placeholder Netlify** — nenhuma atividade pública, DNS ou pagamento live decorreu disso; a exceção autorizada foi respeitada à risca. Gotcha do caminho: PowerShell com aspas curvas do chat quebrava `$env:`, e o atributo Hidden do `.husky\_` voltou a travar `pnpm`/`npx` (mesmo bug de 04/08) — contornado com `$env:HUSKY='0'`. **📋 D4 (B1/B3):** Contabilizei contratada pra abertura da empresa (CNPJ) + regime tributário, previsão **6 dias úteis (~13/08/2026)** — contrato Mercado Pago formaliza-se assim que o CNPJ estiver ativo. Checklist jurídico atualizado. Ver memórias [[project-d4-juridico]] e [[project-infra-ambientes]].

</details>

<details>
<summary><strong>Histórico de atualizações anteriores</strong></summary>

**2026-08-04** — **3 CVEs críticas corrigidas e verificadas ao vivo + 6 PRs Dependabot mesclados + shareo-dev excluído + decisão nova do fundador sobre ambiente de produção.** Sessão de manutenção/segurança/infra, sem mudança de produto. **Segurança:** `next-auth` 5.0.0-beta.31→beta.32 + `@auth/prisma-adapter`→2.11.3 (PR **#256**) corrigiram 3 vulnerabilidades críticas (GHSA-8fpg-xm3f-6cx3 + GHSA-7rqj-j65f-68wh ×2) — achado colateral: o `pnpm-lock.yaml` travava `@auth/prisma-adapter` numa versão vulnerável mesmo dentro do range já permitido pelo `package.json`, nenhum Dependabot detectaria isso sozinho. **Verificado ao vivo em staging** (não só CI): login+logout via browser real pós-deploy, ambos 200. Dependabot alerts caiu de 3 críticas → **0 críticas** (confirmado via API do GitHub). **Higiene de repo (PR #255):** 6 arquivos órfãos removidos (incluindo 1 arquivo de **outro negócio** — inventário de confeitaria — que tinha ido parar no repo por engano), 2 scripts já citados pela documentação mas nunca commitados agora versionados. **`shareo-dev` (Supabase) EXCLUÍDO** — uso real mínimo confirmado por query direta (1 usuário, 8 itens, 0 reservas, parado desde 16/07); `next dev` local sem banco até recriar. **6 PRs do Dependabot mesclados** (#258/#252/#251/#240/#253, +#250 fechado como redundante) — 2 conflitos de lockfile em cadeia resolvidos via `@dependabot rebase`. **Tabela de custo de infra corrigida 3×** (PR #259) depois de eu errar valores sem verificar: org Supabase é **Pro** (não Free), estrutura real é **~$25/mês base + ~$10/mês por projeto adicional** (não $10 isolado, corrigido 2× após o fundador flagrar com screenshot), Mapbox confirmado na fonte oficial (50k map loads + 100k geocodes grátis/mês, 5-10× melhor que Google Maps pro mesmo uso), Apple Developer Program adicionado (~$99/ano, só quando a Fase 2 iOS começar). **D4 (PR #260):** fundador nomeou responsáveis pros 4 itens que seguiam sem resposta desde 30/06 — B1/B3 em andamento com o fundador Raimundo (CNPJ/contrato/tributário contabilizados), C2/C3 com Raimundo Gomes da Silva nomeado responsável/DPO formal pro período de MVP + 1º ano. **🆕 Decisão do fundador sobre ambiente de produção:** `shareo.com.br` continua redirecionando pro placeholder Netlify até B1 resolver, **mas o projeto será migrado para Vercel produção + Supabase produção (`shareo-prd`) já para uso interno** (equipe técnica + fundadores) — exceção estreita à regra absoluta de "nenhuma atividade de produção antes de D4", com acesso restrito via Vercel Deployment Protection (mesmo mecanismo do staging). **Execução ainda não iniciada** — decisão registrada, aguardando ordem explícita para criar os recursos. **Estado final: 0 PRs abertos**, todos os deploys de staging verdes, health check `healthy`. Ver memórias [[project-sessao-0804-seguranca-infra]], [[project-infra-ambientes]], [[project-d4-juridico]].

**2026-07-22** — **Inventário Supabase limpo (2 projetos) + `main` verde nos 7 checks pela 1ª vez desde 10/07.** Auditoria de "quais Supabase o ShareO usa" virou faxina: só **shareo-dev** (`kehbrjlllfkooauaswtp`) e **shareo-staging** (`zythygwvmrwrqmnrdufq`) estão em uso, ambos na org corporativa; os 2 refs antigos da org pessoal (`jtianehxosfdrhjzqvqj` dev / `fflpuoluiqmhpvcxubqi` staging) foram **deletados por Roberto em 22/07** — DNS não resolve mais. 4 PRs mesclados: **#238** (ref morto fora de 7 arquivos — 3 guards de banco abortavam sempre e 2 fallbacks apontavam para host inexistente — + **senha de banco em texto puro** removida de `staging-add-pickup-token.ts`/`staging-add-activated-at.ts`), **#239** (`tar>=7.5.19` GHSA-23hp-3jrh-7fpw + `pnpm.overrides` → `pnpm-workspace.yaml`; **achado colateral: os pins de `@types/react` do mobile estavam inativos** desde a subida para pnpm 11, o CI já avisava e ninguém tinha visto), **#241** (`Mobile Tests` vermelho desde 16/07 era **bomba-relógio**, não bug: o teste do calendário fixava `2026-07-10` como "data futura" e essa data passou; `isPast` tem precedência sobre `isOcc`, o componente estava certo), **#242** (rótulo do banco no `.ps1` de migrations financeiras imprimia o projeto morto enquanto conectava no certo → passa a **derivar o ref da `DIRECT_URL` real**, nunca string fixa). Varredura do histórico do git: **nenhuma chave dos projetos deletados vazada**; a única credencial no histórico é a senha do staging antigo (projeto inexistente, sem alvo). Removidas 6 regras que guardavam essa senha em texto puro no `.claude/settings.local.json` (gitignored pelo ignore global → invisível a `git grep`). Verificação: staging `db:ok`/`storage:ok`/`storage_private:ok`, imagem real de 85 KB baixada do bucket, smoke #20 (bucket privado) verde. Removidos 3 `.env` obsoletos (`.env.staging-check` apontava para o ref antigo). **Total do dia: 10 PRs mesclados, 0 PRs abertos ao final.** Além dos 4 acima: **#243/#244/#245** (inventário Supabase correto no STATUS + cabeçalho de 24 KB numa linha só quebrado em parágrafos — inviabilizava revisar qualquer diff do arquivo no GitHub), **#246** (TEST-BL1/BL2 já resolvidos pelo #168 em 04/07 e o backlog não sabia; TEST-BL4 reduzido ao que falta de fato — o E2E de cancelamento já existia), **#247** (CLAUDE.md dizia que a stack mobile é NativeWind; medição real: 58 arquivos `StyleSheet.create` / 57 `useTheme()` × 5 `className`, e 3 desses concentram 144 das 147 ocorrências — quem lê a regra de transcrição literal escrevia tela nova no padrão errado), **#237** (últimas cores hardcoded → tokens; rebaseado antes de mesclar porque os checks eram de 16/07 e o `Security Audit` só ficou verde com o #239. **Registro: o coração favoritado mudou de `#E74C3C` para `#C0392B` no claro** — o PR se descrevia como "zero mudança visual", e não era). **Higiene de repo:** 76 → 7 branches locais e 3 → 1 worktrees, apagando só o comprovadamente contido na `main` (em repo com squash, `git branch --merged` subconta e `git cherry` superconta — decide comparar conteúdo + estado do PR). Apagada também a remota `docs/guia-testar-app-android`, cujo tip `815808d` era o commit órfão que fez a categoria Moda ressuscitar. **Aberto, nada bloqueante:** TEST-BL3 e as 3 telas de perfil em NativeWind (decisão dos fundadores), TEST-BL4 parcial (falta E2E de reembolso + axe nos forms), dois `ADR-027` colidindo em número, 23 vulnerabilidades abaixo de `critical` + `dependabot/sharp-0.35.3`, 6 branches de docs sem PR, e a senha do staging antigo ainda no histórico do git (sem alvo — projeto deletado).

</details>

<details>
<summary><strong>Histórico de atualizações anteriores</strong></summary>

**2026-07-16** — **6 bugs reais do app mobile achados por usuários (Roberto + tester Raimundo) e corrigidos, todos validados em device com rebuild do APK grátis.** Sessão de feedback pós-distribuição do APK: (1) **RentBanner ausente na Explorar** — o app não tinha o card "Como alugar no ShareO" que o site mostra com `?intent=rent`; criado `apps/mobile/components/items/RentBanner.tsx` (transcrição de `app/itens/_RentBanner.tsx`) e injetado sempre visível na tela; (2) **CTA "Quero ser avisado" duplicado na Home** — `<ListaVIP/>` já embute o `FounderCaptureForm` internamente, mas um comentário antigo (nunca verificado) alegava que o site repetia o form perto do rodapé — **falso**, o site chama `<ListaVIP/>` uma única vez; bloco duplicado removido; (3) **pull-to-refresh não funcionava na Início** — `ScrollView` sem `RefreshControl` (Explorar já tinha); adicionado refazendo as 2 queries da Home; (4) **rótulos do FilterBottomSheet cortados sob fonte grande** (achado pelo tester Raimundo, device com acessibilidade "tamanho de fonte" aumentado) — 1ª tentativa `flexShrink:1` **não bastou** (Text encolhia mas não quebrava linha, por manter `flexBasis:auto`); fix definitivo = `flex:1` (zera flexBasis, dá largura real pro wrap), **validado via `adb shell settings put system font_scale 1.3` + cold restart real** (força-stop antes, não só `am start`); (5) **distância default alterada pra "Até 2 km"** (decisão do fundador; site mantém "Qualquer" — divergência mobile-only documentada no código); (6) **RentBanner cortado pelos chips de categoria** — causa raiz: wrapper era um `ScrollView` (`scrollEnabled=false`) que não dimensiona ao conteúdo como uma `View` comum; trocado por `View` simples. **Achado que NÃO era bug:** testando fonte grande, login e bottom nav pareciam cortados também — **investigação confirmou que era artefato do método de teste** (mudar `font_scale` via adb com o processo já rodando não recalcula layouts existentes; só um cold-restart de verdade reflete a mudança) — nenhum código extra foi tocado. **GOTCHA de método de teste confirmado:** `adb shell am start` num processo já rodando só o traz pra frente ("Warning: Activity not started..."), não reinicia — pra testar mudança de `font_scale`/Configuration, sempre `am force-stop` antes. 6 commits no main, todos com tsc+jest verdes (610/611, falha pré-existente no calendário do item sem relação), 3 builds do APK grátis disparados e validados em device (Moto G56) a cada fix. Link estável de distribuição (mesmo de sempre, sobrescrito a cada build): `github.com/robertoepifanio-byte/SHAREO/releases/download/apk-preview/shareo-preview.apk`.

**2026-07-10** — **✅ APK GRÁTIS (GitHub Actions) INSTALADO E NAVEGADO EM DEVICE (Moto G56).** O `shareo-preview.apk` (build `.github/workflows/apk-build.yml`, v1.0.0 arm64, aponta staging) foi baixado do Release público, instalado e navegado sem crash: login nativo → **Explorar** (chips 80px centralizados, **Moda removida**, **Esporte/Lazer** — tudo confirmado no device) → **detalhe do item** (banner economia 95%, card do dono, **calendário de disponibilidade** Jul/Ago) → **calculadora de locação** (Diário R$20 / Semanal R$60 [3×] / Mensal R$300 [15×]) → **MobileMenu** (toggle de tema Claro/Sistema/Escuro + Explorar▾/Anunciar▾/Atividade/Minha Conta▾/Sair). **GOTCHA de instalação resolvido — `INSTALL_FAILED_UPDATE_INCOMPATIBLE`:** o APK debug-signed conflitava com o build EAS anterior (assinatura diferente); o pacote `com.shareo.app` era um **fantasma** (não listado em `pm list` de nenhum dos 2 perfis do device — `0:Roberto`+`11:Vault Profile`) mas `adb uninstall com.shareo.app` GLOBAL retornou `Success` e destravou o install. Sequência confiável: `adb uninstall` → `adb install` → `adb shell am start -n com.shareo.app/.MainActivity` (`monkey -c LAUNCHER` não focou). Testers com versão anterior precisam desinstalar antes. Nada tocado em "Solicitar locação"/"Sair" (evitar reserva/notificação real no celular pessoal). **0 PRs abertos.**

**2026-07-09 (madrugada)** — **EXPO_TOKEN RESOLVIDO + PRs #226–#230 e #214 (iOS Fase 1) MESCLADOS no main.** 2ª parte da sessão: **EXPO_TOKEN recriado por Roberto** → 2 builds EAS gerados (`81a980f6` preview, `1a8afc14` development); o **development foi instalado no device** (via `curl` do `applicationArchiveUrl` + `adb install -r`) e virou a base de verificação — **crítico: só o build EAS limpo elimina o erro `topSvgLayout` do dev-client velho, que MASCARAVA a verificação do Explorar**. Mesclados: **#226** ícone "Todas" branco no dark (flood-fill, igual #224); **#227** remove categoria **Moda** + renomeia **Esporte→"Esporte/Lazer"** (código em ~19 arquivos + banco de **staging atualizado ao vivo**: moda excluída [0 itens], esporte renomeado; refaz o commit órfão `815808d` que nunca virou PR e por isso "voltou"; GOTCHA: import PJ casa por `category.name` → dropdown do template atualizado); **#228/#229/#230** chips de categoria do **Explorar** (bug real: rótulos sumindo no Android) → `CategoryChip` vira **card vertical** (ícone 80px em cima, rótulo 2 linhas embaixo, centralizado sobre o rótulo), **ScrollView horizontal precisou de `height` explícita** (não auto-cresce), tudo device-confirmado; **#214 iOS Fase 1** — mesclado após resolver merge grande com o main (8 conflitos: peguei o main para os arquivos de UI/dados já verificados hoje — CategoryChip/explorar/FilterBottomSheet/MobileMenu/seed — e o branch para os arquivos iOS-específicos; `@rnmapbox/maps` instalado; gates re-rodados: mobile tsc0/jest611/bundle1889, web tsc0/jest872; mapa nativo entra como scaffolding INATIVO — "Ver no mapa" segue abrindo o mapa web). **Distribuição de build a testers BLOQUEADA pela cota EAS:** ao gerar preview novo do main achei+corrigi uma regressão do #214 (plugin `@rnmapbox/maps` força prebuild → policy `appVersion` exigia `expo-updates` ausente → **#232 fixa `runtimeVersion` literal `"1.0.0"`**, sem dep nova; o build passou do prebuild) mas parou na **cota de builds Android do plano Free do EAS** (esgotada este mês, reset ~31/07). **✅ CONTORNADO com alternativa GRÁTIS:** repo é público → GitHub Actions grátis/ilimitado; novo workflow `.github/workflows/apk-build.yml` (#234/#235) builda o APK via `expo prebuild`+Gradle e publica num Release público — **build verde E2E, APK 49.86 MB em github.com/robertoepifanio-byte/SHAREO/releases/tag/apk-preview** (link direto, sem login; sem mapa nativo/adiado; só arm64-v8a). Causa raiz destravada: `babel-preset-expo` não era declarado (pnpm estrito o escondia do bundle do Gradle) → declarado como devDep. **0 PRs abertos.**

**2026-07-09 (noite)** — **PRs #221–#224 MESCLADOS no main (reserva + rodapé + hero carro+moto + menu reordenado + ícone dark) — tudo confirmado em device E web. EXPO_TOKEN achado INVÁLIDO (depois resolvido, ver acima).** Sessão longa fechou 4 PRs, todos validados ao vivo no device (Moto G56) e por Roberto na web (staging): **#221** — 2 fixes de reserva (pull-to-refresh com `RefreshControl` no detalhe; câmera no CheckInOut/ReturnChecklist via helper `pickImageAsset()` com Alert câmera/galeria) + fix de navegação do rodapé (`AppFooter` usava allowlist invertida `NATIVE_ROUTES` defasada → Sobre/Perfil/Cadastro abriam no navegador; corrigido espelhando o `MobileMenu`) + **6 telas nativas do rodapé** (Comunidade/Como ganhar/Termos/Privacidade/Segurança/Central de ajuda, transcritas 1:1 por 6 agentes paralelos, fiação central; `/ajuda` com 13 seções + âncoras via `?anchor=`); **#222** — nova imagem do hero (carro+moto+furadeira/câmera/caixa/projetor/bike/escada), PNG 1346×820→webp via sharp, **cache-bust: renomeado `hero-items.webp`→`hero-carro-moto.webp`** (reusar nome serviria cache velho no CDN); **#223** — reordenação do menu lateral (web+mobile): **Tema subiu p/ o topo** (acima de Início) e **Sobre desceu** p/ acima de Sair/Entrar; **#224** — ícone de Eletrodomésticos (`casa-jardim.png`) tinha interior verde/transparente ≠ dos outros 6 (interior branco) e destoava no dark mode; corrigido com **flood-fill de branco no interior** (sharp: BFS das bordas marca o exterior, pinta só o interior transparente, preserva anel+cantos), 512×512, aplicado em `public/icons/` + `apps/mobile/assets/icons/`.

**Como o re-teste foi destravado:** o dev-client `com.shareo.app` tinha sumido do celular pós-reboot → rebaixado o build EAS development anterior (`455bab6d`) via `eas-cli`+`adb install` (dev-client carrega o JS ao vivo do Metro).

**Gotcha recorrente:** LogBox `Unsupported top level event type "topSvgLayout"` — mismatch do `react-native-svg` nativo do build velho (06/07) com o JS atual; **NÃO-FATAL** (some com build EAS novo).

**🔴 EXPO_TOKEN INVÁLIDO:** o secret existe (07/07) mas ao disparar `eas-release.yml` o `eas build` falha com **"The bearer token is invalid"** — Roberto precisa recriar o token em **expo.dev/settings/access-tokens** (conta `shareo`) + `gh secret set EXPO_TOKEN`; depois re-disparar (o workflow tem `workflow_dispatch` com inputs profile/platform; `--no-wait`). Nenhum build EAS válido ainda; zero tags `mobile-v*`. — histórico anterior: 2026-07-09 (dia) — **Fluxo de reserva do app validado FIM A FIM em device (Moto G56) + tags de release por plataforma + manual do fundador.** Sessão cobriu 3 frentes: **(1) Validação ao vivo** — o ciclo completo de locação no app Android foi confirmado end-to-end num device real (criar reserva→aprovar via API admin→marcar paga→`mark_active`→código de retirada `792052`→ContractBanner assinado→ReturnCountdown/Checklist→CheckInOut sem crash→Devolver→ReviewForm dono 5★→`confirm_return`→**COMPLETED**), reconfirmando os 2 fixes críticos anteriores (bug `paymentStatus`: botão Pagar some + caixa do código aparece) e o sweep de auth #188 (ContractBanner+ReviewForm passam sem 401 no Bearer). Plano `docs/planos/plano-validacao-app-android-pwa.md` atualizado com ✅ CONFIRMADO EM DEVICE.

**(2) Tags de release por plataforma (ADR-027, PR #219 mesclado)** — `web-v*`→deploy web (`deploy.yml`, antes `v*` bare), `mobile-v*`→build EAS. Dois pontos load-bearing no `deploy.yml` (glob `on.push.tags` + `if: startsWith(...,'refs/tags/web-v')`) e um órfão pego na verificação adversarial (`scripts/setup-supabase-envs.md:117` allowlist do Environment production). `package.json` raiz 1.11.0→1.12.0; **mobile NÃO bumpa** (runtimeVersion `appVersion` acopla → quebraria dev-client instalado).

**(3) Manual do fundador (PR #220 mesclado)** — `docs/manual-desenvolvimento-shareo.md` (~400 linhas, 9 seções) + Artifact publicado (🧭).

**🔧 Pendente:** PR #221 aberto (branch `fix/mobile-reserva-refresh-camera`) com 2 fixes em `apps/mobile/app/reservas/[id].tsx` — pull-to-refresh (RefreshControl) e câmera no CheckInOut (Alert Tirar foto/Galeria, antes só galeria) — **ambos device-UNVERIFIED**: Metro/dev-client no Windows travou em bundle cacheado (watcher/EPERM), re-teste precisa de reboot do PC / Watchman / build EAS novo. Telas novas ainda não vistas em device: Sobre, Dashboard, Meus Anúncios, perfil/*, LGPD, anunciar/dicas, anunciar/estimativa, calendário/grids do detalhe. — histórico anterior: 2026-07-07 (tarde) — **Smoke E2E automatizado do app Android (emulador + Maestro) — PR #218 aberto, 100% verde.** A pedido do Roberto ("quero validar novamente o app Android"), em vez de testar no celular físico do fundador (risco documentado de reserva/notificação real acidental), criado `.github/workflows/android-smoke.yml` espelhando o `ios-smoke.yml` (PR #214): emulador Android na nuvem (`reactivecircus/android-emulator-runner`, API 34 google_apis) + roteiro Maestro (`apps/mobile/.maestro/android-smoke.yaml`, login→Home→Explorar→mapa Mapbox nativo). Build usado: EAS preview `55e2dd7e` (commit `f99598d`, já com os fixes recentes — login "Voltar para o início", ícone Eletrodomésticos, correções de device s41, botão voltar Dashboard, fix do menu).

**Resultado 100% verde** (run `28894009403`), confirmado visualmente: Home com stats reais (88 itens/44 proprietários/7 categorias), mapa nativo Mapbox renderizando em dark mode.

**2 gotchas de CI resolvidos**: (1) `reactivecircus/android-emulator-runner` roda cada linha do `script:` como uma chamada `sh -c` isolada, não um script contínuo — a continuação `\` do Maestro virou path literal quebrado, corrigido movendo a lógica pra um `.sh` próprio; (2) o emulador Android (cold start, sem GPU real) demora mais pra hidratar o bridge JS que o simulador iOS — o 1º input (e-mail) se perdeu silenciosamente mesmo com o log dizendo `COMPLETED`, corrigido com `waitForAnimationToEnd` antes de cada tap/input + `assertVisible: ${EMAIL}` como checagem intermediária.

**PR #218 não mesclado ainda** — pendente decisão do Roberto sobre deixar rodando automaticamente em todo PR mobile. — histórico anterior: 2026-07-05 (tarde) — **5 telas nativas novas + regressão do menu corrigida + 4 bugs de device + 1º build "preview" pra testers externos.** **PR #210**: regressão em `MobileMenu.tsx` — um fix anterior usou prefixo genérico `/perfil/` que sequestrava pro navegador telas nativas JÁ construídas (editar/endereco/seguranca/recebimentos/repasses/dados); corrigido com lista exata + `ROUTE_ALIASES`.

**PR #211**: as 5 telas que realmente não tinham equivalente nativo — Sobre, Meus Anúncios, Dashboard, Estimativa de Ganhos, Dicas para Anfitriões (5 agentes em paralelo, 1 arquivo cada, integração de wiring feita centralmente; novas rotas `GET /api/dashboard` e `GET /api/platform-config/public`).

**PR #212**: 4 bugs achados testando ao vivo no device — Dashboard sem botão voltar (tela prendia o usuário); `SafeAreaView` de `"react-native"` é iOS-only, no Android ficava sem inset (menu colava na status bar) — trocado por `react-native-safe-area-context`; ícone do chip de categoria em Explorar menor que o site (comentário dizia 52, código tinha 40); **decisão do fundador**: ícones de categoria aumentados pra 96px nos dois lados (site `app/itens/page.tsx` + mobile), não é mais divergência mobile-only.

**Build `a5737229`** (profile `preview` → APK autônomo, sem Metro) é o primeiro build desta fase realmente distribuível a testers externos sem precisar do ambiente de dev. — histórico anterior: 2026-07-05 (manhã) — **cadastro nativo do app mobile (PR #208), resolve decisão D1 do handoff.** `apps/mobile/app/(auth)/register.tsx` era a última tela do app que redirecionava para o site (`Linking.openURL` para `shareo.com.br/cadastro`); agora transcreve `RegisterForm.tsx` campo a campo (nome, e-mail, senha com força+mostrar/ocultar, cidade/estado, aviso LGPD, consentimento Termos+Privacidade+versão, confirmação de idade 18+, banner de indicação `?ref=`), reutilizando a mesma rota server-side (`POST /api/auth/register`) e o login JWT nativo já existente para o auto-login pós-cadastro (em vez de `signIn`+`/bem-vindo`, que não tem equivalente nativo).

**Zero telas do app mobile redirecionam para o site.** Também rodado, além de `tsc`+`jest`, um bundle real via `expo export` para confirmar que o Metro processa o arquivo (lição da sessão anterior: `tsc`/`jest` verdes não garantem bundling). — histórico anterior: 2026-07-04 noite — **limpeza de PRs pendentes + MOB-BL3 (dark mode do app) resolvido.** Mesclados: #191–195 (fixes/cleanup que tinham ficado abertos), #168/#170 (testes/a11y do site), #202 (LGPD — CI corrigido, `pnpm-lock.yaml` desatualizado), **#206 dark mode** (migração de ~500 cores hardcoded → tokens dinâmicos em 35 arquivos de `apps/mobile/`, 6 agentes em paralelo, testado em device real pelo fundador; achado 1 bug real só em runtime — `declare` em campo de classe do `ErrorBoundary.tsx` passava no `tsc` mas quebrava o bundle do Metro, corrigido). Fechados como obsoletos: #166/#167 (auditoria UI/UX de 02/07, 100% superada pelo redesign).

**Zero PRs abertos ao final.** Resta só o mapa nativo (Mapbox), pendente de build EAS supervisionado (junto com a validação visual definitiva da tela de LGPD do #202, que também depende do mesmo build). — histórico anterior: 2026-07-04 manhã (validação em device do app Android — Moto G56, dev-client contra staging real — cobrindo os PRs de features/auth/testes mesclados em 07-03/07-04: **#183–#190**, ver `docs/auditorias/validacao-manha-2026-07-04.md`).

**Ciclo E2E completo validado numa reserva real**: pagamento (confirmação manual admin) → código de retirada → contrato assinado → CheckInOut/checklist de devolução → devolução confirmada → **Concluída** → avaliações do item e do proprietário enviadas.

**2 bugs reais achados e corrigidos**: (1) encoding corrompido no campo `city` do usuário Admin ShareO no banco do **shareo-staging** (`ef bf bd` no lugar do "ã" de "São Paulo", provável seed/reparo rodado com encoding errado) — corrigido via UPDATE direto, verificado por API; (2) `ContractBanner` do app mobile travava mostrando "assinatura pendente" mesmo com o contrato já assinado no backend (`useEffect` de sincronização só rodava uma vez) — corrigido em `apps/mobile/app/reservas/[id].tsx`, **PR #191 aberto**. Novo `docs/backlog-site-padroes-pos-validacao-mobile.md`: o mesmo anti-padrão (`useState(initial...)` sem resync) existe em 3 componentes do site (`_ContractBanner.tsx`, `_CheckInOut.tsx`, `_ReviewForm.tsx`) — não reproduzido lá, roteiro de verificação documentado pra outra sessão. Outras 3 suspeitas de bug (date picker sem bloquear data ocupada, "falha silenciosa" no submit, seletor de tema) foram **descartadas** após checar o código — paridade correta com o site ou comportamento intencionalmente gated. — histórico anterior: 2026-07-01 (s41 cont. — **rodada "itens sem dependência externa, em paralelo" (3 streams)**: **#150** polimento de navegabilidade (10 achados: N2 rótulo Indicações; **N3 tab Chat habilitada** →/mensagens; F4 chip #conta; F5 anchors MobileMenu; **F6 CTA "Avaliar"** na lista de reservas; F7 `?back=` em dashboard/favoritos/meus-anúncios; F8 atalhos Recebimentos/Repasses; P1 back-links /anunciar; P3 copy selfie; P6 link dados→documentos); **#149** UI polish (P7/P8 checkbox, P10 spinner) + **log admin `kyc.selfie.view`** (com after()) — **peguei no review 2 problemas do agente e corrigi antes de mesclar: P19 era REGRESSÃO de contraste (revertido text-white) e o log estava fire-and-forget no render (→after())**; **#148** rascunho do adendo à Política sobre biometria (gated). Lição: subagente pode introduzir regressão — revisar diff antes de mesclar. tsc+lint verdes.

**2026-07-01 (antes)**: **achados das 3 revisões RESOLVIDOS**: N1 dead-link do CTA mobile de reserva → **#142** (rola até `#price-calc`); a11y do modal de KYC → **#143** (role=dialog+aria+foco/Escape/focus-trap+scroll-lock; tap targets ≥44px); dark mode da Central de Ajuda → **#144** (overrides `dark:` seções/callouts + token do status PENDING; **verificado no preview**); prep pró-flag biometria → **#145** (botão de revogação na UI + export art.20). Backlog atualizado (#146). Restam pós-D4/DPO: log admin `kyc.selfie.view` + adendo à Política. Tudo inerte com flags OFF.

**2026-06-30**: **item 4.4 BIOMETRIA implementado + 3 revisões (código/UI/nav)**: **#139** consentimento biométrico da selfie do KYC (LGPD art.11, decisão C1) atrás da flag `biometricConsentRequired` **OFF** → KYC atual idêntico; schema+migration aditiva, endpoint de revogação, painel de consentimento no modal. 3 subagentes revisaram read-only; **#140** aplicou os fixes SEGUROS (guard da revogação não rebaixa VERIFIED antigo; Ajuda "Privacidade e dados→Verificar"→"Documentos"; "Histórico financeiro"→"Meus repasses"; "7 dias/semana"→seg–sex 09h–17h; texto do consentimento ≥12px). **Gotcha:** no Bash `git commit -m @'...'` prefixa `@` no assunto (usar `-F`). — histórico da mesma sessão: **PARECER FORMAL recebido (MP como PSP) + respostas dos fundadores à pauta EXECUTADAS**: 9 PRs mesclados (#129–#137) — Central de Ajuda migrada p/ Mercado Pago (SLA 8h/4h seg–sex 09–17, sem "seguro 1%"); legal-hold + logs Marco Civil art.15 c/ `after()` (#130); drafts jurídicos A1–E3 + biometria (#131); **boleto excluído** do Checkout Pro (#135, cartão+Pix); **cláusula E3 art.19 reescrita** como faculdade futura (#136, canal de denúncia = roadmap H2); **copy de pagamento p/ validação da advogada** (#137, backlog 📅 01/07); PIX manual = tarefa de cutover documentada (#133); pauta+PDF (#134). **Correção conceitual:** publicar copy MP em staging NÃO é propaganda enganosa (staging=teste, sem consumidor real, MP OFF); CDC art.30/37 só vale no go-live de produção. Tudo **gated D4**, flags OFF. — histórico da mesma sessão: **Mercado Pago Fase 2 MESCLADA + CICLO E2E VALIDADO no sandbox** (PRs #120/#121/#122 no ar; reserva PAID + split R$12/R$68 + webhook REAL do MP + idempotente; validado via API server-to-server contornando as paredes do sandbox) + **override de sandbox `MP_SANDBOX_SELLER_TOKEN`** (#122, staging-only, 🔴 remover antes do go-live) + **roteiro de teste p/ validação humana** (`docs/guias/roteiro-teste-mercadopago.md`/`.pdf`) + **meta s41: revisão da Central de Ajuda** (página descreve Stripe; modelo é MP). D4 segue gated pelo parecer FORMAL. Histórico s40 abaixo — **Mercado Pago Fase 2 implementada** (OAuth+checkout split+webhook na branch `feat/mercadopago-fase2-oauth-checkout`, atrás da flag OFF; split validado contra a API real do sandbox; E2E interativo só em staging) + **meta de conformidade zero-dependência ENTREGUE** (auditoria + RIPD/transferência #116 + aceite de contrato #117 + logs art.15/expurgos #118, todos flag-OFF) + **Mercado Pago Fase 1 scaffolding inerte** (#119 mesclado, flag OFF) + **guias D4/MP refrescados** (cobrança #115 mira o parecer FORMAL; guia dos fundadores p/ Modelo B). D4 segue gated pelo parecer FORMAL. Histórico s39 abaixo — **migração Supabase dev+staging → org corporativa** (refs novos, staging no ar + E2E verde) + base demo no banco novo; **falso-bug de cadastro do tester = URL/deploy Vercel antigo (resolvido, não era código)** + PRs #112/#113/#114 mesclados (UX/resiliência + docs `shareo-prod` + health check) + **deploys Vercel antigos despublicados (Vercel Authentication ON, só `staging.shareo.com.br` público)** + **briefing jurídico do D4 (`docs/juridico/briefing-juridico-d4.md` + PDF) e skill `/shareo-juridico`** (2 questões fundacionais novas: contrato/caução e Marco Civil) + **parecer D4 PRELIMINAR recebido (modelo viável, 5 ajustes; Mercado Pago decidido; marca registrada) → `docs/juridico/checklist-conformidade-juridica.md`; D4 ainda gated pelo parecer FORMAL**; antes: s38 — walk-through PIX manual validado + fixes importação Google Sheets; histórico abaixo)

</details>
**Ambiente staging**: https://staging.shareo.com.br (canônico desde 15/06; `shareo-rouge.vercel.app` segue como alias) — deploy via GitHub Actions "Deploy - ShareO" (token); projeto Vercel **shareo** agora no Team **shareo-marketplace** (Pro), Production = staging
**Último commit (main)**: `6d2e04a` (#214, iOS Fase 1). Sequência recente no main: `0388640` (#226 ícone Todas) → `f3c9735` (#227 Moda removida/Esporte→Esporte-Lazer) → `f047e42`/`daa0b4a`/`19b1a1a` (#228/#229/#230 chips Explorar) → `6d2e04a` (#214 iOS Fase 1). Antes: #221–#225 (reserva/rodapé/hero/menu/ícone dark). **0 PRs abertos.** `package.json` raiz `1.12.0`; mobile ainda `1.0.0`. ✅ **EXPO_TOKEN resolvido** — builds EAS `81a980f6` (preview) e `1a8afc14` (development, instalado no device) prontos; preview novo do main atual disparado p/ testers. Ainda zero tags `mobile-v*` (corte de release mobile pendente). s41 (cont. 2026-06-30→07-05) — parecer FORMAL + respostas da pauta (#129–#137) + biometria #139 + fixes das revisões (#140, #142–#146) + rodada paralela nav/UI/audit (#148–#150) + **Sprint de Endurecimento Zero-Dependência (#152–#155)** + banner Embaixadores (#157–#159) + **release v1.11.0** + **build-out do App Android (#160–#163)** + **redesign do app mobile por transcrição do site (#171–#173)** + **expansão de features + auth sweep + testes (#183–#190)** + **6 telas nativas de Minha Conta + LGPD (#196–#202)** + **limpeza de PRs pendentes + dark mode ativado (#191–195, #168–170, #206)** + **STATUS.md (#207)** + **cadastro nativo (#208)** + **STATUS.md (#209)** + **fix regressão menu (#210)** + **5 telas nativas novas (#211)** + **fixes de device + ícones 96px (#212)**. **0 PRs abertos.** Build `preview` `a5737229` disponível pra testers externos.

**📱 Build-out do App Android — META CUMPRIDA (2026-07-02):** meta `planos/meta-app-android-build.md` executada — **4 PRs mesclados**: **#160** Fase 1 build-ready (hipótese Gradle H1: `react-native-css-interop` ausente das deps do NativeWind 4 → adicionado; lockfile regenerado no review; guia `mobile/mobile-build-android.md`); **#161** Fase 2 reservar+pagar (tela de datas espelhando `_PriceCalc`, `apps/mobile/lib/pricing.ts`, botão Pagar; **rota MP unificada**: `/api/payments/mp/checkout` com `resolveUserId(req)` + `client:"mobile"` → back_urls deep-link `shareo://`, rota duplicada removida no review, −126 linhas); **#162** Fase 3 docs de loja (listing/Data Safety/checklist); **#163** Fase 5 paridade (**Anunciar PF** com fotos DRAFT→AVAILABLE, **Favoritos** com optimistic update, **KYC** doc+selfie respeitando a flag de biometria; 5 rotas web ganharam Bearer via `resolveUserId` — guard de images inlinado com semântica idêntica ao `assertOwnerOrAdmin`, verificado; **mapa ADIADO** — exige config nativa). **Validação: E2E Regressão staging SUCCESS ×2 (pós-#161 e pós-#163) — site mobile-first intacto**; tsc 0; jest web 791; jest mobile 36; health healthy. **Pendem (externos/gated):** Fase 4 go-live (D4); disparo do `eas build` (conta Expo dos fundadores — `docs/mobile/mobile-build-android.md`); conta Play + screenshots; mapa (backlog). Flag `mercadoPagoEnabled` segue OFF — fluxo inerte-seguro.

**🧹 Sprint de Endurecimento Zero-Dependência (2026-07-01, s41):** meta `planos/meta-hardening-zero-dependencia-s41.md` (regras: 0 feature nova, nada quebrado, sem dependência externa, gated D4). 5 especialistas (2 dev + 3 auditoria read-only). **4 PRs mesclados, tsc+lint verdes:** **#152** mobile/hardening (`API_URL`→staging p/ contornar Vercel Auth 401, `eas.json` `appVersionSource`, copy pagamento; `logAccess` já era `after()`); **#153** +309 testes unitários/a11y jest-axe (488→797) + bug WCAG `RatingStars` (`role="img"`); **#154** segurança (**A1** `emailVerifyToken` gravado em claro→`hashToken` — bug real; **A2** extensão de upload via MIME e não via nome do cliente, dedup `EXT_BY_MIME` em `lib/imageUpload`; **M1/M2** não vazar erro cru do Storage; **M3** não logar token de reset; **M4** rate-limit `upload:30/min` nos 4 endpoints; **M5** `timingSafeEqual` no cron-guard); **#155** a11y WCAG (tap targets ≥44px, `aria-haspopup`/`aria-label`/`aria-hidden`/`role=img`, contraste `ListingQualityIndicator`, dark mode em `_BookingActions`/`app/page.tsx`). **Backlog consolidado** (`backlog-atividades-priorizadas.md`, seção nova) com ~34 achados p/ ANÁLISE: SEC-BL1–8 (tokens/segredos/chave PIX em claro→migração dupla-leitura; cripto de chat; budgets de rate-limit), A11Y-BL1–9 (focus-traps, navegação por seta, min-height, ordem do menu), DRY-BL1–11 (dois `EmptyState`, cores de status divergentes, `packages/shared`, dedup de formatadores bit-idêntico), TEST-BL1–4, MOB-BL1–2. **Lição confirmada:** revisei o diff de CADA PR antes de mesclar (bug real A1 pego na revisão; `type="button"` conferido = sem `<form>`).
**Release atual**: [`v1.11.0`](https://github.com/robertoepifanio-byte/SHAREO/releases/tag/v1.11.0) — 84 commits desde v1.10.0: MP Fase 2 (flag OFF), biometria, RIPD/logs art.15/contrato (flags OFF), Sprint de Endurecimento (#152–#155), dedup/avatar, import PJ. Rollback: `git checkout v1.10.0`. Produção segue gated por D4.
**Release anterior**: [`v1.10.0`](https://github.com/robertoepifanio-byte/SHAREO/releases/tag/v1.10.0) — baseline pré-refatoração de deduplicação.
**Release anterior**: [`v1.9.0`](https://github.com/robertoepifanio-byte/SHAREO/releases/tag/v1.9.0) — **Modo Escuro (dark mode)** (PRs #51–#62) — no ar no staging; aguarda D4 para produção

---

## ⚠️ BLOQUEADOR ATIVO

**D4 — parecer jurídico FORMAL RECEBIDO (2026-06-30, revisado com o Mercado Pago como PSP).** ShareO deixa de ser *merchant of record* → risco da Lei 12.865 reduzido. **Condição 1 de 4 de go-live cumprida (parecer ✅).** Restam: **(2) contrato Mercado Pago assinado + conta PJ ativa — EM ANDAMENTO (fundador; único bloqueador totalmente em aberto)**; (3) Termos/Política revisados e **publicados** (conteúdo aprovado — resposta D1; publica só no go-live); (4) checklist 100% (destravado; pendem B3 tributarista, C2 DPAs, C3 RIPD/DPO). **Respostas dos fundadores à pauta jurídica registradas** (16/17) — ver [`docs/juridico/checklist-conformidade-juridica.md`](juridico/checklist-conformidade-juridica.md) e a pauta [`docs/auditorias/pauta-revisao-fundadores-s41.md`](auditorias/pauta-revisao-fundadores-s41.md). **Nenhuma atividade de produção** (Supabase prod, deploy, DNS, go-live) antes do cumprimento das 4 condições. Regra absoluta.

---

## 🗑️ Configuração de validação REMOVIDA (histórico) — PIX manual da plataforma

**Esta seção descreve uma configuração que NÃO EXISTE MAIS.** Removida integralmente do código em **2026-08-19** (ver entrada do dia no topo deste arquivo e [ADR-028](adr/ADR-028-reversao-stripe-connect.md)) — código, campo `Booking.pixDeclaredAt` e a seção `PlatformPixConfig`/`getPlatformPixConfig()` de `lib/platform-config.ts` foram excluídos, não apenas desligados.

Histórico, mantido só para contexto: entre **2026-06-25** (s38) e **2026-08-19**, a fase de validação em staging usou o "PIX ShareO" configurado como a chave PIX **pessoal** de Raimundo Gomes da Silva (Banco Santander) — validado ponta a ponta em 25/06 (reserva `cmqtwxdun0005tvyghov9jwto`, split R$20,25/R$114,75 calculado corretamente, PR [#84](https://github.com/robertoepifanio-byte/SHAREO/pull/84)). Era **STAGING apenas** (`platformPixEnabled` default off) e sempre tratado como temporário — o risco regulatório de dinheiro de terceiros passando pela conta pessoal de um sócio motivou a remoção definitiva junto da reversão para Stripe Connect.

---

## Resumo Executivo

**💳 s41 — Mercado Pago Fase 2 MESCLADA + CICLO E2E VALIDADO no sandbox** (2026-06-30). A Fase 2 saiu da branch e foi para `main`: **PR #120** (OAuth onboarding + checkout split + webhook), **PR #121** (fix Modelo B: `paymentClientForSeller()` consulta o pagamento com o token do **vendedor** — token da app dá 404 no split), **PR #122** (override de sandbox). **✅ Ciclo E2E completo validado no staging:** reserva `cmqzuebqp0001ky04ttba3kg9` → **PAID**, `mpPaymentId`, `pickupToken` gerado, **split correto** (R$80 → taxa R$12/15% → locador R$68); o **webhook REAL do MP** chamou o staging sozinho e marcou PAID, e um POST manual posterior foi **idempotente** (fila `mercado_pago_event_queue` COMPLETED, sem efeito duplicado). **Como contornei as paredes do sandbox** (OAuth com test user exige verificação de e-mail que não chega; Checkout Pro no navegador dá "uma das partes é de teste" se logar): pagamento criado via **API server-to-server** (`/v1/card_tokens` + `/v1/payments`, cartão `APRO`, coletor = conta de teste do app). **⚠️ Override de sandbox (#122):** `sandboxSellerTokenOverride()` lê `MP_SANDBOX_SELLER_TOKEN` (env staging) → checkout/webhook usam esse token, pulando o decrypt do campo cifrado. Necessário porque o token semeado fora cifrado com a `ENCRYPTION_KEY` **local**, mas o staging usa outra (a do staging é **Sensitive no Vercel = irrecuperável**; provado: 0/30 `cpfEncrypted` do staging descriptografam com a chave local). **🔴 REMOVER** (env + helper + 2 call sites) antes do go-live. **Roteiro de validação humana** entregue: `docs/guias/roteiro-teste-mercadopago.md`/`.pdf` (regra única = pagar **como convidado**, sem logar no MP; canal de report WhatsApp 84 99662-2346). **🔜 Pendente:** retorno dos testers; `MP_WEBHOOK_SECRET` (valor do painel MP + Vercel — ação humana); remover #122; desacoplar PIX NOT NULL fica p/ a remoção do legado. Flag `mercadoPagoEnabled` **ON no staging** (decisão do fundador). Nada em produção (gated **D4**). Ver [[project-mercadopago-migration]].

**🎯 META s41 — Revisão da Central de Ajuda (`/ajuda`) pelos especialistas** (estabelecida 2026-06-30). **Gatilho:** a página `app/ajuda/page.tsx` ainda descreve o pagamento via **Stripe/cartão de crédito** (passos do locatário #5, FAQs "Pagamento e Segurança", "Como o pagamento funciona", "Como funciona o pagamento do locatário", "O ShareO é regulamentado") — porém o modelo **decidido** é **Mercado Pago (PIX/cartão/boleto)**, Modelo B/split, repasse semanal. **Objetivo:** revisar e atualizar TODA a Central de Ajuda contra o estado atual do produto e do parecer D4, sem publicar nada voltado a pagamento antes do sign-off (gated D4). **Escopo por especialista:** (1) **product-owner-shareo** — varrer cada passo/FAQ e listar divergências com o modelo decidido (MP no lugar de Stripe; repasse semanal às segundas; taxa 15% dinâmica via `getPlatformFeeRate()`; teto R$500/transação; valor máx. do bem R$1.000; onboarding de recebimento via conexão MP) e priorizar correções; (2) **designer-shareo** — clareza/escaneabilidade/a11y dos accordions e da busca, sem regressão visual; (3) **seguranca-shareo/jurídico (`/shareo-juridico`)** — checar afirmações sensíveis (caução, seguro 1%, "dinheiro retido na plataforma", LGPD/DPO, "regulamentado pelo Banco Central via Stripe") contra o parecer D4 e a decisão MP, marcando o que **NÃO pode ser publicado** antes do D4. **Entregável:** relatório read-only com lista priorizada de edições + rascunho do texto novo das seções de pagamento (atrás de revisão), **sem mesclar mudanças voltadas a pagamento até o D4**. **Restrições:** não inventar funcionalidade; manter taxa via `getPlatformFeeRate()` (nunca hardcode); produção gated por D4. _Pronta para despachar aos subagentes quando o fundador autorizar._

**💳 s40 — Mercado Pago Fase 2 IMPLEMENTADA (OAuth + checkout split + webhook) — branch, não commitada** (2026-06-29). Com as **credenciais de TESTE em mãos** (fundadores forneceram; `.env.local`), implementada a **Fase 2 completa** na branch `feat/mercadopago-fase2-oauth-checkout`, **tudo atrás do gate `isMercadoPagoActive()`** (flag `mercadoPagoEnabled` + credenciais → flag OFF = fluxo atual intacto). Entregue: **SDK** `mercadopago@^3.1` + `lib/mercadopago.ts` (cliente lazy, OAuth `buildOAuthAuthorizeUrl`/`exchangeOAuthCode`/`refreshSellerToken`, gate); **schema aditivo** migrado no **shareo-dev** (`20260629000000_...`: `owner_payment_accounts.mp*` com tokens via `encryptPII`, `bookings.mp*`, tabela `mercado_pago_event_queue`); **OAuth onboarding** (`/api/payments/mp/connect` + `/api/mp/oauth/callback` + card "Conectar Mercado Pago" em `/perfil/recebimentos`, **update-only** pois PIX é NOT NULL); **checkout split** (`/api/payments/mp/checkout` com `marketplace_fee` 15%, guards CONFIRMED/dono/teto R$500 + refresh token) + `MpPayButton` com prioridade sobre PIX/Stripe na reserva; **webhook** (`/api/mp/webhook` 2 tempos idempotente + `lib/payments/mark-booking-paid.ts` espelhando os efeitos do Stripe — pickupToken/notificação/comissão embaixador). `tsc`+`next lint` limpos; smoke das 3 rotas OK. **✅ Validação local (opção a):** test users criados (vendedor/comprador sandbox MLB); **`Preference.create` com `marketplace_fee` ACEITO e ecoado pela API real do MP** (o split funciona); `init_point` gerado. **🧱 Parede do localhost (por design do MP):** o MP recusa `http://localhost` no OAuth authorize, nos `back_urls` (com `auto_return`) e no webhook (não alcança a máquina) → **o E2E interativo (connect→pagar→webhook→PAID) só roda em STAGING (HTTPS público)**. **🔜 Retomar:** commit+PR → credenciais de teste no staging (Vercel env + GitHub Secret `NEXT_PUBLIC_MP_PUBLIC_KEY_STAGING`) → fundador registra redirect/webhook `staging.shareo.com.br` no painel + confirma Marketplace → flag ON no staging → ciclo completo no browser. Nada em produção (gated **D4**). Ver [[project-mercadopago-migration]].

**💳 s40 — Mercado Pago: scaffolding inerte da Fase 1 + guia dos fundadores + cobrança refrescada** (2026-06-29). Preparada a **fundação do MP** (Modelo B/split, ADR-026) **sem tocar no fluxo de pagamento atual** (PIX manual / Stripe oculto): `lib/mercadopago.ts` (lê env + guards `isMercadoPagoConfigured`/`getMercadoPagoCredentials`, **sem SDK, sem checkout, não importado por nenhuma rota** → inerte), flag `getMercadoPagoConfig()` (`PlatformConfig.mercadoPagoEnabled` **default OFF**, mesmo padrão do aceite de contrato), `.env.example` documenta as 5 variáveis (Fase 1: `MP_CLIENT_ID`/`MP_CLIENT_SECRET`/`MP_ACCESS_TOKEN`; Fase 2: `NEXT_PUBLIC_MP_PUBLIC_KEY`/`MP_WEBHOOK_SECRET`) + URIs OAuth/webhook. `tsc`+`next lint` limpos (gotcha local: rodar `prisma generate` após os merges #117/#118 p/ o client reconhecer `contractVersion`/`accessLog`). **[PR #119](https://github.com/robertoepifanio-byte/SHAREO/pull/119)** — **mesclado** (CI verde: Build/E2E/Lint/Tests/Security/Preview). **Guia dos fundadores** [`juridico/mercadopago-procedimentos-fundadores.md`](juridico/mercadopago-procedimentos-fundadores.md) reescrito para **Modelo B decidido** + passo-a-passo oficial de criação da app (**Online Payments / não-ecommerce / Checkout Pro / Marketplace**) + checklist de credenciais (mapeado do doc do painel MP que o fundador forneceu; gotcha: criar app pode exigir **verificar identidade da PJ**). **Material de cobrança** [`juridico/d4-cobranca-juridico.md`](juridico/d4-cobranca-juridico.md) ([#115](https://github.com/robertoepifanio-byte/SHAREO/pull/115), **mesclado**) atualizado para mirar o **parecer FORMAL** (preliminar já recebido) + 7 questões. **🔜 Bloqueador da Fase 2:** fundadores criarem **conta PJ + app MP** e passarem **Client ID + Secret + Access Token de teste**. Nada em produção (gated D4). Ver [[project-mercadopago-migration]].

**🤖 s40 — Meta de conformidade: atividades zero-dependência ENTREGUES (4 frentes mescladas)** (2026-06-29). Enquanto o parecer FORMAL não chega, executadas **em staging** (atrás de flag/draft, **sem produção, sem pagamento real**) as atividades sem dependência da redação final — **4/4 entregues, deploys verdes, E2E verde**: 🔒 **auditoria técnica** ([`docs/auditorias/auditoria-conformidade-tecnica-s40.md`](auditorias/auditoria-conformidade-tecnica-s40.md)) — achou que o **Marco Civil art. 15 (logs 6 meses) está NÃO conforme** e que a **selfie de KYC pode ser dado biométrico** (LGPD art. 11), + 6 ressalvas LGPD (chave única AES/HMAC; exclusão vs. retenção fiscal; export art. 20 incompleto; etc.) + inventário de 19 categorias de dados; 📋 **rascunhos RIPD + transferência internacional** ([PR #116](https://github.com/robertoepifanio-byte/SHAREO/pull/116), **mesclado**); 🛠️ **aceite eletrônico do contrato de locação** (mecanismo versionado, flag `rentalContractAcceptanceEnabled` **default OFF** — com off não muda nada; reconciliou com o `ContractAcceptance` já existente) ([PR #117](https://github.com/robertoepifanio-byte/SHAREO/pull/117), **mesclado**, migração `add_contract_acceptance_version` aplicada limpa); 🔧 **retenção de logs (art. 15) + expurgos LGPD** (DevOps): tabela `access_logs` (sa-east-1) + `lib/access-log.ts` atrás da flag `accessLogsEnabled` **default OFF** (zero I/O com off) + 3 crons de expurgo (`purge-admin-logs`/`purge-consent-ips`/`purge-access-logs`) + `docs/juridico/retencao-logs-art15.md` (Opção I Log Drain × II tabela → recomenda II p/ H1) ([PR #118](https://github.com/robertoepifanio-byte/SHAREO/pull/118), **mesclado**, migração `add_access_logs` aplicada limpa). **4 questões para o jurídico/fundadores decidirem** antes de ativar (não bloqueiam o merge porque nada é ligado): prazos de expurgo (5a/180d), escopo do art. 15 (rotas autenticadas × públicas), trava de expurgo sob ordem judicial, e Opção I × II. Tudo **gated por D4**; nada em produção; ambas as flags OFF. Rastreado em [`docs/juridico/checklist-conformidade-juridica.md`](juridico/checklist-conformidade-juridica.md) (item 7 logs saiu de 🔴 → 🔨 scaffolding-pronto; itens 3/6 atualizados). Ver [[project-d4-juridico]].

**📜 s39 (cont.) — Parecer D4 PRELIMINAR recebido + Mercado Pago DECIDIDO** (2026-06-28). Chegou o **parecer técnico-jurídico** (PDF 41 págs, **construído sobre o nosso `juridico/briefing-juridico-d4.md`** — cita trechos literais do dossiê), **em revisão com a advogada** (ainda **NÃO é o parecer FORMAL**). Conclusão: **modelo "juridicamente viável"**; go-live depende de **5 ajustes críticos**. **🆕 Decisão dos fundadores: pagamentos via Mercado Pago (PSP), repasse semanal** → terceirizar o arranjo a PSP licenciado **afasta o maior risco** (Lei 12.865 — instituição de pagamento); conta de recebimento = **PJ da ShareO** (substitui a chave PIX pessoal temporária). **Modelo escolhido: B (split/marketplace)** — cada locador (inclusive PF) **conecta conta MP via OAuth** (onboarding novo); plano faseado em [`docs/backlog-atividades-priorizadas.md`](backlog-atividades-priorizadas.md) (fundação atrás de flag → checkout split+webhook → validar no sandbox → **só então** remover PIX-Raimundo + Stripe). **Bloqueador da Fase 1: credenciais de teste do app MP**, que os fundadores vão providenciar. **Achado:** não há "Stripe Connect" no código (é Stripe Checkout normal, oculto); o pagamento ativo é o PIX manual. Ver [[project-mercadopago-migration]] (agora **DECIDIDO**). **🆕 Marca "ShareO" já registrada no INPI** (sai das pendências). Posições do parecer: Fiscal = taxa 15% é receita (**NF** + ISS/PIS/COFINS), 85% não é receita; LGPD = minimização OK, mas **DPO + RIPD obrigatórios** + formalizar transferência internacional (art. 33); CDC = **responsabilidade SOLIDÁRIA** + **arrependimento art. 49 (7 dias antes da retirada)** + risco sem caução ao locatário; PLD = com PSP parte recai no PSP; Marco Civil = **guarda de logs 6 meses** (art. 15) + art. 19. Drafts de Termos/Política/**Contrato de Locação**/Seguro entregues como base para a advogada. Criado **`docs/juridico/checklist-conformidade-juridica.md`** (8 áreas, ✅/🟡/🔨/🔵) rastreando os ajustes. **🚦 D4 NÃO está fechado** — o próprio parecer condiciona go-live a *parecer FORMAL + contrato PSP assinado + conta PJ ativa + Termos/Política revisados publicados + checklist 100%*. **Nenhuma atividade de produção** (regra absoluta). Ver memória [[project-d4-juridico]].

**⚖️ s39 (cont.) — Briefing jurídico do D4 + skill `/shareo-juridico`** (2026-06-28). Criado o **dossiê standalone** [`docs/juridico/briefing-juridico-d4.md`](juridico/briefing-juridico-d4.md) (+ PDF `briefing-juridico-d4.pdf`, untracked como os outros PDFs) para entregar à consultoria: os **3 fluxos** (dinheiro/dados/locação, cada um com a pergunta central), o **mapa de competências jurídicas** (🔴 núcleo crítico / 🟡 fundacional / 🟢 complementar), **subprocessadores e transferência internacional** (Resend/Sentry/Mapbox/Vercel nos EUA; Supabase sa-east-1) e ficha técnica; cruza com `juridico/d4-cobranca-juridico.md` (mensagem pronta + 5 questões-núcleo). Criada a **skill `/shareo-juridico`** (`.claude/commands/shareo-juridico.md`) — Analista de Conformidade que mapeia as 10 áreas e classifica **OK/ATENÇÃO/CONSULTAR/N/A**, com trava de **nunca opinar como advogado**. Rodando-a sobre o D4 inteiro surgiram **2 questões fundacionais NOVAS** além das 5: **#6** contrato de locação locador↔locatário + risco de dano/perda **sem caução** (CC arts. 565+); **#7** responsabilidade do provedor por conteúdo de terceiros (**Marco Civil**, Lei 12.965 art. 19). Produção segue **gated por D4** (5 áreas 🔴 CONSULTAR: Pagamentos, PLD, Fiscal, Societário + faces de CDC/Civil). Ver memória [[project-d4-juridico]].

**🐛 s39 (cont.) — Falso bug de cadastro (URL errada) + correções de UX/resiliência** (2026-06-27). Tester (Raimundo) recebia **"Erro ao concluir o cadastro"** determinístico (celular + desktop). **Causa REAL: ele estava num deploy Vercel ANTIGO** (`*.vercel.app`, ambiente/banco próprios), **não bug de código** — em `staging.shareo.com.br` o fluxo sempre funcionou (reproduzi signup `201` + completar `200`; e o cadastro dele concluiu OK ao vivo, `raimundo1965@gmail.com`, signup→completar em 43s). Diagnóstico descartou metodicamente: **ENCRYPTION_KEY** (reproduzi completar 200 logando como admin via `/api/auth/csrf`+`callback/credentials`+PATCH), **DB/schema** (replicado o UPDATE em transação revertida), **rate-limit** (`checkRateLimit` é fail-open) — monitorando o banco do staging **ao vivo** (polling Prisma) e provando o runtime DB com um throwaway via HTTP. **✅ Deploys Vercel antigos despublicados (2026-06-28):** **Vercel Authentication ("Require Log In")** LIGADO no projeto `shareo` (Settings → Deployment Protection) → todo `*.vercel.app` (previews + aliases) exige login do time; só o domínio custom **`staging.shareo.com.br` fica público** (validado em aba anônima). Testers usam **só** ele. **PRs MESCLADOS (no ar no staging):** [#113](https://github.com/robertoepifanio-byte/SHAREO/pull/113) — 5 correções de UX/resiliência (helpers **Telefone**/**Endereço** mais claros; **endereço do anúncio travado ao perfil** também no editar, fonte única; **auto-reload em `ChunkLoadError`** no error boundary, com trava anti-loop; mensagem clara para **sessão obsoleta** NOT_FOUND/401); [#112](https://github.com/robertoepifanio-byte/SHAREO/pull/112) — docs do projeto Vercel **`shareo-prod`** separado no checklist de go-live (+ bug latente `VERCEL_PROJECT_ID_PROD` no `deploy.yml`); [#114](https://github.com/robertoepifanio-byte/SHAREO/pull/114) — health check pós-deploy → `staging.shareo.com.br` (domínio custom isento da proteção; sem isso o curl pegaria 401 e o deploy quebraria). Deploy verde com health check 200 no domínio canônico. **Gotcha p/ o cutover:** re-seed + auth **JWT-sem-adapter** deixa **sessões órfãs** (AUTH_SECRET não muda → token decodifica → user.id inexistente → 404 NOT_FOUND) → **rotacionar `AUTH_SECRET` ao re-seedar** (replicar no `shareo-prd`). Ver memória [[feedback-reseed-stale-jwt]].

**🏗️ s39 — Migração Supabase dev+staging → org corporativa (staging NO AR)** (2026-06-27). Dois projetos Supabase novos na org corporativa `Shareo Marketplace de aluguel` (slug `ohrwffrbcnccuflhmbpr`, FREE/NANO/sa-east-1): **dev `kehbrjlllfkooauaswtp`** (antigo `jtianehxosfdrhjzqvqj`), **staging `zythygwvmrwrqmnrdufq`** (antigo `fflpuoluiqmhpvcxubqi`). Por projeto: `migrate deploy` em banco vazio (36/36) → re-seed → buckets (`item-images`/`booking-photos`/`id-docs`); staging ganhou também a **base demo** (`seed-staging-demo.ts`: 10 PJ + 49 PF + 71 itens + 60 reservas + 25 reviews — PJ ficaram com cadastro PJ incompleto por lacuna pré-existente do script, que não envia os campos KYB `cpfResponsavel`/`responsavelLegal`/`declaracaoVinculoPJ`; **não é regressão da migração**). **Staging NO AR e validado**: health check `{db:ok, storage:ok, storage_private:ok}` + **E2E Regressão verde** contra a base populada. **🔑 Causa-raiz que travou o flip por horas:** o deploy de staging (`deploy.yml`) injeta de **GitHub Secrets `*_STAGING`** (não só do Vercel) — `NEXT_PUBLIC_SUPABASE_URL/_ANON_KEY_STAGING` são **inlinados no `vercel build`**, e `DATABASE_URL/DIRECT_URL_STAGING` dirigem os passos migrate/seed; as env vars do Vercel só valem para o **runtime**. Sintoma clássico: `db:ok` (runtime Vercel) + `storage:error` (URL inlinada do secret antigo). Fix: `gh secret set` nos 4 `*_STAGING` lendo de `.env.staging-new`. Outras lições: pooler host = `aws-1-sa-east-1` (pegar na aba ORM do Connect); senha com char reservado (`$)&?,]:`) → **URL-encode**; **NEXT_PUBLIC_* no Vercel nunca Sensitive** (some do build) — recriar sem o flag; Vercel CLI não acessa o time `shareo-marketplace` pela conta pessoal (usar `gh`/painel). **✅ Migração 100% concluída:** `.env` local flipado p/ dev novo; **Data API hardening** (`public` removido dos Exposed schemas nos 2 projetos, verificado HTTP 404 — obs: a publishable key `sb_` já não lia nada mesmo com 200); temporários limpos; docs commitadas (`5905ce3`). **Testes adicionais (validação exaustiva):** 20 owners de teste provisionados (`teste_pf_01..10` + `teste_pj_01..10@demo.shareo.com.br`, senha `Teste@2026`, PJ com KYB aprovado — via Prisma direto pois CNPJ fake não passa na Receita e a ENCRYPTION_KEY do staging não está local) — PF e PJ anunciam OK; **`daily-sim`** rodou 18 reservas no staging novo (14 ciclo completo anúncio→pagamento→retirada→devolução→repasse + cancel/disputa/pgto-pendente), tudo verde. **Lacuna pré-existente (não-regressão):** `seed-staging-demo.ts` não envia campos KYB PJ → os 10 PJ do demo ficam com cadastro incompleto (corrigido com os owners de teste). Produção segue gated por **D4**. Ver memória [[project-infra-ambientes]].

**🐛 s38 — Fixes importação Google Sheets + logo Pratika IA** (2026-06-25). **(1) RangeError parseCSVRow** (`57d3fa7`): `while (i <= line.length)` causava loop infinito ao fim de cada linha — quando `i === line.length`, `line[i]` é `undefined`, índice não avança, array cresce até >4,3 bilhões de entradas e V8 lança `RangeError: Invalid array length`. Fix: `while (i < line.length)`. Afetava CSV e Google Sheets. **(2) Google Sheets API v4** (`052641f`→`fdc5e38`): `/export?format=csv` e `/gviz/tq?tqx=out:csv` redirecionam para HTML em IPs de datacenter (Vercel) sem cookies de browser — mesmo com planilha "anyone with link". Solução definitiva: **Sheets API v4** (`sheets.googleapis.com/v4/spreadsheets/{id}/values/A:Z?key={API_KEY}`), que funciona para planilhas compartilhadas a partir de servidor. Variável `GOOGLE_SHEETS_API_KEY` adicionada no Vercel (Production + Preview, Sensitive — OK pois não é NEXT_PUBLIC_*). **GOTCHA:** commit com `[skip ci]` pula TODOS os workflows incluindo Deploy — redeploy de env var exige commit sem a flag. **(3) Instrução de compartilhamento** (`76f40dd`): cópia do Google Sheets herda permissões do original (privada). `ImportForm.tsx` reescrito em 4 passos explicitando que o usuário deve compartilhar **sua cópia**, não o modelo. **(4) Logo Pratika IA** (`7c10e09`): `public/logos/pratika-ia-sobre.png` substituído pela versão dinâmica. Seção "Template de Importação PJ" documentada no `CLAUDE.md`. Produção segue gated por **D4**. Ver memória [[project-import-fixes-s38]].

**📦 s37 — Importação PJ 3 formatos + "Anunciar outro item" + fix API import** (2026-06-24). **(1) "Anunciar outro item" + aba Importar oculta para PF** (`cd95d00`): QA levantou que a importação em lote não é prática para proprietários PF. Análise revelou que os únicos campos realmente repetíveis são categoria e condição (localização vem do perfil, preços auto-sugeridos). Solução mínima: `ItemForm.tsx` redireciona pós-create para `/itens/{id}?publicado=true`; `app/itens/[id]/page.tsx` detecta o flag e, quando o viewer é o dono, exibe banner verde "Anúncio publicado com sucesso!" com link "Anunciar outro item" → `/itens/novo?categoriaId=X&condicao=Y`; `app/itens/novo/page.tsx` lê os searchParams e repassa ao form como prefill. Simultaneamente, `app/meus-anuncios/page.tsx` oculta a aba "Importar" para `userType !== "PJ"` (query extra leve `prisma.user.findUnique`). **(2) Fix: API de import retorna IDs** (`5659c55`): antes a response retornava só `{created, updated, failed, errors}` — PJ não sabia quais itens foram criados para adicionar fotos. Agora retorna `items: [{id, title, action}]`; `ImportForm.tsx` lista cada item com botão "Adicionar fotos" → `/itens/[id]/editar` e banner orientando o próximo passo. **(3) Importação PJ: CSV + Excel + Google Sheets** (`3e9e2d6`+`7f16006`): três formatos convergem para o mesmo parser/validação/upsert. Excel via `xlsx@^0.18.5` (SheetJS, server-side only — zero impacto no bundle cliente): `XLSX.read(buffer, {type:"array"})` + `sheet_to_json({raw:false, defval:""})`. Google Sheets sem OAuth: extrai ID da URL → `/export?format=csv&gid=0` (planilha precisa ser pública "qualquer um com link"); valida content-type (HTML = erro amigável). **GOTCHA:** após `pnpm add xlsx`, commitar `package.json` E `pnpm-lock.yaml` juntos — só o lockfile gera `ERR_PNPM_OUTDATED_LOCKFILE` no CI. **(4) Template Google Sheets** (`161a207`): botão "Abrir modelo no Google Sheets" com URL `/copy` (ID `1NEd7Dn-zASPcNwuwMWHTErJWSVgKl3ByinRWfGIjcQI`) — Google pede ao PJ para copiar para o próprio Drive sem editar o original. **Fix de ESLint** (`d76458f`): react/no-unescaped-entities em aspas de instrução de compartilhamento — `&ldquo;...&rdquo;`. Todos os 6 commits em `main` (HEAD `161a207`). Produção segue gated por **D4**. Ver memória [[project-import-improvements-s37]].

**🔒 s36 — QA pré-lançamento + dedup/avatar mesclados + Dependabot security** (2026-06-24). Modelo alterado para **claude-sonnet-4.6**. **(1) QA pré-lançamento (gates automatizados):** bateria de verificação executada: `tsc`+`lint`+`jest` (480/480) + `build` + `pnpm audit --prod` (sem vulnerabilidades críticas/altas/médias nas deps diretas de produção) + `jscpd` (duplicação residual mapeada) + `curl` headers de segurança (`X-Powered-By` ausente, CSP presente). Achados registrados no backlog: **QA-DEP-01** (13 alertas de segurança em transitivas — Dependabot ativado), **QA-LINT-01** (no-unused-expressions em `_IdVerification.tsx` — **CORRIGIDO**), **QA-SEC-HDR-01** (`poweredByHeader:false` em `next.config.ts` — **CORRIGIDO**), **QA-DUP-01** (duplicação residual — backlog pós-D4). **(2) Campanha de deduplicação MESCLADA:** PR [#103](https://github.com/robertoepifanio-byte/SHAREO/pull/103) (squash `970c070`) — fundação compartilhada + adoção, incluindo QA-LINT-01 e QA-SEC-HDR-01. Staging + E2E regressão verdes. Ver [[project-dedup-campaign]]. **(3) Avatar unificado MESCLADO:** PR [#104](https://github.com/robertoepifanio-byte/SHAREO/pull/104) (squash `0ac91ec`) — `components/ui/Avatar.tsx` + `getCurrentUser()`. Rebaseado sobre #103, 4 conflitos resolvidos preservando as duas intenções. Staging + E2E verdes. Ver [[project-avatar-consolidacao]]. **(4) Dependabot security ativado (QA-DEP-01):** alerts e automated security fixes habilitados via API GitHub (`vulnerability-alerts` + `automated-security-fixes`). 13 alertas rastreados (transitivas via `@sentry/nextjs`, `@supabase/ssr`, `expo`). **GOTCHA:** tentar cobrir `/apps/mobile` no `dependabot.yml` gerou 5 PRs (#106–#110) com `ERR_PNPM_OUTDATED_LOCKFILE` (pnpm monorepo tem lockfile único na raiz; Dependabot bumpa `apps/mobile/package.json` sem sincronizar). PRs fechados + config revertida (PR [#111](https://github.com/robertoepifanio-byte/SHAREO/pull/111), `c2e6f6c`). Vulnerabilidades mobile cobertas pelos security-updates repo-wide. Nota no `dependabot.yml` documenta o gotcha. PRs de segurança automáticos aguardados (podem demorar — algumas transitivas exigem bump major do parent). Produção segue gated por **D4**.

**🔁 s35 — Avatar unificado + auditoria/campanha de deduplicação + landing "Em breve" + ponto de retorno v1.10.0** (2026-06-23). ~~Tudo LOCAL/não-mesclado~~ **MESCLADO em s36** (#103/#104). Landing e tag no ar desde s35. Produção do app segue gated por **D4**. **(1) Avatar unificado:** `components/ui/Avatar.tsx` (foto-ou-inicial) + `lib/currentUser.ts` (`getCurrentUser` com `cache()`, fonte única **lida do banco** — JWT sem PrismaAdapter nunca popula `session.user.image`); ~13 call sites; branch `refactor/avatar-consolidacao` (+ fix do `callbackUrl` em `reservas/[id]`). Supersede os fixes parciais já no ar #100/#101/#102. Ver [[project-avatar-consolidacao]]. **(2) Auditoria de deduplicação** (arquiteto+designer, read-only): catálogo de padrões repetidos (formatadores Intl inline, selects Prisma, guards admin/cron/owner, badges, EmptyState, etc.). **Lição:** auditorias passadas (s13/s14/s33) miraram segurança/QA e nunca DRY → toda auditoria geral deve ter eixo de fonte-única. Ver [[feedback-auditoria-duplicacao-dry]]. **(3) Campanha de deduplicação** (3 subagentes `fullstack-dev-shareo` em worktrees + integração própria): fundação compartilhada (`utils/format` presets, `lib/prisma/selects`, `components/ui/{StatusBadge,BookingStatusBadge,StatCard,EmptyState,RatingStars}`, `lib/auth/{require-admin,cron-guard,ownership}`) + adoção em ~30 arquivos; **tsc/lint limpos, jest 480/480, render no preview OK**. Adiado (projetos próprios): C3 (guard 401 ~100 rotas), C6 (boilerplate de fetch ~30 forms), C7 (`types/items.ts`), adoção de `requireAdminApi`. Branch `refactor/dedup` (base `main`, **SEM** avatar). **GOTCHA:** worktree de subagente brancheia de `main`, não do HEAD atual → recriaram a fundação → reconciliei mantendo a minha como canônica (`--ours` no cherry-pick). Ver [[project-dedup-campaign]]. **(4) Ponto de retorno [`v1.10.0`](https://github.com/robertoepifanio-byte/SHAREO/releases/tag/v1.10.0)** — tag + release no GitHub antes da campanha (rollback `git checkout v1.10.0`). **(5) Landing "Em breve" NO AR** em `shareo.com.br`/`www` via **encaminhamento GoDaddy (302) → Netlify Drop** (`public/em-breve/`, local; só o **banner** de marca após simplificação). **GOTCHA:** o "Add domain" do Netlify não gravava o domínio custom (falhou até em aba anônima, DNS 100% propagado) → por isso forwarding. E-mail Zoho + `staging`(Vercel `76.76.21.21`) intactos; NS=GoDaddy (nunca trocar). Ver [[project-landing-em-breve]]. **Próximo (amanhã):** decidir abrir PR de `refactor/dedup` e `refactor/avatar-consolidacao` p/ staging (um de cada vez — tocam arquivos em comum).

**🧪 s34 — 3 novas capacidades de teste (regressão visual + mobile Expo + carga k6)** (2026-06-22). Fechadas as 3 lacunas de teste que faltavam (antes inexistentes), **delegadas a subagentes**, sem novas funcionalidades e sem quebrar nada (cada PR com CI verde; produção segue gated por D4). **(1) Regressão visual** (PR [#97](https://github.com/robertoepifanio-byte/SHAREO/pull/97), `qa-shareo`): **Playwright nativo `toHaveScreenshot`** (NÃO Percy — sem SaaS/custo/egress, alinhado a LGPD). Config isolada `playwright.visual.config.ts` (desktop 1280 + mobile 375), `e2e/visual/visual-regression.spec.ts` (home/itens/login), 18 baselines `*-win32` commitadas, scripts `test:visual`/`test:visual:update`. Dinâmicos mascarados. **Isolada do CI**: baselines são por plataforma (win32≠Linux) e o CI roda lista curada de specs que não inclui `e2e/visual/` → não entra no gate (follow-up: baseline Linux via Docker). **(2) Mobile (Expo)** (PR [#98](https://github.com/robertoepifanio-byte/SHAREO/pull/98), `fullstack-dev-shareo` + finalização própria): `jest-expo` + `@testing-library/react-native` em `apps/mobile`, mocks (secure-store/router/reanimated), **12 testes** (`deriveBookingHistory` puro + `lib/api` com refresh/401), scripts `test`/`test:mobile`. **Gotchas:** não sobrescrever o `transformIgnorePatterns` do preset (quebrava o setup do RN); `jest.config.ts` da raiz passou a ignorar `<rootDir>/apps/` (senão o jest da web coletava os testes do mobile e quebrava os 480). **(3) Carga/stress (k6)** (PR [#99](https://github.com/robertoepifanio-byte/SHAREO/pull/99), `devops-shareo`): k6 (v2.0.0 já instalado) com `load/` (smoke/load/stress + `lib/common.js` + README), script `test:load`, `tsconfig` exclui `load/`. Mira só leitura (`/api/items`, busca, detalhe); **`BASE_URL` default localhost — NUNCA load/stress no staging** (rate limit/testers). Smoke real validado no localhost (1 VU, 100% checks, 0% falhas). **Verificação geral:** `type-check`+`lint` limpos; jest web **480/480** (sem coletar `apps/`); mobile 12/12; visual 18/18. Nenhuma das 3 entrou no gate de CI (`.github/workflows/` intocado) → rodam sob demanda. Ver memória [[project-test-infra-s34]].

**🧹 s34 — Lote de itens menores / housekeeping** (2026-06-22). Quatro pendências menores resolvidas numa branch (`chore/itens-menores-s34`), CI a validar. **(1) Header sem overflow horizontal em ~768px** (`components/layout/AppHeader.tsx`): logado, o header estourava **104px** na faixa md (saudação + tagline + nav + help/theme + cart/sino/avatar não cabiam em 768px). Fix: saudação "Olá, Nome!" e a tagline "Use Mais. Possua Menos." passam a aparecer só em **lg+**, margem da nav reduzida no md (`ml-2 lg:ml-6`), gap do cluster de ações `gap-1.5 lg:gap-2` — **overflow zerado em 375/768/1024/1280** (medido no preview), sem perder nenhum controle funcional. **(2) E-mails de reserva pluralizados p/ multi-item** (Story B): novo helper `bookingItemsLabel(titulo, totalItens)` em `lib/email.ts` ("X + mais N itens"); aplicado nos call sites (confirmação/cancelamento/devolução em `bookings/[id]`, lembretes start/return/overdue + taxa de atraso no `cron/reminders`, lembrete manual em `bookings/[id]/reminder`) lendo `bookingItems`/`_count` — antes citavam só o item principal. **(3) Cron de revalidação de PJs já verificadas (KYB H2)** (`app/api/cron/kyb/route.ts`): novo bloco que re-consulta na Receita as PJs verificadas há **+30 dias** (lote de 25, mais antigas primeiro, respeitando override admin) e, se a empresa encerrou/baixou, volta para a fila de revisão (`cnpjSituacaoVerificada=false`); ATIVA refresca o carimbo. Sem migração, piggyback no cron `/api/cron/kyb` diário já existente. **(4) `MEMORY.md` enxugado** (índice acima do limite de 24,4KB → reescrito como one-liners; detalhe segue nos arquivos-tópico). **Pulado de propósito:** generalizar a copy de `/bem-vindo` — é intencionalmente "de piloto" e correta no pré-lançamento (backlog marca como pós-lançamento). Verificação: tsc + lint limpos, jest **480/480** (excluindo cópias stale em `.claude/worktrees/`, ruído só local). Produção gated por **D4**.

**🎨 s34 — Imagem de marca dos fundadores no hero da home + OG/Twitter** (2026-06-22). Asset criado pelos fundadores (composição dos itens alugáveis — furadeira, câmera, caixa JBL, bike, escada, projetor — com a seta circular verde da economia circular) entrou em **dois lugares**, em duas variantes. **(1) OG/card de compartilhamento** (PR [#92](https://github.com/robertoepifanio-byte/SHAREO/pull/92), `40a80e6`): variante **com logo, fundo branco** → `public/logos/og-image-v5.webp` (1200×630, 61KB) ligada em `app/layout.tsx` (`openGraph.images` + `twitter.images`) — antes a home **não tinha imagem de preview** (`og-image-v4.webp` era órfão, removido); pipeline `scripts/generate-og-image.mjs`. **(2) Hero da home** (PR [#93](https://github.com/robertoepifanio-byte/SHAREO/pull/93), `681bdef`): variante **transparente, sem logo** → `public/logos/hero-items.webp`, em `app/page.tsx`; pipeline `scripts/generate-hero-image.mjs`. **Design final (após 5 iterações com fundador + `designer-shareo` + `product-owner-shareo`):** coluna **ÚNICA centralizada** (layout 2 colunas foi rejeitado), ordem H1 → imagem dos itens (w-230 mobile / w-420 desktop, `priority`) → linha curta **"Tudo perto de você."** → CTAs → busca → stats; imagem reduzida no mobile para não empurrar CTA+busca abaixo da dobra (validado: busca y≈564 < 667 do iPhone SE). **Badge dourado "Transforme itens parados em renda" REMOVIDO** a pedido dos fundadores. **(3) Fix de E2E** (PR [#94](https://github.com/robertoepifanio-byte/SHAREO/pull/94), `ec6ce1b`): remover o badge quebrou `e2e/e2e-home-plan.spec.ts` + `e2e/e2e-usability-plan.spec.ts` (asseriam `[role="note"][aria-label="Proposta de valor"]`) → passaram a asserir a imagem do hero (`getByRole('img', {name:/itens disponíveis para alugar no shareo/i})`). **Os 3 PRs mesclados na `main`, no ar e validados no staging** (HTTP 200; "Tudo perto de você" presente; `hero-items` presente; badge ausente; `og-image-v5` presente), **aprovados pelos fundadores**. Deploy Staging verde nos três; **E2E Regressão Contínua (Staging) verde no #94** (`ec6ce1b`), confirmando o fix do badge (a flakiness pré-existente dos 403 no cleanup não reapareceu nesta corrida). Produção segue gated por **D4**. Ver memória [[project-hero-og-imagem-marca]].

**💳 s34 — Checkout PIX manual da plataforma (validação staging)** (2026-06-22). Decisão dos fundadores: durante a validação em staging, o "PIX ShareO" que recebe as locações é a **chave PESSOAL TEMPORÁRIA do fundador** (Raimundo/Santander/`+5511946741234`) — ver bloco "Configuração TEMPORÁRIA" acima. **MESCLADO (PR [#84](https://github.com/robertoepifanio-byte/SHAREO/pull/84), `982d9f5`) + ATIVADO no staging.** Atrás de flag `platformPixEnabled` (**default OFF no código** → produção pós-D4 não usa por acidente; Stripe cartão segue intacto quando off). **Fluxo (admin confirma):** locatário em reserva CONFIRMED vê painel com a chave PIX (copia-e-cola) + "Já paguei" → `POST /api/bookings/[id]/declare-pix` grava `pixDeclaredAt` (não marca PAID) + notifica admins; admin em `/admin/reservas/[id]` clica "Confirmar recebimento do PIX" → `POST /api/admin/bookings/[id]/confirm-pix` marca PAID + grava split (espelha webhook Stripe) + comissão embaixador + notifica as partes. Coluna **aditiva** `Booking.pixDeclaredAt` (migração `20260622010000`) **aplicada à mão no staging + `migrate resolve`** (gotcha do `migrate deploy`). `tsc`+`lint`+Deploy+E2E Regressão verdes. ✅ **Walk-through manual VALIDADO em s38 (2026-06-25)** — reserva `cmqtwxdun0005tvyghov9jwto` (R$135): locatário→"Já paguei"→admin confirma→PAID (split R$20,25/R$114,75). **🚨 Trocar a chave pessoal pela conta oficial antes do go-live — é matéria do D4.** Ver memória [[project-pix-checkout-manual]].

**📋 s34 (continuação) — validações, auditoria sem dependência, avaliação MP, go-live e infra** (2026-06-22).
- **Validação manual do fluxo de devolução** (s33) confirmada no staging ponta-a-ponta (carimbos `returnRequestedAt` 21:56 → `returnedAt` 21:58 distintos = fluxo de duas etapas; o "—" inicial era cliente Prisma local desatualizado, confirmado por SQL cru). Ver [[project-devolucao-em-andamento]].
- **Auditoria s33 — itens SEM dependência implementados** (PR [#90](https://github.com/robertoepifanio-byte/SHAREO/pull/90), CI verde, **merge SEGURADO** junto do #64 até os testers fecharem): **SEC-ALTO-07** (token de verificação de e-mail agora gravado como **hash SHA-256** via `lib/crypto.hashToken`; sem migração) + **QA-CRIT-02** (teste de integração do `POST /api/bookings`, 13 casos). Restam só os COM dependência: perf (escala) e ARQ-CRIT-05 (decisão de produto). Ver [[project-auditoria-criteriosa]].
- **Mercado Pago × Stripe — EM AVALIAÇÃO** (decisão dos fundadores; nenhum código alterado): análise técnica + `docs/juridico/mercadopago-procedimentos-fundadores.md`. Modelo A (gateway, ~3-5d, recomendado começar) × B (marketplace/split OAuth, pós-D4). Ver [[project-mercadopago-migration]].
- **Checklist de go-live** consolidado em `docs/checklist-go-live.md` (licenças/infra/segredos/pagamentos/segurança). Licenças OK exceto **mapbox-gl v3** (comercial) e **next-auth beta** (→GA). Ver [[project-go-live-checklist]].
- **Infra:** **Vercel Pro PAGO/ATIVO** (não é mais Trial). **Supabase organizado em 3 ambientes** — `shareo-dev` (`jtianehxosfdrhjzqvqj`, `.env`), `shareo-staging` (`fflpuoluiqmhpvcxubqi`, `.env.local`/`.env.staging-*`), prod **não existe** (pós-D4, org corporativa). **Ambiente local ISOLADO no shareo-dev** (corrigido split em que o cliente Supabase apontava p/ staging; `.env` ganhou as chaves novas `sb_*`, `.env.local` limpo, buckets+schema já existem no dev, service_role rotacionada). **Sentry:** Free atende o lançamento (projeto `shareo-web`/org `shareo-ow`, sampling enxuto); Team só se >1 assento/volume. Ver [[project-infra-ambientes]].

**🔍 s33 — Auditoria criteriosa de TODO o projeto (Seg+Arq+QA) REMEDIADA + banco de produção VALIDADO** (2026-06-21). Auditoria delegada a 3 especialistas em paralelo (`seguranca-shareo`/`arquiteto-shareo`/`qa-shareo`). **Todos os criticals (🔴) e altos acionáveis (🟠) corrigidos e no ar (deploy + E2E Regressão Staging verdes em cada PR).** **(1) Criticals do carrinho** ([#74](https://github.com/robertoepifanio-byte/SHAREO/pull/74)): `confirm` revalida TODOS os itens (anti double-booking de secundário), `POST /bookings` `Serializable`+`P2034`, `findOverlappingItem` filtra `deletedAt`, **teto R$500 validado no POST** (não só no checkout). **(2) Criticals de segurança** ([#75](https://github.com/robertoepifanio-byte/SHAREO/pull/75)): removido o **bypass de `Bearer CRON_SECRET` em `/api/admin/*`** no middleware (era escalada total ao painel sem sessão); `ambassador-decay` via `Bearer` (não query); **kill-switch `E2E_BYPASS_DISABLED`** no bypass de rate limit (prod desliga; staging inalterado); chave **PIX não vai mais aos logs**; rate limit no refresh mobile. **(3) Altos SEC+ARQ** ([#76](https://github.com/robertoepifanio-byte/SHAREO/pull/76)): `id-verification` exige adminRole; `extend` filtra item deletado; rate limit em `POST /bookings` e `/founders/leads`; `photoUrl` de disputa/avaliação só do nosso Storage; extensão de upload pelo MIME; trilha LGPD `KYB_QUEUE_VIEWED`; teto de 500 no filtro de distância; loops de `pickupToken` com teto. **(4) Perf** ([#77](https://github.com/robertoepifanio-byte/SHAREO/pull/77)): ARQ-ALTO-10 — a listagem `/itens` **não exibe nota no card**, então as reviews viraram **lazy-load** (só com `minRating`); denormalizar seria over-engineering. **(5) ✅ ARQ-ALTO-15 — banco de produção VALIDADO** (maior risco de go-live do Arquiteto): `migrate diff` replayando as 34 migrations num banco vazio = **"No difference detected"** + `migrate deploy` em vazio = 34/34 sem erro → **a produção pós-D4 sobe íntegra**. Runbook: criar a prod SEMPRE via `migrate deploy` em banco VAZIO, **nunca clonar staging** (registrado em [[project-d4-juridico]]). **Adiados (NÃO bloqueantes):** perf medição-dependente (índices admin/dashboard/pub-sub — Arquiteto marcou adiável, scale atual não justifica), SEC-ALTO-07, **ARQ-CRIT-05** (avaliação por item — decisão de produto), **QA-CRIT-02** (teste de integração do `POST /api/bookings`), ~30 médios/baixos. Produção segue gated por **D4**. Ver memória [[project-auditoria-criteriosa]].

**🛒 s32 — Carrinho de locação multi-item do mesmo dono (Story B / ADR-025) + lote de UX p/ tester** (2026-06-21). **(1) Lote de UX (4 PRs mesclados + no ar no staging):** [#65](https://github.com/robertoepifanio-byte/SHAREO/pull/65) onboarding — signup passa a cair em `/bem-vindo` (escolha "Explorar" × "Completar cadastro") + aviso "Complete seu cadastro" no dashboard; [#66](https://github.com/robertoepifanio-byte/SHAREO/pull/66) `/bem-vindo` — removido selo de piloto e "Completar cadastro" destacado (botão preenchido); [#67](https://github.com/robertoepifanio-byte/SHAREO/pull/67) **descoberta da importação PJ** — não havia link p/ "Meus Anúncios" no menu do avatar nem no mobile (a aba "Importar" vive lá) → adicionado "📦 Meus Anúncios" nos dois menus; e o `public/template-importacao.csv` tinha categoria inexistente "Casa e Jardim" (slug `casa-jardim`=**"Eletrodomésticos"**) + Natal/RN → corrigido; [#68](https://github.com/robertoepifanio-byte/SHAREO/pull/68) copy do hero `/sobre` + "renda" no lugar de "lucro". Raimundo (raimundo1965@gmail.com) **excluído** da base de staging p/ refazer cadastro CPF→CNPJ do zero. **(2) Pergunta de tester → consulta a especialistas:** "carrinho p/ alugar vários itens, pagar o total e gerar locações por proprietário". Arquiteto + PO convergiram: **construir só o caso MESMO DONO** (Story B, ~80% do valor / ~20% do esforço, não mexe em split/pagamento); **carrinho MULTI-DONO** (pagamento agregado + split por dono) fica **H3/pós-D4** (registrado p/ depois). **(3) Implementação Story B — MESCLADA + NO AR no staging** (PR [#69](https://github.com/robertoepifanio-byte/SHAREO/pull/69) squash, Fases 1–6): Fase 1 modelo `BookingItem` (booking_items) + migração aditiva idempotente com **backfill** (53→53); Fase 2 disponibilidade lê de booking_items (`lib/booking-availability.findOverlappingItem`) nos 3 pontos (criação/confirm/`/availability`); Fase 3 `POST /api/bookings` aceita `additionalItemIds` (mesmo dono/mesmo período, retrocompatível, valida `MIXED_OWNERS`); Fase 4 **carrinho** client-side (localStorage, `lib/cart.ts`, escopo de 1 dono) — `AddToRentalButton` na página do item, `CartIndicator` no header, página `/carrinho` (período escolhido lá, 1 POST com itemId+additionalItemIds); Fase 5 exibição (lista "Itens desta locação" em `/reservas/[id]` + badge "+N itens" na lista); Fase 6 E2E `e2e/multi-item-booking.spec.ts` (#32–#35, escrito pelo qa-shareo). **(3b) Descoberta** (PR [#70](https://github.com/robertoepifanio-byte/SHAREO/pull/70)): quadro **"Ou itens do mesmo anunciante"** na página do item, acima de "Você também pode gostar" (até 4 itens AVAILABLE do mesmo dono; similares passam a excluir os do mesmo dono). **Migração no staging:** aplicada **À MÃO antes do merge** (lição KYB) — `db execute --url $DIRECT_URL` + `migrate resolve --applied` → "schema up to date" → sem 500 de runtime. **Verificação:** tsc + jest 466 + smoke real (2 BookingItems, soma, item secundário bloqueia quando CONFIRMED) + **E2E 4/4 verde local** (sem ajuste de código de app) + **Deploy Staging + E2E Regressão Contínua (Staging) verdes** + smoke ao vivo (`/carrinho`, "Adicionar a uma locação", register 400 sem 500, quadro do mesmo anunciante 4 cards). **Decisões de escopo:** mesmo dono + mesmo período; 1 avaliação por reserva; teto R$500 sobre o total da locação (no checkout). Produção gated por **D4**. **(3c) Validação do fundador + ajustes (2026-06-21):** título do quadro passou de "Ou itens do mesmo anunciante" → **"Itens do mesmo anunciante"** (PR [#71](https://github.com/robertoepifanio-byte/SHAREO/pull/71), removido o "Ou"). **Gotcha de dados:** o quadro só renderiza quando o anunciante tem **2+ itens AVAILABLE**; a base demo tinha a maioria dos anunciantes com 1 item → o fundador navegava e "não achava" o quadro (era DADO, não bug). Corrigido com **`scripts/topup-demo-owners.ts`** (PR [#72](https://github.com/robertoepifanio-byte/SHAREO/pull/72), dry-run/`--confirm`, aditivo, **só contas de teste** `@demo.shareo.com.br`/`@daily-sim.shareo.test`, NUNCA humanos/admins): cria itens clonando conteúdo real co-localizado → staging foi de **23→42 de 44** anunciantes com 2+ itens (87→132 AVAILABLE). **Carrinho validado pelo fundador ao vivo no staging.** **Registrado p/ depois:** carrinho multi-dono (H3); pluralizar e-mails de reserva p/ multi-item. Ver memória [[project-multi-item-booking]].

**🌓 s31 — Modo Escuro IMPLEMENTADO e no ar no staging** (2026-06-20). Saiu do plano (s30) para produção-no-staging em 6 entregas, cada uma verificada no preview e mesclada na `main`. **Fase 1 — fundação de tokens** (PR [#52](https://github.com/robertoepifanio-byte/SHAREO/pull/52)): `globals.css` com CSS vars em canais RGB em `:root`+`.dark` (valores do claro idênticos) + `tailwind.config.ts` apontando p/ `rgb(var(--x)/<alpha-value>)` + `darkMode:'class'`; refactor puro, regressão zero no claro. **Fase 2 — provider+toggle+forms** (PR #53→reaberto como [#59](https://github.com/robertoepifanio-byte/SHAREO/pull/59), integrado via #60): `next-themes`+`ThemeProvider` com **nonce CSP** (x-nonce→Providers→ThemeProvider) + `suppressHydrationWarning` no `<html>`; `ThemeToggle` tri-state (Claro/Sistema/Escuro) no header e no MobileMenu; `@tailwindcss/forms`→`{strategy:'class'}` após auditoria de inputs. **Fase 3 — migração de cor fixa** (PR [#54](https://github.com/robertoepifanio-byte/SHAREO/pull/54)): ~78 trocas hardcoded→token em 36 arquivos, decidindo **por ocorrência** (superfície que adapta × chrome colorido onde `text-white` permanece). **Fase 4 — componentes especiais** (PR [#55](https://github.com/robertoepifanio-byte/SHAREO/pull/55)): Mapbox `dark-v11` via `useTheme`, `ThemedToaster` (Sonner segue o tema). **Fase 5 — a11y de contraste** (PR [#56](https://github.com/robertoepifanio-byte/SHAREO/pull/56)): marca usada como TEXTO clareada no dark via override `.dark .text-{primary,brand,blue-medium}` (#4A90D9/#5BD08B/#5BA3E0) + `bg-blue-medium` volta a fill navy (corrige cards `CasosRenda`). **Elevação** (PR [#57](https://github.com/robertoepifanio-byte/SHAREO/pull/57)): sombras theme-aware (`--shadow-rgb` navy→preto). **Teste a11y dark** (PR [#58](https://github.com/robertoepifanio-byte/SHAREO/pull/58)): `e2e/e2e-a11y-plan.spec.ts` roda axe (contraste+ARIA) emulando `prefers-color-scheme: dark`. **Gotcha do merge:** squashar a Fase 1 em `main` divergiu da Fase 1 original das branches → a pilha stacked passou a conflitar e `gh pr merge --delete-branch` fecha o PR filho; resolvido rebaseando o topo sobre `main` (`git rebase --onto main <fase1>`) e integrando via **PR [#60](https://github.com/robertoepifanio-byte/SHAREO/pull/60)** (árvore idêntica). **Verificação:** auditoria de contraste programática + axe = **0 violações nos dois temas**; Lighthouse staging (mobile) **A11y 100 / BP 96 / Perf 86 (CLS 0)**; **E2E de regressão pós-deploy VERDE** (inclui o teste dark) contra o staging; toggle tri-state + next-themes confirmados ao vivo. Docs: spec `docs/planos/dark-mode-plan.md` v0.3 (PR [#51](https://github.com/robertoepifanio-byte/SHAREO/pull/51), checklist §9 fechado) + roteiro de teste de usabilidade `docs/guias/dark-mode-teste-usabilidade.md` (PR [#61](https://github.com/robertoepifanio-byte/SHAREO/pull/61)). **Pendências (não-bloqueantes):** execução do teste de usabilidade pelos fundadores (roteiro pronto) e, opcional, investigar o Perf do Lighthouse (TBT — **não atribuível ao dark**, bundle idêntico entre temas). Produção segue gated por **D4**. Ver memória [[project-dark-mode-plan]].

**📋 s30 — Plano e especificação do Modo Escuro (dark mode)** (2026-06-19). **Planejamento — nada implementado na app.** Entregue `docs/planos/dark-mode-plan.md` (spec visual+técnica, **v0.2** após revisão dos papéis `designer-shareo` e `fullstack-dev-shareo`) + `docs/dark-mode-prototype.html` (protótipo standalone com toggle Claro/Escuro/Sistema, validado por screenshots). **PR [#51](https://github.com/robertoepifanio-byte/SHAREO/pull/51)** aberto com a spec (protótipo ainda não commitado). **Diagnóstico-chave:** o ShareO **não tem dark mode** e usa **hex hardcoded** no `tailwind.config.ts` (não CSS vars) = bloqueador estrutural; o `globals.css` tem só algumas vars desconectadas e divergentes. Caminho: mover tokens p/ CSS vars (`:root`+`.dark`, canais RGB), Tailwind aponta p/ `rgb(var(--x)/<alpha-value>)`, `darkMode:'class'`, `next-themes` (no-FOUC). **Achados técnicos da revisão:** ~40 CSS vars (tokens são objetos aninhados); **`@tailwindcss/forms` precisa de `{strategy:'class'}`** senão inputs ficam brancos no dark; **CSP já tem infra de nonce** (`generateNonce`/`x-nonce` no middleware, layout já passa p/ GA) → fix = `<ThemeProvider nonce={nonce}>`; `suppressHydrationWarning` falta no `<html>`; Mapbox `ItemsMap.tsx:94` hardcoded→`useTheme`; Sonner no layout é Server Component (mover p/ client leaf); **dívida de cor fixa medida = ~383 ocorrências em 45+ arquivos** (top: AppFooter/ListaVIP/FounderCaptureForm/MobileMenu). **Paleta dark com contraste WCAG computado:** bg `#0B1524`, surface `#15233B`, foreground `#E8EEF6` (15.7:1), verde-texto `#5BD08B` (9.45:1, ≠ verde-fill `#007B3C`), status confirmed `#5BA3E0`/returned `#B49AF5`/disputed `#F0A35E`, primary fill `#1E4D80`/texto `#4A90D9`. Plano em **6 fases** (Fase 1 = fundação de tokens, refactor puro sem mudança no claro) + checklist a11y/usabilidade. Produção gated por D4. Ver memória [[project-dark-mode-plan]].

**✅ s29 — Release v1.8.0: campanha (UTM + landings) + migração Vercel Pro corporativa + caixas humanas Zoho** (2026-06-19). **(1) Campanha de pré-lançamento — habilitadores técnicos** (PR [#50](https://github.com/robertoepifanio-byte/SHAREO/pull/50), squash `33601cb`), **aprovada pelos fundadores**. **Fase 1 (UTM/atribuição):** `FounderLead` ganha `utmSource/utmMedium/utmCampaign/referrerUrl` (migration `20260618000000_add_founder_lead_utm`, idempotente, aplicada em local + staging via `db execute --url`); `POST /api/founders/leads` passa a **honrar o `source`** validado pelo schema (antes gravava `VIP_LANDING` fixo) + persiste UTM; `FounderCaptureForm` lê `utm_*`/`ref` da URL e **deriva o canal** (`meta/fb/ig`→META_ADS, `google`→GOOGLE_ADS, `ref`→REFERRAL); painel `/admin/fundadores` ganha recorte "Por canal" e "Por campanha (UTM)". **Fase 3 (landings):** `lib/pilot-cities.ts` (14 cidades) + `app/pilotos/[cidade]/page.tsx` (ISR, metadata localizado, contador por cidade, form pré-preenchido com `campaign=piloto-<slug>`) + rotas no `sitemap.xml`. **GA4 (Fase 2) adiado** (sem Measurement ID; medição inicial pelo painel). Verificado: tsc+lint limpos, E2E local do source=META_ADS persistindo, landings ao vivo no staging (`/pilotos/recife`=200, cidade inválida=404). **(2) Migração do Vercel para conta corporativa** — projeto `shareo` transferido da conta pessoal `robertoepifanio-byte` (Hobby) para o **Team `shareo-marketplace` (Pro)**; bloqueio do transfer era um **deploy órfão preso em INITIALIZING há 22 dias** (`dpl_9SvjfDT…`) → removido via `vercel remove` → transfer concluído. GitHub Secrets atualizados (`VERCEL_ORG_ID`→`team_AH51I8nuVC1fmmmorLA9Qefs`, `VERCEL_TOKEN`→token corporativo); conta pessoal **removida do Team** (Leave Team); Deploy Staging revalidado **verde sob o Team novo**, sem dependência da conta pessoal; domínios (`staging.shareo.com.br` + `shareo-rouge.vercel.app`) e env vars acompanharam o projeto; token exposto no chat **revogado**. Esteira deploya por **token** (não pela integração Git). Ver memória [[project-vercel-pro-migracao]]. **(3) Caixas humanas no Zoho Mail Lite** — `atendimento@shareo.com.br` (1 usuário) + aliases `suporte@`/`privacidade@`/`seguranca@`/`noreply@`; DNS na GoDaddy (MX Zoho + SPF + DKIM `zmail`) validado **mail-tester 10/10**; **Resend segue como provedor de envio do app** (`noreply@`, `lib/email.ts`) — papéis complementares, sem conflito de DNS. Ver memória [[project-email-provider-strategy]]. **Release [`v1.8.0`](https://github.com/robertoepifanio-byte/SHAREO/releases/tag/v1.8.0)** (commit `534d1db`, tag + GitHub Release). Segue gated por D4 para produção.

**✅ s28 — Merge de todos os PRs abertos + fix durável do deploy** (2026-06-18). Limpou-se a fila de PRs (de 9 abertos para 0). **(1) Auditoria s14** — #46→#47→#48 mesclados na ordem (squash), CI verde em cada um (esperado inclusive o E2E Playwright). **(2) Email cleanup** [#16](https://github.com/robertoepifanio-byte/SHAREO/pull/16) (`chore/email-cleanup-dead-code`, s17): estava `CONFLICTING`+CI vermelho; **rebaseado na main**, conflito do mock em `register.test.ts` resolvido (mantido só `sendVerificationEmail`), removidos 3 arquivos locais que entraram por engano no commit (`Sem título.md` + guias do robô, intencionalmente não-versionados), validado tsc+jest 9/9+lint → mesclado. **(3) Dependabot (5 PRs)** [#8](https://github.com/robertoepifanio-byte/SHAREO/pull/8)/[#9](https://github.com/robertoepifanio-byte/SHAREO/pull/9) (typescript-eslint dev), [#10](https://github.com/robertoepifanio-byte/SHAREO/pull/10) (sharp), [#11](https://github.com/robertoepifanio-byte/SHAREO/pull/11) (supabase-js), [#12](https://github.com/robertoepifanio-byte/SHAREO/pull/12) (lucide-react): rebaseados via `@dependabot rebase` e mesclados **um a um** (cada merge re-conflita o `pnpm-lock.yaml` dos demais). **Insight:** PRs do Dependabot rodam **sem os secrets do repo** → o job `Build` do CI falha neles (`Invalid URL` em `/_not-found`) mesmo com bump seguro; o `Can't resolve 'dns'` de `ssrfGuard` é só warning (aparece no build verde da main). `Build` **não é check obrigatório** (estado `UNSTABLE`) → mesclar quando Tests+Lint(typecheck) passam (typecheck valida API/ícones usados); validação real do runtime = CI da main pós-merge (com secrets), que ficou **100% verde** (Build/Tests/Lint/E2E). **(4) Fix de deploy** [#49](https://github.com/robertoepifanio-byte/SHAREO/pull/49) (`133bfe8`): os 9 merges do dia estouraram o `api-upload-free` do Vercel free no Deploy Staging; adicionado **`--archive=tgz`** aos 3 comandos `vercel deploy` (preview/staging/prod) → 1 tarball por deploy em vez de milhares de arquivos. **Comprovado funcionando mesmo com a cota já estourada:** Preview Deploy do próprio #49 verde + Deploy Staging pós-merge subiu + E2E de Regressão de Staging verde. Limite eliminado de forma durável. Ver memórias [[project-auditoria-s14-execucao]] e [[project-vercel-free-upload-limit]].

**✅ s27 — Auditoria s14 executada (achados sem dependência externa) em 3 PRs** (2026-06-18). Tratados todos os achados da auditoria s14 que não dependem de jurídico/aprovação de fundadores/rotação de secret/upgrade de deps — corrigidos, já-resolvidos ou pulados com justificativa. **3 PRs ABERTOS p/ `main` (não mergeados; mesclar na ordem #46→#47→#48, sem conflitos).** **PR [#46](https://github.com/robertoepifanio-byte/SHAREO/pull/46)** (`914eda2`): **SEC-MAJ-06** (LGPD art.18) — DELETE `/api/users/me` agora anonimiza em `$transaction` `Review.comment`, `Message.content`(+soft-delete), `borrowerNote`/`ownerNote`, chave PIX (`OwnerPaymentAccount`) e docs de identidade (`idDocumentUrl`/`idSelfieUrl`/`idRejectionReason`+status→UNVERIFIED) + limpeza best-effort do bucket privado `id-docs` via `after()` (**insumo direto do D4**); **S14-MIN-07** (`/api/health` só expõe `e.message` fora de produção); **Mi17** (remove `icones/` órfão do matcher); **Mi13** (deleta `hooks/useAuth.ts` morto); **GAP-M-07b** (`take:24` nas imagens da página SSR `/itens/[id]`). **PR [#47](https://github.com/robertoepifanio-byte/SHAREO/pull/47)** (`06fe171`): **S14-M-11** (PlatformConfig lê de um load cacheado em memória, TTL 60s + `clearPlatformConfigCache()` na escrita admin — substitui ~59 `prisma.find`/request; caveat: cache por instância serverless, propagação até 60s entre instâncias); **S14-M-18** (`/api/admin/geocode-items` filtra no banco `latitude:0 OR longitude:0` — sentinela real igual ao create — em lotes de 100 c/ `hasMore`, em vez de carregar todos os itens; corrige o `==null` latente, pois Item.lat/lng são `Float` não-nulos). **PR [#48](https://github.com/robertoepifanio-byte/SHAREO/pull/48)** (`332f582`): **S14-M-17** (16 specs ganham fallback `?? process.env.STAGING_URL ??` antes do localhost — raiz das falsas-falhas do s13); **S14-M-12 (fatia segura)** (rota `reminder` ganha `code` nos 4 envelopes que só tinham `message` — aditivo, não quebra UI). **Falsos positivos / já-resolvidos (sem ação):** **Mi14** (troca de role já invalida sessão), **S14-M-16** (`pickupToken @unique` já no schema + migration `20260609000001`), **S14-M-15** ("3 fotos" é dica, não limite), **4 spec-bugs** (já corrigidos no s17 — locators do auth, pickupToken no security2 #21, 400 aceito no email-verification #4). **Pulados c/ justificativa:** **S14-M-13** (helper de owner-guard — 7 sites com intenções divergentes: bypass admin/NOT_FOUND-IDOR/msgs custom; abstrair authz piora clareza logo antes do go-live), **S14-M-12 massa** (trocar `{error:"str"}`→objeto em ~19 rotas arrisca quebrar UI/asserts E2E, não verificável por unit), **S14-MIN-11** (`consentIp` é campo de auditoria de consentimento por ADR-021/022, já fora de Sentry/respostas), **S14-MIN-12** (rotação refresh mobile exige migration + auth mobile não testada). **Verificação:** `tsc`+`next lint`+`jest` (450 passed, 24 suites) verdes nos 3 PRs; E2E não roda localmente (precisa staging+secrets), mudanças de spec são troca de string verificada por tsc. **Auditoria s14 ENCERRADA** quanto a itens sem dependência externa. Ver memória [[project-auditoria-s14-execucao]].

**✅ s26 — Base de validação demo no staging + busca de usuários no admin + lote de UX/copy** (2026-06-18). **(1) Reset + repovoamento da base de staging para validação dos fundadores** (PR [#43](https://github.com/robertoepifanio-byte/SHAREO/pull/43), `08873d6`): **hard delete** de ~4.300 registros preservando **8 contas** (5 humanas — roberto.epifanio@gmail.com, roberto.epifanio1961@outlook.com, raimundo1965@gmail.com, testethiago@gmail.com, thiagogarbuio10@gmail.com — + 3 admins @shareo.com.br) e repovoamento com **10 PJ + 50 PF** (`@demo.shareo.com.br`), **80 itens AVAILABLE todos com 3 fotos Unsplash curadas** que casam com o produto, **84 reservas cobrindo os 7 estados**, ~39 payouts PENDING (D4), ~117 reviews; nenhuma reserva > R$500. Scripts novos em `scripts/` (fora do build): `staging-cleanup.ts` (hard delete com keep-list, dry-run sem `--confirm`, ordem FK-safe), `seed-staging-demo.ts`, `polish-staging-demo.ts`, `topup-staging-demo.ts`, `fix-incomplete-pf.ts`, `lib/sim-shared.ts` (helpers extraídos do daily-sim + `generateValidCNPJ`), `lib/demo-catalog.ts` (30 produtos + 12 capitais). **⚠️ Retenção: NÃO rodar `--reset`** (dados retidos p/ validação; demo usa `@demo.shareo.com.br`, não `@daily-sim.shareo.test`). Gotchas: **E2E_SECRET precisa existir no ambiente Preview do Vercel** (não só Production); dedup por título normalizado colapsa donos distintos → usar título único; review é `POST /api/bookings/[id]/reviews`. `daily-sim --env staging` (Dia 12) rodado depois — +5 usuários/itens/reservas `@daily-sim.shareo.test`, **mantidos** por decisão do usuário. **(2) Busca de usuários no admin** (PR [#41](https://github.com/robertoepifanio-byte/SHAREO/pull/41), `6ce5792`): `/admin/usuarios` ganhou busca server-side por nome/e-mail (`?q=`, contains case-insensitive direto no banco), contador adaptado e estado vazio; mantém Server Component. **(3) Termos — taxa/repasse/teto explícitos** (PR [#42](https://github.com/robertoepifanio-byte/SHAREO/pull/42), `521f8d2`): seção de Pagamentos explicita taxa 15% (via `getPlatformFeeRate`), repasse semanal (segundas) e teto R$500 — fecha o gap #4 da pauta D4. **(4) Lote de UX/copy** (PR [#44](https://github.com/robertoepifanio-byte/SHAREO/pull/44), `8db7639`): copy de repasse semanal alinhada em recebimentos/repasses/home; **ícone do cadeado ShareO** no pilar "Pagamento protegido" (`public/icons/cadeado-shareo.png`); nova **Missão e História** na página `/sobre` (com parágrafo de visão de futuro); **marcador do mapa com o pin ShareO** (`public/icons/pin-shareo.png`, anchor bottom) em `ItemsMap`; dica de ajuda "Combine retirada e devolução" sem menção a repasse na confirmação; **Programa Embaixadores** mostra os 3 tiers (Bronze/Prata/Ouro) e deriva o nível atual de `activeReferrals` (corrige "Bronze" indevido com 0 indicados) + gramática "Falta 1 indicado ativo"; **busca da Central de Ajuda movida para o hero** (Context: `HelpSearchProvider`/`HelpSearchInput`/`HelpResults`). **(5) Fix da busca da ajuda** (PR [#45](https://github.com/robertoepifanio-byte/SHAREO/pull/45), `d4e4117`): o auto-scroll disparava no 1º caractere e atrapalhava — agora digita sem rolar, contagem clicável "Ver N resultados" ao vivo, rolagem só no clique/Enter. **Todos os PRs com CI verde + deploy de staging confirmado.** Ver memória [[project-staging-demo-base]].

**⏳ s25 — Plano de campanha de pré-lançamento nacional (captação de leads → cidades-piloto)** (2026-06-17). Elaborado um **plano de marketing + habilitadores técnicos** para captar leads (nome/e-mail/cidade/UF/intenção) em todo o Brasil e, por dados, **qualificar 3–5 cidades-piloto** para a entrada em produção (pós-D4). **Status: AGUARDANDO APROVAÇÃO DOS FUNDADORES** — nada implementado ainda. Decisões de direção já tomadas pelo usuário: **mídia de verba baixa (~R$1–3k)** (orgânico + impulsionamentos geo-segmentados cirúrgicos), **escopo completo** (marketing + técnico), critério de piloto por **densidade + equilíbrio** (≥80 leads na cidade E cada lado proprietário/locatário ≥30% do total). **Diagnóstico-chave:** a captura já está 100% pronta (form VIP, `FounderLead` com city/state/intent, painel `/admin/fundadores` por cidade, convite em massa, indicação `?ref=`); as lacunas que impedem **medir/otimizar** a campanha são (1) **atribuição de canal/UTM** — hoje `app/api/founders/leads/route.ts:68` grava `source:"VIP_LANDING"` fixo, **ignorando** o `source`/UTM que o schema valida e o form envia; (2) **GA4** (ausente, CSP já permite); (3) **landing pages `/pilotos/[cidade]`** (ISR) como destino dos anúncios + isca de SEO. Plano completo salvo em `~/.claude/plans/acione-o-agente-ou-ticklish-quail.md`. Não há agente "marketing" dedicado — execução acionaria `seo-shareo`/`fullstack-dev-shareo`/`product-owner-shareo`. Ver memória [[project-campanha-prelancamento]].

**✅ s24 — Pré-lançamento de pilotos + ajustes do tester na precificação + UX/copy** (2026-06-17). **(1) Fluxo de pré-lançamento** (PR [#38](https://github.com/robertoepifanio-byte/SHAREO/pull/38), squash `6b9bb7f`): fecha o ciclo divulgação → lista de interessados → cidade-piloto → convite → 1º acesso com link de indicação. Novo **`/admin/fundadores`** (nav "Interessados", SUPERADMIN+OPERACIONAL): `FounderLead` agrupados por cidade/estado (ranking — total, intenção anunciar/alugar, PENDING/INVITED/CONVERTED, indicados via `referredById`); botão "Convidar (N)" por cidade (confirma — dispara e-mails). `POST /api/admin/founders/invite` passou a aceitar **`city`** (antes só `wave`/`state`). Nova **`/bem-vindo`** (onboarding do 1º login — `/definir-senha` redireciona p/ `/login?callbackUrl=/bem-vindo`): explica navegar (cadastro simples) × anunciar/alugar (completar) e destaca "Gerar meu link de indicação"; e-mail de convite menciona indicação. Rastreio **já estava pronto** (`/cadastro?ref=` aplica `applyReferralCode` no signup). D4: gera+acumula, payout bloqueado. **(2) Precificação do anúncio (bug do tester, 2 PRs):** a "Sugestão" semanal/mensal era calculada a partir da diária **mínima da faixa** (valor do bem), não mudava ao alterar a diária digitada → PR [#37](https://github.com/robertoepifanio-byte/SHAREO/pull/37) (`6bf90fd`) passou a basear na diária atual + botão "Recalcular"; retorno do tester pediu **automático** → PR [#39](https://github.com/robertoepifanio-byte/SHAREO/pull/39) (`3d9c4d6`): ao mudar a diária, semana/mês **não customizados** se atualizam sozinhos (× multiplicador), preservando valores digitados à mão (`weekTouched`/`monthTouched`); "Recalcular" só p/ campos customizados. **(3) UX config embaixadores** (PR [#36](https://github.com/robertoepifanio-byte/SHAREO/pull/36), `6af74e3`): em `/admin/embaixadores` o botão "Salvar" só habilita com alteração ("Sem alterações" caso contrário) + confirmação ao ligar "Payout habilitado" (pós-D4). **(4) Copy de pagamento desatualizada** (PR [#40](https://github.com/robertoepifanio-byte/SHAREO/pull/40), `9f0c258`): removidas as frases "Pagamento liberado só após confirmação da retirada" (trust box do item) e "repassado via PIX 3 dias após a devolução confirmada" (tela de sucesso) — política de repasse agora é **semanal, toda segunda-feira**. **⚠️ Deploy do #40 pendente:** o "Deploy Staging" do `9f0c258` falhou por **limite diário do Vercel free** (`api-upload-free`, >5000 uploads/dia após ~14 deploys no dia) — NÃO é código; #37/#39/#38 já subiram (auto-recalc no ar). Decisão: **re-disparar quando a cota resetar (~24h)** via `gh run rerun 27713608806 --failed`; fix futuro = `vercel deploy --archive=tgz`. Ver memórias [[project-prelaunch-pilots]] e [[project-vercel-free-upload-limit]].

**✅ s23 — Correções do painel admin + robô resiliente a 429** (2026-06-17). **(1) "Painel Admin" deslogava o admin** (bug do tester): ao clicar em qualquer rota `/admin`, o admin era redirecionado p/ Início **deslogado**. Causa: ao rebaixar/desativar um admin gravava-se `admin:blocked:<userId>` no Redis (TTL 30d), checada no `middleware.ts` via `isAdminBlocked` → redirecionava **qualquer** token daquele userId p/ `/sair`, inclusive um **login novo e válido** de admin re-promovido. Fix (PR [#33](https://github.com/robertoepifanio-byte/SHAREO/pull/33), squash `da9e3d6`): removidos `redisKey`/`blockAdminToken`/`isAdminBlocked`/`unblockAdminToken` de `lib/redis-admin-blocklist.ts`; passa a usar **só o epoch** já existente (`invalidateUserSessions`/`isSessionStale`, SEC-CRIT-04) — mata sessões **antigas** (token com `loginAt` < epoch), mas o login novo reflete o estado atual e o `authorize()` barra conta inativa; a rota admin do middleware só barra quem não é (mais) `ADMIN` pelo token. A chave órfã do `admin@shareo.com.br` foi **deletada** via REST API do Upstash staging (banco `modest-ocelot-143145`) → destravado na hora. **Senha do admin documentada estava errada** (`ShareO@2026`) → corrigida p/ **`Admin@shareo2026`** (do seed) no CLAUDE.md (`18b5f55`); `financeiro@`/`operacional@` NÃO vêm do seed (criados via UI, `d9b763a`). **(2) "Ver conversa" da disputa dava 404** (PR [#34](https://github.com/robertoepifanio-byte/SHAREO/pull/34), squash `0a1674b`): o link em `/admin/disputas` apontava p/ `/mensagens/[id]`, que faz `notFound()` p/ quem não é participante (o admin nunca é) e ainda marca como lido / abre o chat com envio. Fix: nova rota `app/admin/disputas/[id]/page.tsx` (id = conversationId) = **visão SOMENTE LEITURA** no layout admin (sem checagem de participante, sem read-receipts, sem envio; item + locatário/proprietário com selo de papel + histórico + "Ver reserva"). Verificado no preview local logado como admin não-participante: `/mensagens/<conv>`=404 (bug), `/admin/disputas/<conv>`=200. **(2b) "Ver reserva" no Financeiro/Disputas também dava 404** (PR [#35](https://github.com/robertoepifanio-byte/SHAREO/pull/35), squash `1495c52`) — mesmo padrão: os links apontavam p/ `/reservas/[id]`, gated por participante (`app/reservas/[id]/page.tsx:140`); criada `app/admin/reservas/[id]/page.tsx` = detalhe **read-only** no layout admin (partes, período, split financeiro priorizando `platformFeeAmount`/`ownerNetAmount` persistidos, observações/disputa, link p/ a conversa); os 3 links do admin (Financeiro + a conversa do #34) repontados p/ `/admin/reservas/[id]`. **Padrão geral:** página de usuário gated por participante (`/mensagens/[id]`, `/reservas/[id]`) nunca serve p/ admin → sempre criar visão read-only sob `/admin/*`. **(2c)** `admin@shareo.com.br` promovido de `ADMIN_OPERACIONAL`→**`ADMIN_SUPERADMIN`** no staging (UPDATE Prisma + bump de epoch p/ forçar re-login). **⚠️ Lição (custou 2 round-trips de CI):** criar uma **rota de página** nova faz `<a href="/rota-interna">` **pré-existentes** (em `app/admin/disputas/page.tsx` e `app/admin/page.tsx`) passarem a casar com uma página → erro `@next/next/no-html-link-for-pages`; o **cache do ESLint do Next** (`.next/cache/eslint`) mascara isso localmente (não re-linta arquivos não alterados) e só o CI pega — trocar por `<Link>` ou limpar o cache antes do push. **(3) Robô daily-sim resiliente a 429** (PR [#32](https://github.com/robertoepifanio-byte/SHAREO/pull/32), squash `fe36065`): o bypass `x-e2e-token` nunca pegou no staging (E2E_SECRET de `.env.staging-migrate` tem 32 chars, runtime usa 64) → o robô tomava 429 sob carga (ex. `--repasse`); agora o `http.json` detecta **429**, lê `Retry-After` e **re-tenta sozinho** (até 5×, teto ~65s).

**✅ s22 — Fix de comissão na reserva + copy do rodapé + Robô de Validação Diária** (2026-06-17). **(1) Comissão (bug do tester Raimundo):** a tela `/reservas/[id]` calculava a taxa como `totalPrice − dailyPrice×totalDays` → **R$ 0,00** em reserva diária (e negativo com cupom). Modelo correto (confirmado: checkout + protótipo) — o locatário paga só a locação; a ShareO **retém 15% do repasse ao dono**. Fix usando `calcSplit()` (mesma função do checkout): a tela mostra Total da locação + Taxa retida + repasse (rótulo por perfil "Você recebe"/"Proprietário recebe"); a `_PriceCalc` parou de **somar** a taxa ao total do locatário (mostrava R$ 184 numa locação de R$ 160). Rodapé: tagline trocada p/ "Por que comprar se você pode alugar? / Marketplace de geração de renda…". PR [#27](https://github.com/robertoepifanio-byte/SHAREO/pull/27) (squash `0bd57d5`) — CI + Deploy Staging + E2E Regressão verdes. **Só exibição — checkout/booking intactos.** **(2) Robô de Validação Diária** (`scripts/daily-sim.ts` + README, PR [#28](https://github.com/robertoepifanio-byte/SHAREO/pull/28) `9271f75`): acionado manual 1×/dia, simula um dia de operação (cadastro→anúncio→locação→Pix fictício→retirada/devolução→chat→repasse). **Híbrido** (decisão dos fundadores): fluxos críticos via API HTTP real (exercita gates/validações/split), apoio via Prisma. `--env local`(default)`|staging` (prod bloqueada por D4), desfechos ponderados, logs em `scripts/daily-sim-logs/`; `scripts/` fora do build/CI. **Bug de ambiente corrigido** (PR [#29](https://github.com/robertoepifanio-byte/SHAREO/pull/29) `4278af1`): `loadEnvFile` "primeiro-vence" deixava o Prisma no banco **LOCAL** durante `--env staging` (o shell já tinha `DATABASE_URL` local) → `markEmailVerified` dava P2025. Fix: `loadEnvFile` **sobrescreve** (o arquivo do `--env` é a fonte da verdade) + Prisma usa `DIRECT_URL` + `dbRetry`. `E2E_SECRET` copiado p/ `.env.staging-migrate`. **Gotcha Windows:** `npm`/`npx`/`tsx` (.ps1) são bloqueados pela política do PowerShell → usar **cmd** ou `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned`. **Staging validado:** Dias 1–2 rodados limpos (10 usuários / 10 anúncios; ~6 payouts pendentes). **⚠️ Regra de retenção (instrução do usuário):** os dados PERMANECEM até a exclusão ser explicitamente solicitada — **não rodar `--reset`**. Guias `docs/guia-robo-validacao-diaria.{md,html}` mantidos **locais** (não versionados — decisão do usuário).

**✅ Release v1.6.0 — Cadastro progressivo + LGPD + Convite-piloto** (sessão s21, 2026-06-16 — PR [#26](https://github.com/robertoepifanio-byte/SHAREO/pull/26) mergeado em `main` `66bcef7`; CI 100% verde incl. E2E Playwright). Revisão do cadastro a partir do PDF dos fundadores, em 3 frentes. **(A) Cadastro progressivo:** o signup passa a ser **mínimo** (nome/e-mail/senha/cidade/estado) só para navegar; o **cadastro completo** (CPF/CNPJ + endereço) é exigido **apenas ao Anunciar ou Alugar**. Novo `User.profileCompletedAt` (+ `ageDeclaredAt`) com migration idempotente aplicada em **local e staging** (`fflpuoluiqmhpvcxubqi` via `db execute` — staging é gerido por `db push`, NÃO `migrate deploy`); página `/cadastro/completar` + `PATCH /api/users/me/complete-registration`; gates lendo do banco em `itens/novo`, `POST /api/items` e `POST /api/bookings` (403 `REGISTRATION_INCOMPLETE`) + CTA em `_PriceCalc`; helpers extraídos (`lib/registration`, `lib/forms/{masks,address}`, `lib/geocodeUser`). **Pegadinha:** `middleware.ts` tratava `/cadastro/*` como guest-only (`startsWith`) → usuário logado era jogado p/ `/dashboard` antes da página de completar rodar; `/cadastro/completar` foi excluído da regra (a página exige login). **(B) LGPD:** `lib/legal-config.ts` como fonte única de `CONSENT_VERSION`/`DPO_EMAIL` (reconcilia o `v1.0`/`v1.1` divergente); finalidade no ponto de coleta + canal do DPO no form; declaração de 18+ **persistida** (`ageDeclaredAt`); bairro opcional explícito. Itens jurídicos (idade documental, split PF×PJ, menores, RIPD) ficam **[D4]**. **(C) Convite-piloto:** `FounderLead` ganha `city`/`state` (região dos pilotos, capturados no `FounderCaptureForm`); fluxo "senha no 1º acesso" reaproveitando `PasswordResetToken` (`lib/founderInvite` + e-mail), página `/definir-senha/[token]`, `reset-password` marca o lead `CONVERTED`, trigger admin `POST /api/admin/founders/invite`. **Verificação:** build/tsc/lint/jest (450 testes) + E2E Playwright verdes; fluxo signup→gate→completar→anunciar/alugar testado no preview; CPF cifra/decifra ok. Bug do próprio PR corrigido na revisão: gate em `POST /api/items` quebrava o teste de integração (mock sem `user.findUnique`) e 2 specs E2E (signup mínimo não anuncia direto) — ambos ajustados antes do merge. **Pós-merge:** release `v1.6.0` (tag + GitHub Release, `746d718`); o workflow Deploy buildou+deployou no staging, rodou as migrations (resolve+deploy) e a **E2E Regressão Contínua (Staging) passou em 3m54s**; `Deploy Production` permanece gated por D4.

**🔒 Fix de segurança — Data API do Supabase exposta (staging)** (sessão s20, 2026-06-16). Alerta crítico do Supabase `rls_disabled_in_public` no projeto staging `fflpuoluiqmhpvcxubqi` era **real**: com RLS desligado (decisão arquitetural — NextAuth, não Supabase Auth) **+ a Data API (PostgREST) expondo o schema `public` para a chave `anon`** (pública, vai no bundle), qualquer um lia/editava/apagava todas as tabelas via `GET /rest/v1/<tabela>` (comprovado: `users` com anon = 200+linhas). **Fix (dashboard, Opção A):** Settings → API → Data API → **remover `public` dos Exposed schemas**. Resultado validado: REST anon = **404** ✅ · app/Prisma = **200** ✅ · **chat Realtime funcionando** ✅ (serviço separado, publication `supabase_realtime`, independente da Data API). **NÃO ligar RLS** (quebraria o Realtime — sem Supabase Auth não há policy). O alerta é check estático da flag RLS → pode persistir mesmo com o risco fechado. ⚠️ **Replicar na produção pós-D4** (e nunca pôr `E2E_SECRET` no runtime de prod — SEC-MAJ-07).

**✅ CI 100% verde — Lighthouse + drift de migrations + contraste/a11y + E2E resiliente** (sessão s20, 2026-06-16 — PRs [#21](https://github.com/robertoepifanio-byte/SHAREO/pull/21)/[#22](https://github.com/robertoepifanio-byte/SHAREO/pull/22)/[#23](https://github.com/robertoepifanio-byte/SHAREO/pull/23)/#25, `main` `d25eb71`→`c8c34be`). Todos os jobs verdes: **Build/Lint/Tests/Security/E2E local** (CI) + **Deploy Staging/E2E Regressão Contínua** (Deploy). **(1) Gate Lighthouse:** o `.lighthouserc.json` usava `preset: "lighthouse:no-pwa"` que assert cada audit individual em error (unused-js/css, bf-cache, meta-description, errors-in-console) — derrubava o Build mesmo com as **categorias** passando. Preset removido → gate só por categoria (perf≥0.75/a11y≥0.9 error; resto warn), que é a intenção já documentada no workflow. `errors-in-console`/`meta-description` eram transientes (propagação de deploy). **(2) Contraste/a11y:** token `destructive` #E74C3C (3.82:1, reprova AA) → **#C0392B** (5.44:1), hover #A93226; `_EnderecoForm` text-red-500→text-destructive; label-content-name-mismatch (logo/CTA), image-redundant-alt (`CategoryIcon` ganhou prop `decorative`), aria-allowed-role (`role="listitem"`→wrapper `display:contents`). axe local = **0 violações**. **(3) Drift schema×migrations** (fixar o Build destravou o job E2E local dormante → seed quebrava com `column isFounder does not exist`): várias features foram sincronizadas ao staging via `prisma db push` **sem migration** (Programa Fundadores, email-verify, tabelas `booking_photos`/`contract_acceptances`/`founder_audit_logs`, enums `CheckPhase`/+IdVerificationStatus/+NotificationType, colunas de `bookings`). DDL autoritativo gerado via `prisma migrate diff --from-url <DB pós-migrate-deploy> --to-schema-datamodel` no Postgres limpo do CI → **3 migrations idempotentes** (`20260616000000_add_founder_program`, `..001_add_enum_values` [ADD VALUE isolado], `..002_sync_remaining_drift`) que funcionam em DB limpo (CI/prod cria) e no staging (db push → no-op). **Fecha também o risco da prod futura** (DB novo por migrations estaria incompleto). **(4) E2E local** escopado p/ os specs "plan" de fluxo (espelha a Regressão de Staging; security*/cron ficam fora). **(5) E2E Regressão de Staging:** `e2e/_support.ts` `apiWithRetry()` (retry 5xx/429, backoff até ~12s) absorve 500 do pool free; **registro honra o bypass `x-e2e-token`** (`/api/auth/register` passa `req` ao checkRateLimit) com `E2E_SECRET` fiado no GitHub Secret + Vercel runtime + job CI. **Causa raiz final dos 429:** o secret `STAGING_URL` apontava p/ um deployment/alias **sem `E2E_SECRET` em runtime** — diagnóstico no CI (`echo ${#E2E_SECRET}`=64 + curl do runner: 429 vs bypass em `staging.shareo.com.br`) isolou o problema → **`STAGING_URL`→`https://staging.shareo.com.br`** → regressão **verde** (zero 429). Armadilha Windows: `prisma generate` dá EPERM com o dev server do preview rodando (lock na query_engine DLL) — usar `npx next build`.

**✅ Backlog s15–s19 registrado + P2 fechado + `/comunidade` no footer** (sessão s20, PR [#20](https://github.com/robertoepifanio-byte/SHAREO/pull/20), `d25eb71`). Backlog v3.9: nova seção "Deltas s15–s19" (staging ao vivo, e-mail do app, domínio próprio, categoria Eletrodomésticos, ícones PWA). P2 #7/#8/#9/#10 marcados concluídos — ícones/screenshots PWA, `/sobre`, `/politicas`, `/suporte`, `/comunidade` já com conteúdo real (não eram mais stubs). `/comunidade` adicionada ao footer (única sem discoverability); `/suporte` e `/politicas` ficam por URL direta (redundantes com `/ajuda`, `/termos`, `/privacidade`).

**✅ Categoria "Eletrodomésticos" + ícone por slug + novo ícone PWA + higiene de repo** (sessão s19, 2026-06-16 — `main` `30dd1fc`→`064fb74`, deployado e validado em staging). **(1) Rename de categoria:** "Casa e Cozinha" → **"Eletrodomésticos"** com slug `casa-jardim` **preservado** (não quebra URLs `?categoryId` nem itens). O nome é lido do **DB** (`categories.name`) em runtime em `/` e `/itens` → `UPDATE` aplicado nos **dois** Supabase (local `jtianehxosfdrhjzqvqj` + staging `fflpuoluiqmhpvcxubqi`) + textos de UI (`prisma/seed.ts`, `_Calculadora`, `_EarningsCalc`, `ImportForm`, hero da home). **(2) `CategoryIcon` resolve o ícone por SLUG**, não pelo nome exibido (`LUCIDE_BY_SLUG` + nova prop `slug`; as queries passam a selecionar `slug`) — renomear o rótulo nunca mais derruba o ícone. **Lição:** o DB muda na hora, mas o código precisa de **deploy**; aplicar o rename só no banco deixou o staging com rótulo novo **sem ícone** até o deploy do código. **(3) Novo ícone PWA da tela inicial** a partir do logo "SO": `pwa-icon-192/512` (any), `pwa-icon-maskable-512` (maskable, safe-zone 80% sobre navy `#001B53`) e `public/icons/shareo-logo.png` (apple-touch — **estava 404**). Fonte única `public/logos/pwa-icon-source.png`; `scripts/generate-pwa-icons.mjs` reescrito para gerar tudo a partir dela (antes desenhava o wordmark "SHARE•O" proceduralmente). **(4) Higiene de repo:** `.obsidian/` (config local do app Obsidian, versionada indevidamente) removida do git (`git rm --cached`) + adicionada ao `.gitignore`. **Validação:** deploy confirmado por hash dos assets + HTML do staging (card "Eletrodomésticos" com ícone `casa-jardim`, `maskable` 200, manifest novo). `main` local estava apenas **atrás** de `origin/main` (não divergente — `c506bba` já estava upstream) → fast-forward; branches temporárias (locais + remota `feat/…`) removidas. **Nota PWA:** o ícone na tela inicial de um celular que já tem o app instalado só troca após remover e re-adicionar (PWA faz cache do ícone).

**✅ Crítica externa (Copilot) analisada read-only + release v1.5.0 + 4 ondas de melhoria** (sessão s15, jun/14 — PR [#13](https://github.com/robertoepifanio-byte/SHAREO/pull/13) mergeado via rebase em `main`, commits `ef85d5f`→`538d35c`). Crítica genérica de Design/Usabilidade/Segurança avaliada por **2 subagentes read-only** (designer + segurança) contra o código: **~85% (design) e ~95% (segurança) improcedente** — refutada ponto a ponto com evidências `arquivo:linha`; a única verdade embutida é de **percepção** (o trabalho de design/segurança não é visível a um avaliador externo que só lê o HTML público). Cortada a release **[`v1.5.0`](https://github.com/robertoepifanio-byte/SHAREO/releases/tag/v1.5.0)** como baseline pré-melhorias (consolida hardening s13+s14; `package.json` sincronizado 1.3.0→1.5.0, corrigindo drift herdado da v1.4.0). Os poucos gaps reais corrigidos em **4 ondas**: **(1 — a11y/UI)** token `muted` declarado em `tailwind.config.ts`, `text-accent`→`text-success` no `FounderCaptureForm` (contraste 2.1→5.1:1, WCAG AA), data da privacidade jan/2025→jun/2026; **(2 — UX)** toasts Sonner (estavam montados no layout mas **inertes** — zero `toast()` em runtime) ligados em favoritar, toggle ativo/pausado e ações de booking; **(3 — visibilidade)** nova página `/seguranca` (linguagem leiga, sem expor detalhes sensíveis), `public/.well-known/security.txt` (RFC 9116), link + trust badges no footer; **(4 — hardening)** `lib/ssrfGuard.ts` agora resolve DNS (`dns.promises.lookup`) e valida cada IP → mitiga **DNS rebinding** + 42 testes unitários, e nota no `middleware.ts` documentando por que `style-src 'unsafe-inline'` permanece (Tailwind/Mapbox). **Validação:** `tsc --noEmit` limpo + **42/42 testes ssrfGuard verdes** (inclui DNS rebinding + fail-closed). **Follow-ups pós-MVP:** TOCTOU residual de DNS (exigiria IP pinning), migração `style-src`→hash SHA-256.

**✅ Auditorias críticas s13 + s14 + hardening de segurança aplicado e validado** (commits `26bbeb5`→`e8c54dc`, jun/13–14). Duas auditorias **read-only multi-aspecto** (QA/E2E + Segurança + Arquitetura, delegadas a subagentes), consolidadas em `docs/backlog-atividades-priorizadas.md` v3.8, com **reverificação do orquestrador** antes de agir. **Resolvidos e validados** (todos sem dependência externa, deliberados com o fundador):
- **s13** — stored XSS no JSON-LD (`lib/jsonLd.ts`), crons **fail-closed**, validação MIME+magic-bytes em uploads (`lib/imageUpload.ts`), **invalidação de sessão pós-troca de senha** (claim `loginAt` + epoch Redis — Caminho B sem migration), headers **COOP + CSP frame-ancestors**, `pickupToken` via `crypto.randomInt`, truncamento de lat/lng, remoção de código morto, + **10 specs E2E** cobrindo gaps (commits `26bbeb5`/`8563b03`/`ef38d34`/`57841b6`/`21ecc69`).
- **s14** — **3 Critical**: idempotência do webhook Stripe via `StripeEventQueue`; **TOCTOU** no booking `confirm` (`$transaction` serializável); TOCTOU no `mark_active` (`updateMany` condicional). Gaps de cobertura completados: lat/lng em `/api/items/[id]`+SSR; invalidação de sessão estendida a **mobile/Bearer** e **reset-password**. 2ª onda: **SSRF guard** em webhooks PJ (`lib/ssrfGuard.ts`), **CSV formula-injection** no export, **admin-role granular**, `geocodeUserLocation` via `after()` (commits `8eaf50b`/`e8c54dc`).
- **Validação:** ciclo booking PENDING→COMPLETED verde no staging; lat/lng truncada confirmada via curl; SSRF guard 9 casos unit; `session-invalidation` E2E verde. `tsc`+`build` verdes em todas as ondas.
- **Lição reforçada:** sempre reverificar achados de subagente — o s13 teve 3 falsos positivos (SEC-CRIT-03, SEC-MIN-10, middleware:99); o s14 foi bem mais preciso.
- **Permanecem para deliberação:** `pickupToken @unique` (migration), PlatformConfig cache (staleness×perf), error-envelopes/ownership-helper (refactor amplo), upload-limit drift (copy/produto), GAP-M-07b, + dependência externa (D4, rotação de secret no Vercel, upgrade de deps, decisões de produto, NextAuth GA).

**✅ Os 2 bugs funcionais reais da suíte E2E corrigidos e validados verde** (commits `767d834`→`14c51f2`, jun/13 s12). A triagem da suíte de staging (s11b: 240✅/17❌) tinha isolado apenas **2 falhas reais** — o resto é artefato de ambiente (BASE_URL, dados de teste, rate-limit sequencial). Ambas agora resolvidas e revalidadas (`6 passed` re-rodando `e2e-a11y-plan` + `phone-verification` com `BASE_URL`+`STAGING_URL` corretos). **(QA-14)** `POST /api/auth/phone/send-otp` passou a autenticar **antes** de validar o body → **401** sem sessão (era 400); stub retorna 422 enquanto a integração Zenvia + migration de schema seguem pendentes (decisão Raimundo). **(QA-01)** As 3 violações de contraste WCAG AA eram o **mesmo elemento** — footer "Desenvolvido por Pratika IA" em 3 páginas públicas. Causa: o footer tem fundo **verde `#007B3C`** (não navy) — branco a 80% dá 4,03:1 (reprova os 4,5:1), corrigido para **`text-white/90`** (4,67:1, alinhado ao resto do footer que já passava). `tsc`+`lint`+`build` ✅. Lição reforçada: conferir o fundo real do elemento antes de escolher o alpha (footer = verde; hero/menus = navy). Higiene de repo na mesma sessão: `.gitignore` ampliado para artefatos não-fonte (PDFs na raiz, `k6/last-summary.json`, PNGs de teste de logo, screenshots de debug, `.critique/`, relatório desempacotado).

**✅ Auditoria técnica por 6 aspectos + execução dos 5 achados** (commits `5cf77c1`→`7eb66c2`, jun/13 s9). Novo `relatorio-auditoria-tecnica.html`: cada aspecto (Performance/UX/Design/Funcionalidades/Segurança/Estratégia) verificado contra o código — nota geral **A−**; riscos genéricos de template desmontados com evidência (não é SPA → SEO 100; XSS em forms não se aplica → Zod+CSP). Os 5 achados sem bloqueador foram **todos executados** (`e04d6a1`, `206a259`): **(P1)** `public/icones/` removido (~15 MB, zero refs — os usados ficam em `public/icons/` EN); **(P2)** PWA icons 944 KB→10/64 KB via sharp (fonte era 1254px); **(P2)** 10 `<img>`→`next/image` — 3 previews `blob:` mantidos de propósito (next/image não otimiza object URL); **(P3)** Mapbox `ItemsMap` com `onError` (degrada só se não carregar) + Resend `sendWithRetry` nos e-mails críticos (verificação/reset) em `lib/email.ts`; **(P3)** baseline Lighthouse **mobile** (Perf 92 · A11y 97 · BP 96 · SEO 100 · LCP 1.6s · CLS 0.008) em `docs/lighthouse-baseline-mobile.md` (o anterior era desktop, só perf 97). `tsc`+`lint`+`build` ✅. Os dois relatórios executivos também atualizados (`033c4c0`): **207 funcionalidades / 27 categorias / 52 endpoints**.

**✅ Avaliações exibidas por completo** (commit `cdf677f`, jun/12). O form de avaliação já coletava emoji de satisfação, critérios por estrela (P3-67/68/69) e foto do item em uso, mas esses dados nunca eram exibidos. Novo `components/reviews/ReviewDetails.tsx` renderiza tudo nos cards de avaliação da página do item e do perfil público. Os demais itens do bloco "features médias" já existiam: badges/Conquistas no perfil público, templates de chat, webhooks PJ.

**✅ Re-auditoria de promessas — v2.0** (commit `409b547`, jun/12). Cada um dos 64 itens da v1.0 foi verificado contra o código: **32 já resolvidos** (a v1.0 ficou obsoleta no mesmo dia e continha erros de catalogação — ex.: webhooks PJ já existiam completos). Doc reorganizado por bloqueador: 9 itens D4, 11 decisão de fundadores, 6 executáveis sem bloqueador (verificação PIX, k6, KPIs GA4, cupom por avaliar, dicas de anfitriões, gate Lighthouse), 6 H2/H3. Regra nova: verificar UI no código antes de declarar item "sem UI".

**✅ Fix crítico — e-mails de verificação não chegavam + links 404** (commits `28ca951`–`5c55582`, jun/12). Duas causas raiz: (1) e-mails disparados fire-and-forget morriam quando a lambda congelava após a resposta — `after()` do `next/server` aplicado em ~30 rotas; (2) `NEXTAUTH_URL` vazia no Vercel + fallback com `??` (que não captura string vazia) gerava links quebrados — criado `lib/app-url.ts` com `||` + strip de barra final, e a var foi recriada limpa no Vercel. UI de reenvio em `/perfil/seguranca` agora mostra o motivo real (429 → "limite atingido, tente em N minutos" via Retry-After; rate limit é 3/hora).

**✅ Bloco rápido jun/12 s7b** (commit `1c6a7dc`). (1) Stats da homepage agora reais do banco: itens disponíveis, proprietários ativos, avaliação média (mín. 5 reviews) e categorias — staging mostra 27 itens / 2 proprietários (decisão aberta: fundadores podem preferir copy aspiracional). (2) Ordenação "Mais alugados" (`?sort=rented` por contagem de bookings) no Explorar. (3) E2E PriceCalc estabilizado — 18/18 em 3 execuções paralelas (fix de race de hidratação + seletor escopado) + teste novo do teto R$500. (4) Verificado que já existiam (doc promessas desatualizado): link Indicações, filtro minRating, cron ambassador-decay. (5) P1/P2 sem bloqueador (`287c64c`): getAutoCancelConfig, getPayoutWindowDays, getUploadLimits, RATE_LIMITS nomeado em 13 endpoints, alerta R$500 no PriceCalc, `await` em payout.create. Suíte: 23 suítes / 408 testes ✅.

**✅ Staging re-seedado por capitais** (jun/12). 29 itens demo antigos de Natal soft-deletados (preservados itens E2E — a "Câmera Fixture E2E Smoke Test" tem 235 bookings da suíte); `seed-demo-items.ts` recriou 21 itens, 3 por capital (SP, RJ, BH, Curitiba, POA, Salvador, Recife), **todos com foto validada**. Regra: só sobe item com foto — os 8 itens do `prisma/seed.ts` (sem imagens) não foram recriados. Validado no banco e na API pública.

**✅ Fixes UI da sessão jun/12** (commits `9a7e395`–`031ed30`). (1) Badge do sino zera ao ler a mensagem — abrir conversa marca notificações `NEW_MESSAGE` como lidas + novo `PATCH /api/notifications/[id]/read` + clique individual no dropdown (optimistic). (2) Compartilhamento embaixador: og:description do /cadastro e template WhatsApp reescritos. (3) Endereço: dados salvos exibidos; placeholders "Ex: …" removidos; criar conta sem cidade/UF pré-preenchidos. (4) Telefone do perfil: placeholder "55 99 999999999" + normalização para `+55DDDNÚMERO` no submit.

**✅ Lançamento nacional — referências a Natal/RN removidas** (commit `7b83b4a`, jun/12). Decisão dos fundadores: o ShareO lança em âmbito nacional. UI sem Natal/RN hardcoded (placeholders, Lista VIP, e-mails de fundador, metadados "em todo o Brasil"); depoimentos fictícios com cidades variadas (SP/BH/Curitiba/POA); mapa default centra no Brasil (zoom 4) para usuário sem cidade; /sobre sem o stat "cidade de origem". Seeds (`prisma/seed.ts`, `scripts/seed-demo-items.ts`) espalham itens por 8 capitais. ADR-002 e ADR-015 receberam notas de atualização (premissas geográficas superadas). Cidade real do usuário logado continua exibida normalmente.

**✅ Bugfix crítico — pickupToken no fluxo PIX** (commit `db9397d`, jun/10). O token de retirada só era gerado pelo webhook do Stripe; no fluxo PIX (MVP) o `mark_active` falharia sempre com `TOKEN_REQUIRED`. Agora o token é gerado no `confirm` quando ainda não existe, e o `GET /api/bookings/[id]` retorna o token (locatário exibe o código). Suite nova `e2e/features-jun09.spec.ts`: **16/16 ✅** (token, actualTime, multiplicadores, 3 fotos, taxa dinâmica). booking-flow 5/5 ✅, financeiro 40/40 ✅ (testes defasados corrigidos: 7 cards de métricas, locator de hrefs).

**✅ Crítica de design implementada — 3 prioridades** (commits `e62e8ef`/`8998860`, jun/10). (1) Seed de demonstração: `scripts/seed-demo-items.ts` — 21 itens com fotos Unsplash validadas, 3 por categoria, bairros reais de Natal; listagem deixou de mostrar itens de teste com imagem preta. (2) Detalhe do item mobile: grid com `order` — preço + CTA "Solicitar locação" acima da dobra, antes da descrição. (3) `/ajuda`: guias "Primeiros Passos" em `<details>` fechados por padrão — altura mobile caiu de ~16.100px para ~9.600px (FAQs já tinham accordion+busca via `HelpSearch`).

**✅ Taxa da plataforma 100% dinâmica** (commit `31068af`). Zero hardcodes de "15%" em todo o codebase — sempre lido de `PlatformConfig` via `getPlatformFeeRate()`. Multiplicadores semanal (3×) e mensal (15×) configuráveis pelo SuperAdmin em `/admin/financeiro`. Central de ajuda, calculadoras e todas as páginas de conteúdo atualizam automaticamente quando o SuperAdmin altera a taxa.

**✅ Módulo Financeiro MVP completo.** Fases 0–4 implementadas e deployadas em staging (commit `65cbf67`). Fluxo financeiro ponta-a-ponta: PIX cadastrado → checkout com split → payout automático → four-eyes admin → chargebacks → exportação CSV → informe IR → relatório mensal. Suite E2E financeiro: **33 testes** (suite base: 87 passed). ADRs 012–020 documentados.

**✅ Gestão de admins via UI** (commit `d9b763a`). Tela `/admin/usuarios/admins` exclusiva para SUPERADMIN: criar admins, alterar `adminRole` por dropdown, ativar/desativar — todas as ações auditadas no `AdminLog`. Admins criados nos dois bancos (local + staging): `admin@shareo.com.br` (SUPERADMIN), `financeiro@shareo.com.br` (FINANCEIRO), `operacional@shareo.com.br` (OPERACIONAL).

**✅ Conteúdo pós-módulo financeiro atualizado** (commit `d9b763a`). Central de ajuda, ganhar/page, Seguranca, reservas/sucesso: taxa corrigida para 15%, caução removida do MVP, PIX + hold 3 dias documentados, limite R$ 500, rota real informe IR. Hero: "casa e jardim" → "casa e cozinha".

**✅ Segurança admin hardening (sessão noite jun/05)** — commits `a75c015`→`4148f30`. Nav filtrada por role, guards em todas as rotas admin, blocklist Redis (Edge-compatible), rate limiting, audit log antes/depois, invalidação de sessão pós-troca de senha, formulário inline de senha para admins, tsconfig corrigido, ESLint a11y corrigido, SENTRY_AUTH_TOKEN expirado removido, webhook Vercel reativado via CLI.

**✅ Testes E2E admin** — `e2e/admin-usuarios.spec.ts` — **28 passed / 0 failed / 6 skipped** (por design) contra staging `shareo-rouge.vercel.app`. Sessions fixture: `session-admin.json`, `session-financeiro.json`, `session-operacional.json`.

**✅ Validação manual staging** (05/06/2026) — todos os fluxos validados: login por role, restrições de rota, painel financeiro, exportação CSV, contas PIX, segurança admin, gestão de admins.

**✅ UX admin segregada** (commit `46ea0e6`) — link "Painel Admin" no dropdown/mobile; menu "Anunciar" oculto para admins; seção "Atividade" substituída por atalhos admin; banner upgrade PJ oculto em `/perfil`.

**✅ P0 sem bloqueador externo resolvidos** (jun/12). (1) 6 scripts temporários deletados do repo. (2) 4 hardcoded P0 movidos para `PlatformConfig`: política de cancelamento (horas + percentuais), thresholds de tier embaixador (Prata/Ouro), janela de referral e multiplicador de late fee. Cada um tem função assíncrona em `lib/platform-config.ts` com fallback para o valor anterior como default. TypeScript limpo.

**✅ Auditoria de promessas — 64 itens catalogados** (jun/12). PO levantou todas as features prometidas (ADRs, schema, protótipo, backlog) que ainda não estão implementadas ponta-a-ponta. Documento completo em `docs/promessas-nao-implementadas.md`. Bloqueadores identificados: D4 trava 8 itens críticos; 16 valores hardcoded sem bloqueador externo (P0–P2); 7 gaps protótipo vs. app. Top P0 imediatos: scripts temporários, `/politicas` stub, limiares embaixador hardcoded, janela referral hardcoded, política de cancelamento hardcoded.

Próximo passo: resolver P0 sem bloqueador externo (hardcoded + scripts) → aguardar D4 (jurídico) para go-live em produção.

---

## Fases concluídas

| Fase | Destaques |
|---|---|
| **H1 MVP** | Auth JWT, CRUD itens, busca/filtros, chat Supabase Realtime, avaliações, favoritos, admin |
| **H2 Growth** | E-mail transacional (Resend), analytics PJ, vitrine `/loja/[slug]`, CSV import, PJ Premium |
| **H3 Scale** | Stripe Checkout Sessions, late fee, outbound webhooks, React Native scaffold |
| **Pós-H3** | SEO (sitemap/robots), PWA (manifest/service worker), geocoding automático, mapa Mapbox |
| **v2 UX** | Paleta Navy/Verde, BookingProgressBar (5 etapas), FilterBottomSheet, StickyBookingCTA |
| **Pós-v2** | Confirmação devolução bilateral, status DRAFT p/ itens sem foto, área de perfil (7 sub-páginas) |
| **Sessões jun/04** | Nav reestruturada (dropdowns), HelpButton, MobileMenu expansível, pills de categoria 2 linhas |
| **Módulo Financeiro (jun/05)** | PIX, split, payout, four-eyes, chargebacks, exportação CSV, informe IR, relatório mensal |
| **Sessão jun/05 tarde** | Gestão de admins via UI, conteúdo central de ajuda corrigido, admins criados em local + staging |
| **Sessão jun/05 noite** | Hardening segurança admin (nav por role, guards, blocklist Redis, rate limit, audit log, senha inline), E2E admin spec, fix build Vercel (Sentry + Edge Runtime + tsconfig + a11y) |
| **Sessão jun/05 tarde/noite 2** | E2E admin 28/0/6 ✅ contra staging, validação manual completa ✅, UX admin segregada (Painel Admin no menu, Anunciar oculto para admins, atalhos admin no dropdown/mobile, banner PJ oculto) |
| **Sessão jun/06** | GitGuardian fix (senhas movidas para env vars, histórico scrubado), id-docs bug fix (getPublicUrl → storage path), security2 25/0/3 ✅, security3 12/12 ✅ (smokes #23-#27), fix crítico: middleware retorna 401 para /api/* sem auth (era 307 redirect) |
| **Sessão jun/09** | Token retirada + endereço anti-golpe, prazo por horário real, repasse segundas, 3 fotos MVP, multiplicadores SuperAdmin, taxa 100% dinâmica, STATUS.md + relatório executivo com arquitetura produção, briefing D4 |
| **Sessão jun/10 manhã** | Bugfix pickupToken PIX (gerado no confirm), fix a11y labels painéis retirada/devolução, e2e/features-jun09 16/16 ✅, crítica de design + 3 prioridades (seed demo 21 itens, detalhe mobile CTA acima da dobra, /ajuda accordion −40% altura), testes defasados corrigidos (financeiro 7 cards, ajuda-plan taxa dinâmica) |
| **Sessão jun/10 tarde/noite** | Verificação de identidade: e-mail de resultado aprovação/rejeição (sendIdVerifiedEmail + sendIdRejectedEmail via Resend), fix upload mobile (removido capture forçado, compressão Canvas client-side máx 4MB), hint câmera na selfie, log detalhado erro Supabase, bucket id-docs criado no staging. Backlog v3.2: item P3 #25 verificação celular Zenvia (decisão Raimundo). Aguarda deploy manual (`npx vercel --prod`) para tester validar. |
| **Sessão jun/11** | Programa de Embaixadores (ADR-022): schema com AmbassadorProfile/Referral/AmbassadorCommission, migration local, lib/ambassador.ts (funções puras), lib/referral.ts reescrito (Referral PENDING em vez de ReferralCredit), webhook Stripe integrado, cron ambassador-decay, /api/ambassador/consent, /perfil/embaixador (painel tier/link/histórico), /admin/embaixadores (métricas + config SuperAdmin). Payout bloqueado (ambassadorPayoutEnabled=false) até D4. Commit 538b7f4. |

---

## Módulo Financeiro — Detalhe (jun/05)

| Fase | Itens | Commit |
|---|---|---|
| Fase 0 | ADRs 012–017 | `2ef6445` |
| Fase 1 | AdminRoles, schema financeiro, cadastro PIX, checkout split, teto R$500 | `2ef6445` |
| Fase 2 | Cron payout, trigger 3 dias, dashboards GMV/receita, audit log, four-eyes | `2ef6445` |
| E2E base | 21/22 testes passando | `80e7640` |
| Fase 3 | FIN-7 chargebacks (webhook dispute) | `31082cf` |
| Fase 3 | FIN-8 exportação CSV síncrona/assíncrona + ExportJob | `8038c8f` |
| Fase 4 | Informe IR, relatório mensal, card reavaliação Stripe Connect | `b66490c` |
| Docs | ADRs 018–020, CLAUDE.md atualizado | `65cbf67` |

**Decisões dos fundadores (D1–D4):**
- D1 (pagamentos, **superado — ver decisão vigente abaixo**): originalmente PIX centralizado no MVP, Stripe Connect BR reavaliado ~dez/2026. Evoluiu para Mercado Pago Modelo B (ADR-026, 28/06/2026) e depois reverteu para **Stripe Connect como PSP definitivo** ([ADR-028](adr/ADR-028-reversao-stripe-connect.md), 19/08/2026, ver entrada do dia no topo deste arquivo) — construção ainda não iniciada; Mercado Pago dormente; PIX manual removido do código.
- D2: Sem caução, teto R$500 por transação
- D3: AdminRoles implementadas (SUPERADMIN, FINANCEIRO, OPERACIONAL)
- D4: Consulta jurídica pendente — **go-live produção bloqueado até retorno**

---

## Infra e ambiente

| Item | Status |
|---|---|
| Staging Vercel (`shareo-rouge.vercel.app`) | ✅ Deployado |
| Supabase staging (`zythygwvmrwrqmnrdufq`, sa-east-1) | ✅ Ativo — org corporativa, PRO (migrado 2026-06-27, org upgrade 2026-08-04) |
| Supabase local dev (`kehbrjlllfkooauaswtp`, sa-east-1) | 🗑️ **Deletado em 2026-08-04** — uso real mínimo (1 usuário, 8 itens, 0 reservas), ~$10/mês economizados |
| Supabase antigos (`fflpuoluiqmhpvcxubqi` staging / `jtianehxosfdrhjzqvqj` dev) | 🗑️ **Deletados em 2026-07-22** — org pessoal, DNS não resolve mais |
| CI/CD GitHub Actions (lint → test → build → deploy) | ✅ Ativo |
| UptimeRobot em `/api/health` (5min) | ✅ Ativo |
| Vercel Cron — reminders, auto-cancel, expire, return, reengagement, payout | ✅ Ativos |
| Vercel Cron — monthly-report (1º/mês 12h UTC) | ✅ Registrado em `vercel.json` |
| Sentry (`sentry.client/server/edge.config.ts`) | ✅ Source maps + alertas ativos |
| Upstash Redis (rate limiting) | ✅ Ativo |
| PWA | ✅ Ícones + screenshots + service worker validados |
| Supabase **production** | ⏳ Aguarda D4 (consulta jurídica) |

---

## Testes

| Suite | Status |
|---|---|
| Unit — `bookings`, `pricing`, `crypto`, `auth`, `rateLimit`, `middleware`, `haversine`, `co2`, `format`, `geo` | ✅ Escritos |
| Integration — `bookings/patch`, `bookings/reviews`, `auth/register`, `conversations/messages`, `items/get/post/patch` | ✅ Escritos |
| E2E Playwright staging — suite base | ✅ **87 passed / 0 failed / 20 skipped** |
| E2E Playwright — módulo financeiro (`e2e/financeiro.spec.ts`) | ✅ **33 testes** (21 base + FIN-7/8/Fase4) |
| E2E Playwright — `e2e/security2.spec.ts` (smokes #18–#22) | ✅ **25 passed / 0 failed / 3 skipped** contra staging |
| E2E Playwright — `e2e/security3.spec.ts` (smokes #23–#27) | ✅ **12 passed / 0 failed / 12 skipped** contra staging |
| `coverageThreshold` — 70% para `pricing`, `crypto`, `bookings` | ✅ Configurado |

---

## Releases

| Tag | Commit | Data | Descrição |
|---|---|---|---|
| `v1.5.0` | `80946df` | 2026-06-14 | Hardening de Segurança (auditorias s13+s14) — baseline; +4 ondas pós-crítica Copilot até `538d35c` |
| `v1.4.0` | `823387b` | 2026-06-13 | Configurabilidade, Conformidade & Auditoria Técnica (29 commits desde v1.3.0) |
| `v1.3.0` | `60b5b92` | 2026-06-12 | Lançamento Nacional + Programa de Embaixadores |
| `v1.2.0` | `e20c1e9` | 2026-06-10 | Módulo financeiro + segurança locação + UX |
| `v1.0.0` | `40d1a2b` | 2026-06-04 | Staging validado — ponto de recuperação estável |
| `v1.0` | — | pré-v2 | Estado antes da v2 UX |

---

## Pendências antes de produção

### ✅ P0, P1, P2 — Todos concluídos
### ✅ Módulo Financeiro MVP — Completo

### ⏳ Bloqueadores de produção

| Item | Detalhe |
|---|---|
| **D4 — Consulta jurídica** | Nenhum go-live em produção antes do retorno do jurídico |
| **Supabase production** | Criar 3º projeto Supabase isolado + GitHub environment `production` com Required Reviewers |
| ~~**Validação manual staging financeiro**~~ | ✅ **Concluída** — todos os fluxos validados em 05/06/2026 |

### 🟢 Próximas frentes (pós-produção)

| Item | Detalhe |
|---|---|
| FIN-6 — Caução | Implementar após V1-Financeiro estável em produção |
| Stripe Connect | **PSP definitivo decidido** ([ADR-028](adr/ADR-028-reversao-stripe-connect.md), 19/08/2026) — não é mais "reavaliação futura". Construção pendente: onboarding do proprietário (Custom/Express), checkout com split real, webhooks de Connect, NFS-e sobre a comissão. Mercado Pago (ADR-026) dormente; desligar `mercadoPagoEnabled` em staging é pendência de execução separada. |
| IR — PDF formal | ADR-019: sem PDF no MVP, adicionar em V1+ se houver demanda |
| Relatório mensal por e-mail | ADR-020: V1+ quando SendGrid/Resend configurado |
| P3 backlog | Lighthouse, k6, gamificação, Expo, CO₂ — ver `docs/backlog-atividades-priorizadas.md` |

---

## Decisões técnicas consolidadas

| Decisão | Escolha |
|---|---|
| Auth | NextAuth.js v5 — JWT sem PrismaAdapter (`authorize()` → `prisma.user.findUnique` direto) |
| RLS | **Desabilitado** — incompatível com PgBouncer. Segurança via `ownerId !== session.user.id → 403` |
| Upload | Supabase Storage via service role key server-side |
| Geocoding | Mapbox API fire-and-forget em `lib/geocodeItem.ts` |
| Filtro distância | Haversine em JS pós-fetch (não no Prisma) |
| NEXT_PUBLIC_* no Vercel | **Nunca marcar como Sensitive** — não é injetado no build |
| Dark mode | Fora do escopo — não planejado para H1 |
| WhatsApp | Fora do escopo — decisão explícita do produto |
| Caução | Adiada para pós V1-Financeiro (D2) |
| Testes E2E staging | `workers: 1` obrigatório — estado compartilhado entre specs |
| Pagamentos | PSP definitivo: **Stripe Connect** ([ADR-028](adr/ADR-028-reversao-stripe-connect.md), 2026-08-19) — construção não iniciada. Mercado Pago (ADR-026) dormente. PIX manual da plataforma removido do código. |
| Exportação financeira | Síncrono ≤90 dias, assíncrono >90 dias (ADR-016) |
| Retenção de dados | 5 anos CTN Art. 173, anonimização LGPD (ADR-017) |

---

## Sessões recentes

| Sessão | Destaques |
|---|---|
| **jun/06** | GitGuardian fix (senhas movidas para env vars, histórico scrubado), id-docs bug fix, security2 25/0/3 ✅, security3 12/12 ✅ (smokes #23-#27), fix crítico: middleware retorna 401 para /api/* sem auth |
| **jun/07** | ViaCEP auto-fill endereço, CSP atualizado (`viacep.com.br`), Lighthouse CI smoke #32 ✅ |
| **jun/08** | Logo recortado (sharp) 1254×1254→1216×275, ícone mobile atualizado, taxa plataforma fix `10% → 15%` |
| **jun/09** | Multiplicadores semanal (3×) / mensal (15×) configuráveis SuperAdmin; taxa 100% dinâmica (zero hardcode); ajuda page atualizada (seguro/extravio, indicação, limite R$1.000, precificação 3–5%); fotos MVP limitadas a 3 |
| **jun/12 s7** | Hardcoded P0/P1/P2 → PlatformConfig (cancelamento, tiers embaixador, referral, late fee, auto-cancel, payout window, uploads); CEP ViaCEP no cadastro; RATE_LIMITS nomeado; auditoria de promessas (64 itens); suíte 408 testes verde |
| **jun/12 s7b** | Fix e-mails (after() em ~30 rotas + NEXTAUTH_URL recriada + lib/app-url.ts); stats homepage reais; sort "Mais alugados"; E2E PriceCalc 18/18 estável + teste teto R$500; UI reenvio de verificação mostra motivo real (429) |
| **jun/12 s7c** | ReviewDetails.tsx exibe emoji/critérios/foto nos cards de avaliação (item + perfil público); re-auditoria promessas v2.0 — 32/64 resolvidos, doc reorganizado por bloqueador |
| **jun/13 s8** | P-1..P-6 sem bloqueador (`bd53b26`): PIX checklist admin, k6 50VUs + gru1, GA4 6 eventos, Coupon model/migration/lib + checkout com split-absorção, dicas anfitrião, Lighthouse CI gate |
| **jun/13 s12** | Corrigidos os 2 bugs funcionais reais da suíte E2E: **QA-14** phone-otp 401 antes do body (`767d834`) e **QA-01** contraste WCAG no footer verde → `text-white/90` (`14c51f2`); revalidados verde (`6 passed`). `.gitignore` ampliado p/ artefatos não-fonte |
| **jun/14 s15** | Crítica externa (Copilot) analisada read-only por designer+segurança (~85–95% improcedente, refutada com evidência); release **v1.5.0** baseline; **4 ondas** mergeadas (PR #13, rebase): token `muted` + contraste WCAG, toasts Sonner, página `/seguranca` + `security.txt` RFC 9116, `ssrfGuard` DNS anti-rebinding + 42 testes. `tsc` limpo + 42/42 ssrfGuard ✅ |
| **ago/27** | **Suíte E2E de staging de 66 falhas a ZERO — e nenhuma das 66 era, no fim, um defeito de produto isolado.** Três causas de infra explicavam a maioria, todas do mesmo padrão *fixture que não é fixture*: (1) [create-admin-sessions.ts](../scripts/create-admin-sessions.ts) importava de `playwright`, pacote que **não é dependência** — morria em MODULE_NOT_FOUND antes da 1ª linha, e por isso as sessões financeiro/operacional estavam congeladas em 05/06; (2) as contas `financeiro@`/`operacional@` **não existiam mais** (sumiram no wipe de 07/08, nunca estiveram no seed); (3) o item de `test-item-id.json` **não existia** (404) e nada validava — `POST /api/bookings` respondia 422 e ~30 specs caíam sem apontar a causa. 🪤 **O achado mais instrutivo veio da autorrevisão, não da investigação:** eu "consertei" o provisionamento de fixture no script sem ver que o `deploy.yml` já fazia o mesmo UPDATE em SQL cru — e que ele listava só **2 dos 3** fixtures, deixando o admin de fora. Minha correção teria criado uma segunda fonte da verdade em vez de eliminar a primeira. 🪤 **Dois testes estavam verdes sem provar nada:** o smoke de pins do Mapbox casava um bottom-sheet oculto e caía em annotation silenciosa; e a harness derrubava o mapa por CORS (o `x-e2e-token` global virava preflight nos tiles) — o produto estava são o tempo todo. 🪤 **Duas asserções eu mesmo afrouxei** para ficar verde e a revisão pegou: prazo de disputa com regex genérica aceitaria "3 dias", justamente o que o #387 unificou em 5; e a taxa condicionada à existência da linha virava no-op. **Bug de produto real:** tap target de 32px violando os 44px do DS — o E2E pegou 1, a revisão achou outros 3 idênticos. **PRs [#389](https://github.com/robertoepifanio-byte/SHAREO/pull/389) e [#390](https://github.com/robertoepifanio-byte/SHAREO/pull/390).** Local: 66→0 falhas, 217→274 passes. CI: `E2E Regressão Contínua` de cronicamente vermelho para **41 passed, 0 failed**. Também: `E2E_SECRET` rotacionado nos 3 lugares (o local defasado mascarava 22 falhas fantasma), texto da 2ª locação restaurado na Ajuda (perdido junto com a caução em 9b9d5ba) e 15 reservas ACTIVE residuais soft-deletadas do item fixture. **Dependabot fechado no mesmo dia: 7 → 0 alertas.** `nanoid` e `postcss` sairam sozinhos com a subida do SDK do Expo (confirma a decisao de 19/08 de esperar a trilha upstream em vez de forcar override no bundle mobile); os 5 restantes foram dispensados com justificativa e gatilho de reabertura, nenhum alcancando o runtime web. 🪤 A triagem corrigiu uma premissa das rodadas anteriores: `uuid` nao era so trilha Expo — entrou no runtime web via `exceljs`, dependencia que NOS mesmos introduzimos ao trocar `xlsx` na rodada 1. A conclusao sobreviveu (o exceljs chama so `v4`, e a falha e em v3/v5/v6 com `buf`), mas por pouco. |
| **ago/25** | **Cancelamento por quem cancela + janela assimétrica de disputa + fixture admin de CI de verdade — tudo verificado em device real.** [#386](https://github.com/robertoepifanio-byte/ShareO/pull/386) reescreve reembolso de cancelamento: era por antecedência (100/70/50%), agora é por quem cancela (locador cancela → 100%; locatário cancela → 100% menos a taxa **real** da Stripe via `balance_transaction.fee`, nunca estimada). [#387](https://github.com/robertoepifanio-byte/ShareO/pull/387) torna a janela de disputa assimétrica (locatário só com reserva `ACTIVE`; locador só após devolução, até 48h) e unifica as duas rotas de abertura de disputa (`PATCH action=open_dispute` e `POST /dispute`) num único `lib/openDispute.ts`. [#388](https://github.com/robertoepifanio-byte/ShareO/pull/388) corrige bug pré-existente (não-regressão, confirmado por run anterior ao mesmo dia): `promoteToAdmin()` em `create-staging-fixtures.ts` só imprimia instrução manual, nunca promovia de verdade — o "E2E Regressão Contínua" ficava permanentemente vermelho nos specs admin, mascarando regressão real; `deploy.yml` ganhou `DIRECT_URL_STAGING`. Verificado no pipeline real: 39 passed vs. 28 antes. **APK do dia (commit `38a7bee`) instalado e testado no Moto G56 via USB** — texto da política de cancelamento e da janela de disputa conferem verbatim com o site; ScreenHeader e PjGate (de PRs anteriores do dia) também confirmados na tela. Gap real encontrado: app mobile só liga a entrada de "completar cadastro" em 2 das 4 telas do site (dashboard + item) — carrinho e bem-vindo nem existem como telas no app. |
| **jul/22** | **Limpeza dos Supabase antigos + CI verde nos 7 checks.** Auditoria dos refs em uso → os 2 projetos da org pessoal estavam mortos/órfãos e foram **deletados**. 4 PRs: **#238** ref morto fora de 7 arquivos (3 guards de banco abortavam sempre; fallbacks para host inexistente) + **senha de banco em texto puro** removida de 2 scripts; **#239** `tar>=7.5.19` (GHSA-23hp-3jrh-7fpw, advisory nova derrubou o audit sem mudança de código) + `pnpm.overrides` migrado para `pnpm-workspace.yaml` — **os pins de `@types/react` do mobile estavam silenciosamente inativos** desde o pnpm 11; **#241** teste do calendário mobile usava data fixa (`2026-07-10`) que virou passado e derrubou o CI sozinho em 16/07 — `isPast` tem precedência sobre `isOcc`, componente estava correto; **#242** rótulo do banco no `.ps1` de migrations financeiras mentia o destino → passa a **derivar o ref da URL real**. Varredura do histórico: nenhuma chave dos projetos deletados vazada, só 1 senha (projeto morto). 6 regras com credencial em texto puro removidas do `.claude/settings.local.json`. **+6 PRs de doc/UI no mesmo dia:** #243/#244/#245 (inventário Supabase + cabeçalho de 24 KB numa linha só quebrado em parágrafos), #246 (TEST-BL1/BL2 já resolvidos pelo #168 desde 04/07; TEST-BL4 estreitado), #247 (stack mobile no CLAUDE.md era `NativeWind`, medição real: 58 `StyleSheet` / 57 `useTheme()` × 5 `className`), #237 (cores hardcoded → tokens, rebaseado antes de mesclar; coração favoritado mudou `#E74C3C`→`#C0392B` no claro). **Higiene:** 76→7 branches, 3→1 worktrees; apagada a remota `docs/guia-testar-app-android` (tip `815808d` = commit órfão da Moda). **10 PRs mesclados, 0 abertos ao final.** |

---

## Arquitetura de Produção (planejamento pós-D4)

> Toda esta seção é **planejamento** — nenhuma ação executada antes do sign-off D4.

### Infraestrutura de serviços

**Atualizado em 2026-08-04** — org Supabase confirmada em **Pro** (screenshot real do painel; corrige a versão anterior desta tabela, que dizia "Free tier"/"org FREE só comporta 2 projetos" — não é mais verdade). Estrutura de custo confirmada **ao vivo na tela de criação de projeto**: **~US$25/mês de base** (plano Pro da org) **+ ~US$10/mês por projeto adicional** — não é um valor fixo isolado de $10/projeto como uma versão anterior desta tabela chegou a registrar. Vercel Pro e Supabase Pro já são licenças **ativas**, não mais projeção futura — mas os valores exatos cobrados hoje **não foram verificados nesta sessão** (sem acesso a token/API de billing); marcados como "checar no painel" onde não há confirmação direta.

| Serviço | Plano atual (staging) | Plano produção | Custo/mês |
|---|---|---|---|
| **Vercel** | **Pro (ativo)** — Team `shareo-marketplace` | Mesmo plano | ~US$20/mês *(preço de tabela público — checar valor real no painel de cobrança)* |
| **Supabase — base Pro da org** | **Pro (ativo)** — org `Shareo Marketplace de aluguel` | Mesmo plano | **~US$25/mês confirmado** (base da org, cobre o(s) projeto(s) incluído(s)) |
| **Supabase — projeto adicional** | `shareo-prd` (`jdxdndrhjxtkaifbpagr`) **criado 2026-08-05**, uso interno | Mesmo projeto | **+~US$10/mês confirmado, por projeto além do incluído na base** |
| **Upstash Redis** | Pay-as-you-go | Pay-as-you-go (rate limit + blocklist) | ~US$0–5/mês *(não verificado)* |
| **Resend (e-mail)** | Free (100/dia) | Starter US$20/mês — 50.000 e-mails/mês | ~US$20/mês *(não verificado)* |
| **Sentry** | Free (5k eventos/mês) | Team US$26/mês se ultrapassar | ~US$0–26/mês *(não verificado)* |
| **Mapbox — mapa (GL JS)** | Free — **50.000 map loads/mês** (`mapbox-gl` v3.3.0 + `react-map-gl`, token `NEXT_PUBLIC_MAPBOX_TOKEN`) | Pay-as-you-go acima do free: $5,00/1k (50-100k) → $4,00/1k (100-200k) → $3,00/1k (200k-1M) | US$0/mês hoje *(confirmado em mapbox.com/pricing, 04/08)* |
| **Mapbox — Geocoding API** | Free — **100.000 requests/mês** (`lib/geocodeItem.ts`, fire-and-forget no anúncio) | Pay-as-you-go acima do free: $0,75/1k (100-500k) → $0,60/1k (500k-1M) | US$0/mês hoje *(confirmado em mapbox.com/pricing, 04/08)* |
| **Mercado Pago** | Test/sandbox mode | Live mode — % sobre transação | % sobre GMV |
| **UptimeRobot** | Free (5 min interval) | Free suficiente para MVP | US$0 |
| **Apple Developer Program** | ⏳ Sem conta ainda — iOS Fase 1 é só scaffolding/CI (ver [[project-app-ios-fase1]]) | Necessário p/ teste em device real, TestFlight e publicação na App Store (Fase 2, deferida) | ~US$99/**ano** (~US$8,25/mês) *(preço de tabela público Apple — não verificado, é individual, não a org)* |

**Estimativa infra mês 1:** ~US$73–103/mês (Supabase = ~$25 base + ~$10 do `shareo-prd`, já criado, = ~$35; + Apple Developer ~$8/mês quando a Fase 2 iOS começar; resto não verificado nesta sessão).

**Pré-requisitos Mapbox (confirmado 04/08):** modelo puramente pay-as-you-go, **sem assinatura fixa obrigatória** — só o token de acesso já configurado (`NEXT_PUBLIC_MAPBOX_TOKEN`). Mapa e Geocoding são produtos/cotas **separados**, cada um com seu free tier próprio (não compartilham). Obrigação contratual do ToS: atribuição de marca visível ("© Mapbox © OpenStreetMap") no rodapé/mapa — já rastreado na tabela de licenças (linha `mapbox-gl`, item 6 abaixo).

### Projetos Supabase — 3 ambientes isolados

| Ambiente | Project ID | Uso |
|---|---|---|
| Local dev | 🗑️ excluído 2026-08-04 (era `kehbrjlllfkooauaswtp`) | Recriar quando precisar de `next dev` com banco local |
| Staging | `zythygwvmrwrqmnrdufq` | Validação CI/CD — dados de fixture |
| **Produção** | ✅ `jdxdndrhjxtkaifbpagr` (`shareo-prd`), criado 2026-08-05 | **Uso interno only** (equipe técnica + fundadores, atrás de Vercel Deployment Protection) — dados reais de usuários **só pós-D4 + domínio real**, nunca misturar |

Staging na org corporativa `Shareo Marketplace de aluguel` (**PRO** desde 04/08, NANO, sa-east-1), migrada em 2026-06-27. Os refs antigos da org pessoal (`jtianehxosfdrhjzqvqj` dev / `fflpuoluiqmhpvcxubqi` staging) foram **deletados em 2026-07-22** — se aparecerem em doc ou script, é resíduo. Org já em Pro → sem limite de contagem de projetos; custo é ~US$25/mês de base + ~US$10/mês por projeto além do incluído na base (confirmado 04/08).

**Regra absoluta:** variáveis de env de produção (`DATABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXTAUTH_SECRET` etc.) **nunca** no `.env`, apenas em GitHub Secrets + Vercel env `production`.

### Variáveis de ambiente por ambiente

| Variável | Local | Staging | Produção |
|---|---|---|---|
| `DATABASE_URL` | Supabase local | Supabase staging | Supabase produção (novo) |
| `NEXTAUTH_URL` | `http://localhost:3000` | `https://shareo-rouge.vercel.app` | `https://shareo.com.br` (domínio real) |
| `NEXTAUTH_SECRET` | Qualquer string | Secret staging | Secret produção — **diferente** |
| `NEXT_PUBLIC_MAPBOX_TOKEN` | Token dev | Token staging | Token produção — quota separada |
| `RESEND_API_KEY` | Opcional | Key staging | Key produção |
| `STRIPE_SECRET_KEY` | `sk_test_...` | `sk_test_...` | `sk_live_...` — só após D4 |
| `CRON_SECRET` | *(em env/GitHub Secrets — nunca versionar; repo público)* | Staging value | Novo secret produção |
| `ENCRYPTION_KEY` | 64 hex chars | Staging value | **Novo** — rotacionar antes de prod |

### Plano de provisionamento produção (pós-D4)

```
1. Criar Supabase production (sa-east-1, mesmo region do staging)
2. Executar migrations: prisma migrate deploy --schema=prisma/schema.prisma
3. Seed inicial: pnpm db:seed (apenas categorias + PlatformConfig defaults)
4. Criar Vercel project "shareo-production" ou configurar environment "production" no projeto atual
5. Configurar domínio (shareo.com.br ou equivalente) no Vercel
6. GitHub environment "production" com Required Reviewers (mínimo 1 aprovação para deploy)
7. Adicionar todas as variáveis de produção (NUNCA marcar NEXT_PUBLIC_* como Sensitive)
8. Smoke test manual nos fluxos críticos antes de abrir para usuários
9. Ativar Stripe live mode + webhook live endpoint
10. Tag web-v1.12.0
```

### Limites operacionais do MVP (produção)

| Limite | Valor | Configurável? |
|---|---|---|
| Valor máximo do bem anunciado | R$1.000 por item | SuperAdmin — `maxItemValueCents` |
| Teto por transação | R$500 (`CHECKOUT_MAX_CENTS = 50000`) | `lib/platform-config.ts` |
| Taxa da plataforma (default) | 15% (`DEFAULT_FEE_RATE = 1500` bps) | SuperAdmin via `/admin/financeiro` |
| Multiplicador semanal (sugerido) | 3× a diária | SuperAdmin via `/admin/financeiro` |
| Multiplicador mensal (sugerido) | 15× a diária | SuperAdmin via `/admin/financeiro` |
| Fotos por item | 3 no MVP | Hard-coded `_ItemImageUpload.tsx` |
| Rate limit (default) | 20 req/10s por IP | Upstash Ratelimit |
| Sessão JWT access token | 15 min | `lib/auth.ts` |
| Retenção de dados financeiros | 5 anos | ADR-017 (CTN Art. 173) |
| Cutoff payout semanal | Domingo 23h59 BRT | `/api/cron/payout` toda segunda |

### Licenças de dependências críticas

| Pacote | Versão | Licença | Ponto de atenção |
|---|---|---|---|
| `next` | 15.3.3 | MIT | OK |
| `next-auth` | 5.0.0-beta.31 | ISC | **Beta** — monitorar breaking changes pré-GA |
| `@prisma/client` | 6.7.0 | Apache-2.0 | OK |
| `stripe` | 22.1.1 | MIT | Termos Stripe (2.99% + R$0.39 BR) — verificar com jurídico |
| `mapbox-gl` | 3.3.0 | BSD-3 + Mapbox ToS | Mapbox ToS exige atribuição de marca |
| `react-map-gl` | 7.1.7 | MIT | OK — wrapper sobre mapbox-gl |
| `@supabase/supabase-js` | 2.46.0 | MIT | OK |
| `resend` | 4.0.0 | MIT | ToS Resend — sem spam, opt-out obrigatório |
| `@sentry/nextjs` | 8.14.0 | MIT + Sentry ToS | DSN separado para produção |
| `bcryptjs` | 2.4.3 | MIT | OK |
| `jose` | 5.9.0 | MIT | OK |
| `sharp` | 0.33.4 | Apache-2.0 | OK |
| `lucide-react` | 0.468.0 | ISC | OK |
| `@upstash/ratelimit` | 2.0.8 | MIT | OK |
| `zod` | 3.23.8 | MIT | OK |

**Aviso:** `next-auth` ainda está em beta (v5 beta.31). Antes do go-live em produção, avaliar se há versão estável disponível ou documentar risco aceito.

### Pontos de atenção pré-produção

| # | Item | Risco | Ação |
|---|---|---|---|
| 1 | **D4 — Jurídico** | Bloqueador total | Aguardar sign-off formal |
| 2 | **Stripe live mode** | Financeiro | Ativar apenas após D4; verificar KYC Stripe BR |
| 3 | **NextAuth v5 beta** | Técnico médio | Checar changelog antes do go-live; planejar migração se sair GA com breaking changes |
| 4 | **RLS desabilitado** | Segurança | Compensado por guards server-side; documentar e revisar com jurídico (LGPD) |
| 5 | **ENCRYPTION_KEY rotação** | Segurança | Gerar nova key para produção — dados criptografados em staging são ilegíveis em prod (esperado) |
| 6 | **Mapbox ToS atribuição** | Legal | Footer/mapa deve exibir "© Mapbox © OpenStreetMap" |
| 7 | **Resend domínio verificado** | E-mail | Configurar SPF/DKIM para `@shareo.com.br` antes de enviar e-mails transacionais |
| 8 | **Scripts temporários** | Segurança | Deletar 6 scripts antes de qualquer go-live (listados abaixo) |
| 9 | **Supabase Storage buckets** | Ops | Criar buckets `item-images`, `booking-photos`, `id-docs` no projeto produção |
| 10 | **Cron secrets distintos** | Segurança | `CRON_SECRET` produção deve ser diferente do staging |

### Scripts temporários a deletar antes de produção

```
scripts/reset-fixture-pwd.ts
scripts/delete-e2e-admins.ts
scripts/clear-rl.mjs
scripts/fix-admin-roles.ts
scripts/set-fixture-admin-role.ts
scripts/verify-admin-sessions.ts
```
