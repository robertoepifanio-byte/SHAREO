# Dependências
node_modules/
.pnp
.pnp.js

# Build
.next/
out/
dist/

# Variáveis de ambiente — NUNCA commitar
.env
.env.local
.env.*.local
.env.staging
.env.production

# Testes
coverage/
playwright-report/
playwright-report-staging/
playwright-report-visual/
test-results/
# Session fixtures e dados de teste — não commitar
e2e/fixtures/session-*.json
e2e/fixtures/test-item-id.json
e2e/fixtures/test-booking-id.json

# Prisma
prisma/migrations/dev.db
prisma/migrations/dev.db-journal

# IDE
.vscode/
.idea/
.obsidian/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Sentry
.sentryclirc

# Vercel
.vercel
tsconfig.tsbuildinfo

# Expo
apps/mobile/.expo/

# Arquivos temporários Word
Documentos/giv_temp.docx
Documentos/giv_unpacked/
.env*
e2e-plan-report.json
e2e-auth-plan-report.json
e2e-share-plan-report.json
e2e-suite-report.json
# Resultados por plano da suite E2E — gravados em disco porque o worker
# reinicia depois de uma falha e leva o acumulador em memoria junto.
.e2e-planos/
e2e-a11y-report.json
e2e-crossbrowser-report.json
e2e-performance-report.json
e2e-home-report.json
e2e-security-report.json
e2e-usability-report.json
e2e-integration-report.json
e2e-governance-report.json
e2e-ajuda-report.json

# Lighthouse — artefatos brutos (baseline humano fica em docs/lighthouse-baseline-mobile.md)
lighthouse-report.json
lighthouse-mobile-baseline.report.json
lighthouse-mobile-baseline.report.html

# k6 — saída bruta de run de carga
k6/last-summary.json

# Relatórios PDF exportados na raiz (versão fonte é o .html em docs/)
/*.pdf

# Pasta residual de ícones em PT (substituída por public/icons/ em EN — s9)
icones/

# Experimentos de recorte de logo / screenshots de debug (não-fonte)
public/logos/pratika-ia-*.png
scripts/*.fixture.png
scripts/debug-*.png

# Artefatos de ferramentas (code-review/critique, relatórios desempacotados)
.critique/
docs/relatorio-pagamentos-unpacked/

# Robô de validação diária — logs/relatórios/estado (dados de simulação)
scripts/daily-sim-logs/

# Binarios derivados de docs — PDFs/DOCX sao gerados dos .md (pipeline md-to-pdf)
# ou vivem no Drive; nao versionar (repo incha e binario nao faz diff)
docs/**/*.pdf
docs/**/*.docx

# Copias locais de builds do app (EAS) — APK de ~200 MB por build. Guardadas aqui
# porque o artefato do EAS expira e ja foi apagado por engano (10/08), mas nao
# versionadas: o binario e derivado do commit e nao faz diff.
builds/
*.apk
*.aab

# Placeholder "Em breve" servido no shareo.com.br pelo Netlify — NAO e publicado
# a partir deste repositorio (nao ha netlify.toml aqui). Entrou por engano num
# `git add` amplo em 12/08 e foi destrackeado; ignorado para nao voltar sozinho.
# Os arquivos seguem em disco.
public/em-breve/

# Certificados digitais (e-CNPJ / A1) — NUNCA versionar: repositorio PUBLICO.
# O .pfx contem a chave privada da empresa. Os arquivos reais foram movidos
# em 20/08/2026 para ..\ShareO-certificados\ (fora da arvore do repo); estas
# regras existem para o caso de uma copia reaparecer aqui dentro.
*.pfx
*.p12
*.cer
*.crt
*.key
Certificado_Digital*

# Backups manuais (scripts/backup-manual.sh) — NUNCA versionar.
# Este repositorio e PUBLICO e o backup contem dump do banco e o bucket
# id-docs, ou seja documentos de identidade de usuarios reais.
backups/

# Lixo da CLI do Supabase (npx supabase ... cria isto)
supabase/.temp/
